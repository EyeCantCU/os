package:
  name: py3-csvw
  version: 3.5.1
  epoch: 0
  description: Python library to work with CSVW described tabular data
  copyright:
    - license: Apache-2.0

vars:
  pypi-package: csvw

environment:
  environment:
    SAMPLE_CSVW_FILE: https://raw.githubusercontent.com/cldf/csvw/5bc712fe5e8353ec602eb758522ab674adf86753/tests/fixtures/test.tsv
    SAMPLE_JSON_FILE: https://raw.githubusercontent.com/cldf/csvw/5bc712fe5e8353ec602eb758522ab674adf86753/tests/fixtures/zipped-metadata.json
  contents:
    packages:
      - py3-supported-build-base-dev

data:
  - name: py-versions
    items:
      3.10:
      3.11:
      3.12:
      3.13:

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/cldf/csvw.git
      tag: v${{package.version}}
      expected-commit: 65ce65616e5d8d81416c74083965284e33fc7a24

  - runs: sed -i 's/rfc3986<2/rfc3986/g' setup.cfg

subpackages:
  - range: py-versions
    name: py${{range.key}}-${{vars.pypi-package}}
    description: ${{vars.pypi-package}} installed for python${{range.key}}
    dependencies:
      runtime:
        - py${{range.key}}-attrs
        - py${{range.key}}-babel
        - py${{range.key}}-colorama
        - py${{range.key}}-frictionless-bin
        - py${{range.key}}-isodate
        - py${{range.key}}-jsonschema
        - py${{range.key}}-language-tags
        - py${{range.key}}-python-dateutil
        - py${{range.key}}-rdflib
        - py${{range.key}}-requests
        - py${{range.key}}-rfc3986
        - py${{range.key}}-uritemplate
    pipeline:
      - uses: py/pip-build-install
        with:
          python: python${{range.key}}
      - uses: strip
    test:
      environment:
        contents:
          packages:
            - jq
      pipeline:
        - uses: test/tw/pip-check
        - uses: python/import
          with:
            python: python${{range.key}}
            import: ${{vars.pypi-package}}
        - runs: |
            python${{range.key}} <<EOF | jq ".[].province" | grep -F -e "Hello"
            import json
            from csvw import CSVW
            data = CSVW('${SAMPLE_CSVW_FILE}')
            print(json.dumps(data.to_json(minimal=True)))
            EOF

  - range: py-versions
    name: py${{range.key}}-${{vars.pypi-package}}-bin
    description: Executable binaries for ${{vars.pypi-package}} installed for python${{range.key}}
    dependencies:
      provider-priority: ${{range.value}}
      provides:
        - py3-${{vars.pypi-package}}
        - py3-${{vars.pypi-package}}-bin
      runtime:
        - py${{range.key}}-${{vars.pypi-package}}
    pipeline:
      - uses: split/bin
    test:
      environment:
        contents:
          packages:
            - curl
            - jq
            - sqlite
      pipeline:
        - runs: |
            set -o pipefail
            csvw2datasette --help | grep -F -e "Convert CSVW to data for datasette"
            csvw2json --help | grep -F -e "Convert CSVW to JSON"
            csvw2markdown --help | grep -F -e "show this help message and exit"
            csvw2sqlite --help | grep -F -e "show this help message and exit"
            csvwdescribe --help | grep -F -e "Describe a (set of) CSV file(s)"
            csvwvalidate --help | grep -F -e "Validate a (set of) CSV file(s) described by CSVW metadata."

            CSVW_FILE="$(mktemp)"
            chmod -v 644 "${CSVW_FILE}"
            curl -fsSLo "${CSVW_FILE}" "${SAMPLE_CSVW_FILE}"

            JSON_FILE="$(mktemp)"
            chmod -v 644 "${JSON_FILE}"
            curl -fsSLo "${JSON_FILE}" "${SAMPLE_JSON_FILE}"

            csvw2json "${CSVW_FILE}" | jq -rc .tables[].row[].describes[] | grep -F -e "Hello\\tworld\\t1"

            csvw2markdown "${JSON_FILE}" | grep -F -e "Described by [$(basename "${JSON_FILE}")]"
            touch /tmp/zipped.csv
            csvw2sqlite "${JSON_FILE}" "out.db"
            sqlite3 ./out.db -- .tables | grep -F -e "zipped.csv"
            csvwdescribe "${JSON_FILE}" | jq -rc '."dc:conformsTo"' | grep -F -e "data-package"
            csvwvalidate "${JSON_FILE}" | grep -F -e "OK"

update:
  enabled: true
  git:
    strip-prefix: v

test:
  pipeline:
    - uses: test/emptypackage
