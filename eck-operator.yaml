package:
  name: eck-operator
  version: 2.12.1
  epoch: 0
  description: Elastic Cloud on Kubernetes
  copyright:
    - license: Elastic License 2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go
      - make
  environment:
    CGO_ENABLED: "0"

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/elastic/cloud-on-k8s/
      tag: v2.12.1
      expected-commit: a56215f7ac6766b9d39cbc745cbd4939c49aee26

  - uses: go/bump
    with:
      deps: google.golang.org/protobuf@v1.33.0 golang.org/x/net@v0.23.0

  - runs: |
      mkdir -p ${{targets.contextdir}}/conf
      install -Dm644 ./config/eck.yaml ${{targets.contextdir}}/conf/eck.yaml
      make go-build
      install -Dm755 elastic-operator ${{targets.contextdir}}/usr/bin/elastic-operator

  - uses: strip

subpackages:
  - name: "${{package.name}}-compat"
    # https://github.com/elastic/cloud-on-k8s/blob/f84048e03e6fcb7774b284e28d5eb3eabee234c0/build/Dockerfile#L54
    description: "Compatibility package to place binaries in the location expected by upstream Dockerfile"
    pipeline:
      - runs: |
          mkdir -p "${{targets.contextdir}}"
          ln -sf /usr/bin/elastic-operator ${{targets.contextdir}}/elastic-operator

update:
  enabled: true
  github:
    identifier: elastic/cloud-on-k8s
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - ${{package.name}}-compat
  pipeline:
    - runs: |
        [ -f /usr/bin/elastic-operator ] && exit 0 || exit 1
        /elastic-operator --version
        elastic-operator --version
