package:
  name: chart-iamguarded-cloudnative-pg
  version: 0.1.11
  epoch: 0
  description: cloudnative-pg iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: cloudnative-pg/0.1.11
      expected-commit: 268cf663c07e40f91b72ef133171b93dea1ef0e8

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/cloudnative-pg

  - uses: charts/package
    with:
      chart-path: iamguarded/cloudnative-pg

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: cloudnative-pg/
    use-tag: true
    tag-filter-prefix: cloudnative-pg/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /cloudnative-pg
