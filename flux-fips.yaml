package:
  name: flux-fips
  version: "2.7.1"
  epoch: 0 # GHSA-f9f8-9pmf-xv68
  description: Open and extensible continuous delivery solution for Kubernetes. Powered by GitOps Toolkit.
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - bash
      - build-base
      - busybox
      - ca-certificates-bundle
      - curl
      - git
      - go-fips
      - kustomize-fips

pipeline:
  - uses: git-checkout
    with:
      expected-commit: ca29bb1a41d662495cbf3a8ee6dba7f088ae7310
      repository: https://github.com/fluxcd/flux2
      tag: v${{package.version}}

  - runs: |
      mkdir -p "${{targets.destdir}}"/usr/bin
      make cmd/flux/.manifests.done
      CGO_ENABLED=1 go build -ldflags="-w -X main.VERSION=${{package.version}}" -o ./bin/flux ./cmd/flux
      install -Dm755 bin/flux ${{targets.destdir}}/usr/bin/flux

  - uses: strip

subpackages:
  - name: "flux-fips-compat"
    description: "Compatibility package to place binaries in the location expected by upstream helm charts"
    pipeline:
      - runs: |
          # The helm chart expects the flux binaries to be in /usr/local/bin instead of /usr/bin
          mkdir -p "${{targets.subpkgdir}}"/usr/local/bin
          ln -sf /usr/bin/flux ${{targets.subpkgdir}}/usr/local/bin/flux

update:
  enabled: true
  github:
    identifier: fluxcd/flux2
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - ${{package.name}}-compat
  pipeline:
    # the flux when passing the -h flag exit with error code 2
    - runs: |
        set +e
        flux version --client
        flux --version
        flux --help
    - uses: test/go-fips-check
