package:
  name: chart-iamguarded-nginx
  version: "22.1.0"
  epoch: 0
  description: nginx iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: nginx/${{package.version}}
      expected-commit: e044aecf0bfede5c2a12eb0ecb920cd5ef11b82d

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/nginx

  - uses: charts/package
    with:
      chart-path: iamguarded/nginx

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: nginx/
    use-tag: true
    tag-filter-prefix: nginx/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /nginx
