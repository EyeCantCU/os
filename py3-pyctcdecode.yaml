package:
  name: py3-pyctcdecode
  version: 0.5.0
  epoch: 0
  description: A fast and feature-rich CTC beam search decoder for speech recognition
  copyright:
    - license: Apache-2.0

vars:
  pypi-package: pyctcdecode

environment:
  contents:
    packages:
      - py3-supported-build-base-dev

data:
  # Kenlm currently only supports these
  - name: py-versions-kenlm
    items:
      3.10:
      3.11:
      3.12:

  - name: py-versions
    items:
      3.13:

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/kensho-technologies/pyctcdecode.git
      tag: v${{package.version}}
      expected-commit: 0ca5ccc213dd26b7950a86d67938f8937b9c4486

  - runs: sed -i "/numpy/d ; s@dependencies \= \[@&\n\ \ \ \ \"numpy\"\,@" ./pyproject.toml

subpackages:
  - range: py-versions-kenlm
    name: py${{range.key}}-${{vars.pypi-package}}
    description: ${{vars.pypi-package}} installed for python${{range.key}}
    dependencies:
      runtime:
        - py${{range.key}}-hypothesis
        - py${{range.key}}-numpy-2.2
        - py${{range.key}}-pygtrie
        - py${{range.key}}-kenlm
    pipeline:
      - uses: py/pip-build-install
        with:
          python: python${{range.key}}
      - uses: strip
    test:
      pipeline:
        - uses: test/tw/pip-check
        - uses: python/import
          with:
            python: python${{range.key}}
            import: ${{vars.pypi-package}}

  - range: py-versions
    name: py${{range.key}}-${{vars.pypi-package}}
    description: ${{vars.pypi-package}} installed for python${{range.key}}
    dependencies:
      runtime:
        - py${{range.key}}-hypothesis
        - py${{range.key}}-numpy-2.2
        - py${{range.key}}-pygtrie
    pipeline:
      - uses: py/pip-build-install
        with:
          python: python${{range.key}}
      - uses: strip
    test:
      pipeline:
        - uses: test/tw/pip-check
        - uses: python/import
          with:
            python: python${{range.key}}
            import: ${{vars.pypi-package}}

update:
  enabled: true
  git:
    strip-prefix: v

test:
  pipeline:
    - uses: test/emptypackage
