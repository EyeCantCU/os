package:
  name: grafana-8
  version: 8.5.27
  epoch: 1
  description: The open and composable observability and data visualization platform.
  copyright:
    - license: AGPL-3.0-or-later
  dependencies:
    runtime:
      - bash
    provides:
      - grafana=${{package.full-version}}

environment:
  contents:
    packages:
      - build-base
      - busybox
      - findutils
      - git
      - go
      - nodejs-16
      - posix-libc-utils
      - py3-setuptools
      - openssl
      - python3
      - yarn
  environment:
    NODE_OPTIONS: "--max_old_space_size=10000 --openssl-legacy-provider"

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 5f9e0ace7eb4001f4106f912b0549936db87c68f
      repository: https://github.com/grafana/grafana
      tag: v${{package.version}}

  - uses: go/bump
    with:
      deps: google.golang.org/protobuf@v1.33.0 github.com/golang/protobuf@v1.5.4 github.com/elazarl/goproxy@v0.0.0-20230731152917-f99041a5c027 golang.org/x/crypto@v0.17.0 golang.org/x/net@v0.17.0 github.com/prometheus/exporter-toolkit@v0.7.2

  - runs: |
      yarn set version 3.2.2
      mkdir -p /home/build/plugins-bundled/external
      yarn install
      yarn run build
      yarn run plugins:build-bundled

      # Bump the version of wire used, the old version panics with new Go
      # https://github.com/google/wire/issues/400
      sed -i "s/v0.5.0/v0.6.0/g" .bingo/wire.mod

      make build

      mkdir -p ${{targets.destdir}}/usr/bin
      mv ./bin/linux-*/* ${{targets.destdir}}/usr/bin/

  - name: package
    runs: |
      mkdir -p ${{targets.destdir}}/usr/share/grafana/plugins-bundled
      mkdir -p ${{targets.destdir}}/usr/share/grafana/scripts
      mkdir -p ${{targets.destdir}}/usr/share/grafana/bin
      mv ./conf ${{targets.destdir}}/usr/share/grafana/
      mv ./plugins-bundled/internal ${{targets.destdir}}/usr/share/grafana/plugins-bundled/
      mv ./public ${{targets.destdir}}/usr/share/grafana/
      mv ./scripts/*.sh ${{targets.destdir}}/usr/share/grafana/scripts/
      mkdir -p ${{targets.destdir}}/etc/grafana/
      cp ${{targets.destdir}}/usr/share/grafana/conf/sample.ini ${{targets.destdir}}/etc/grafana/grafana.ini

subpackages:
  - name: ${{package.name}}-oci-compat
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/opt/grafana
          mv ./packaging/docker/run.sh ${{targets.subpkgdir}}/opt/grafana/

update:
  enabled: true
  github:
    use-tag: true
    identifier: grafana/grafana
    tag-filter-prefix: v8.
    strip-prefix: v

test:
  pipeline:
    - runs: /usr/bin/grafana-server -v
    - runs: /usr/bin/grafana-cli -v
