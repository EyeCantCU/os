diff --git a/base/glibc-compatibility/glibc-compatibility.c b/base/glibc-compatibility/glibc-compatibility.c
index 738cda47..befdb2c9 100644
--- a/base/glibc-compatibility/glibc-compatibility.c
+++ b/base/glibc-compatibility/glibc-compatibility.c
@@ -430,3 +430,48 @@ int posix_spawn_file_actions_destroy(posix_spawn_file_actions_t *fa) {
 #if defined (__cplusplus)
 }
 #endif
+
+// C23 function aliases - provide compatibility with newer C standards
+long int __isoc23_strtol(const char *nptr, char **endptr, int base) {
+    return strtol(nptr, endptr, base);
+}
+
+unsigned long int __isoc23_strtoul(const char *nptr, char **endptr, int base) {
+    return strtoul(nptr, endptr, base);
+}
+
+int __isoc23_vsscanf(const char *s, const char *format, va_list args) {
+    return vsscanf(s, format, args);
+}
+
+int __isoc23_sscanf(const char *s, const char *format, ...) {
+    va_list args;
+    int ret;
+    
+    va_start(args, format);
+    ret = __isoc23_vsscanf(s, format, args);
+    va_end(args);
+    
+    return ret;
+}
+
+// Jitter entropy stub functions - provide empty implementations
+int jent_entropy_init_ex(void) {
+    return 0;
+}
+
+void *jent_entropy_collector_alloc(void) {
+    return NULL;
+}
+
+size_t jent_read_entropy(void *ec, char *data, size_t len) {
+    return 0;
+}
+
+void jent_entropy_collector_free(void *ec) {
+    // No-op
+}
+
+const char *jent_version(void) {
+    return "stub";
+}
