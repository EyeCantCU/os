package:
  name: mx
  description: Command-line tool used for the development of Graal projects.
  version: "7.64.0"
  epoch: 0
  copyright:
    - license: GPL-2.0-only
  dependencies:
    runtime:
      - bash
      - git
      - posix-libc-utils
      - python-${{vars.python-version}}

vars:
  # 3.12 is the last version of Python to include 'pipes'
  python-version: '3.12'

environment:
  contents:
    packages:
      - busybox

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/graalvm/mx
      tag: ${{package.version}}
      expected-commit: b7a1cf518975b6f027a553e38e9f1f275416caf4
      destination: ${{targets.contextdir}}/usr/share/mx

  - runs: |
      mkdir -p ${{targets.contextdir}}/usr/bin
      ln -s /usr/share/mx/mx ${{targets.contextdir}}/usr/bin/mx

update:
  enabled: true
  ignore-regex-patterns:
    - '.*checkstyle.*'
    - '.*jackpot.*'
    - '.*jdk.*'
    - 'findbugs.*'
  git: {}

test:
  pipeline:
    - runs: |
        mx --help
        mx version | grep ${{package.version}}
