package:
  name: chart-iamguarded-superset
  version: "3.0.4"
  epoch: 3
  description: superset iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common
      - chart-iamguarded-postgresql
      - chart-iamguarded-redis

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: superset/3.0.1
      expected-commit: 504fc3fa4c12c5699e81db78db975a38a4ecbbe7

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/superset

  - uses: charts/package
    with:
      chart-path: iamguarded/superset

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: superset/
    use-tag: true
    tag-filter-prefix: superset/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /superset
