name: publish-wolfi

on:
  workflow_dispatch: {}

jobs:
  publish:
    uses: ./.github/workflows/.publish-to-public.yaml
    permissions:
      id-token: write
      contents: read
    with:
      staging_repo: chainguard-dev/wolfi-staging
      public_repo: wolfi-dev/os
