# Copyright 2024 Chainguard, Inc.
# SPDX-License-Identifier: Apache-2.0

name: Reusable Build Workflow

on:
  workflow_call:
    inputs:
      checkout_ref:
        required: false
        type: string
        description: 'Commit ref (sha, branch, or tag) to run at. Empty will get head/main.'
      cg_identity:
        type: string
        required: false
        description: "The identity to assume when interacting with chainguard."
      cloud_provider:
        required: false
        type: string
        default: "gcp"
        description: "Which cloud provider we will build the vm"
      arch:
        required: false
        type: string
        default: "x86_64"
        description: "Which acrchitecture we will build the vm"
      push_vms:
        required: false
        type: boolean
        default: false
        description: "Push the vms to the cloud provider"

    secrets:
      # TODO: cpanato
      # These will be dropped in a follow up as we cannot rotate them
      AWS_ACCOUNT_ID:
        required: false
        description: "The AWS account id to use for the marketplace role"
      # TODO: cpanato
      # This will be refactor using OIDC and not credentials
      AZURE_CREDENTIALS:
        required: false
        description: "The credentials to use for the azure login"

permissions: {}

jobs:
  init:
    runs-on: ubuntu-latest
    outputs:
      BUILD_TIMESTAMP: ${{ steps.timestamp.outputs.BUILD_TIMESTAMP }}
    steps:
      - id: timestamp
        run: |
          echo BUILD_TIMESTAMP=$(date --utc "+%Y%m%d-%H%M") >> "$GITHUB_OUTPUT"

  presubmit:
    runs-on: ubuntu-latest
    needs:
      - init

    permissions:
      contents: read # read the repo
      id-token: write # to get a token for the chainguard identity

    env:
      VM_ARCH: ${{ inputs.arch }}
      VM_CLOUD: ${{ inputs.cloud_provider }}
      BUILD_TIMESTAMP: ${{ needs.init.outputs.BUILD_TIMESTAMP }}

    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ inputs.checkout_ref || github.sha }}

      # use public chainguard provider.
      - uses: chainguard-dev/setup-chainctl@2302a56a61228140753b428d1018cb0d0addbec6 # v0.3.0
        with:
          identity: ${{ inputs.cg_identity }}

      - name: Get apk.cgr.dev token
        run: |
          chainctl auth login --identity=${CG_IDENTITY} --audience=apk.cgr.dev
          cred=$(chainctl auth token --audience=apk.cgr.dev)
          echo "HTTP_AUTH=basic:apk.cgr.dev:user:$cred" >> $GITHUB_ENV
        env:
          CG_IDENTITY: ${{ inputs.cg_identity }}

      - uses: actions/setup-go@0aaccfd150d50ccaeb58ebd88d36e91967a5f35b # v5.4.0
        with:
          go-version: stable
          check-latest: true

      # Add a block like this in a separate PR *before* landing tests in a new module.
      - name: Install grype
        if: ${{ inputs.push_vms == true }}
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin

      - name: Install deps
        run: |
          make install-deps

      - name: make clean
        run: |
          make clean

      - name: make apkoaas
        run: |
          make apkoaas

      - name: make builder
        run: |
          make builder

      - name: make disks
        run: |
          make ARCH="${{ env.VM_ARCH }}" "disks-${{ env.VM_CLOUD }}"

      - name: scan disks
        if: ${{ inputs.push_vms == true }}
        run: |
          echo "scanning ${{ env.VM_ARCH }} disks-${{ env.VM_CLOUD }}"
          # Scan for CVEs, fail early
          for disk in output/"${{ env.VM_ARCH }}"/*; do
            grype sbom:${disk}/syft.sbom.json > ${disk}/$(basename $disk)-${{ env.VM_ARCH }}-${{ env.VM_CLOUD }}-${{ env.BUILD_TIMESTAMP }}-grype.output || true
            mv ${disk}/syft.sbom.json ${disk}/$(basename $disk)-${{ env.VM_ARCH }}-${{ env.VM_CLOUD }}-${{ env.BUILD_TIMESTAMP }}-syft.sbom.json
          done

      - name: upload SBOM artifact
        if: ${{ inputs.push_vms == true }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: sbom-${{ env.VM_CLOUD }}-${{ env.VM_ARCH }}-${{ env.BUILD_TIMESTAMP }}
          path: output/${{ env.VM_ARCH }}/*/*-${{ env.VM_ARCH }}-${{ env.VM_CLOUD }}-${{ env.BUILD_TIMESTAMP }}-syft.sbom.json

      - name: upload grype scan output artifact
        if: ${{ inputs.push_vms == true }}
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: grype-${{ env.VM_CLOUD }}-${{ env.VM_ARCH }}-${{ env.BUILD_TIMESTAMP }}
          path: output/${{ env.VM_ARCH }}/*/*-${{ env.VM_ARCH }}-${{ env.VM_CLOUD }}-${{ env.BUILD_TIMESTAMP }}-grype.output

      - name: add HOME/bin to PATH
        if: ${{ inputs.push_vms == true }}
        run: |
          # ensure HOME/bin is in PATH
          d="$HOME/bin"
          mkdir -p "$d"
          p="$PATH"
          echo "original-path: $p"
          p="$d:$PATH"
          echo "new-path: $p"
          echo "PATH=$p" >> "$GITHUB_ENV"

      ## AWS
      - name: "[AWS] Setup Creds"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'aws' }}
        uses: aws-actions/configure-aws-credentials@ececac1a45f3b08a01d2dd070d28d111c5fe6722 # v4.1.0
        with:
          # the secret used is the marketplace AWS account id
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/wolfi-vms-github-postsubmit-role
          aws-region: us-east-1

      - name: "[AWS] Install Deps"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'aws' }}
        run: |
          if out=$(command -v aws); then
              echo "already had aws: $out"
          else
              curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
              unzip awscliv2.zip
              ./aws/install --install-dir $HOME/aws-cli --bin-dir $HOME/bin
          fi
          aws --version

          if ! dpkg-query --show | grep python3-venv ; then
              sudo apt-get install --assume-yes python3-venv
          fi
          mkdir -p $HOME/venvs
          python3 -m venv $HOME/venvs/awspub

          $HOME/venvs/awspub/bin/pip install awspub
          $HOME/venvs/awspub/bin/pip show awspub

          ln -s $HOME/venvs/awspub/bin/awspub $HOME/bin/

      - name: "[AWS] Publish images"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'aws' }}
        run: |
          make -j`nproc` --output-sync=recurse prodawspub BUILD_TIMESTAMP=${BUILD_TIMESTAMP} ARCH=${{ env.VM_ARCH }}

      ## AZURE
      - name: "[AZURE] Install Deps"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'azure' }}
        run: |
          # Install the azure cli
          # The script sets up the key, repos and then installs the package
          # https://learn.microsoft.com/en-us/cli/azure/install-azure-cli-linux?pivots=apt
          curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash

      - name: "[AZURE] Login"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'azure' }}
        uses: azure/login@v2
        with:
          # These credentials were generated on March 20 2025
          # They are good for 365 days. They belong to 'VM Image Uploader'
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: "[AZURE] Publish images"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'azure' }}
        run: |
          arch="${{ env.VM_ARCH }}"
          names=$(make list-azure)
          [ -n "$names" ] || { echo "make list-azure produced empty output"; exit 1; }
          [ "$arch" = "x86_64" ] && az_arch=x64 || az_arch=arm64

          echo "publishing $names to azure. arch=$arch az_arch=$az_arch timestamp=${BUILD_TIMESTAMP}"
          for name in $names; do
              ./tools/azure-image-upload  --arch $az_arch \
              --gallery vmtesting \
              --name "chainguard${name#azure}-${az_arch}" \
              --disk-name "chainguard${name#azure}-${az_arch}-${BUILD_TIMESTAMP}" \
              --image-version ${BUILD_TIMESTAMP%-*}.${BUILD_TIMESTAMP#*-}.0 \
              output/$arch/$name/disk.raw

            # Create image in EAP image gallery as well.
            if [ "$name" == "azure-docker-dev" ]; then
              # Create image in EAP image gallery as well.
              ./tools/azure-image-upload --arch $az_arch \
              --gallery chainguard_vms_eap \
              --name "chainguard${name#azure}-${az_arch}" \
              --disk-name "chainguard${name#azure}-${az_arch}-${BUILD_TIMESTAMP}" \
              --existing-disk \
              --image-version ${BUILD_TIMESTAMP%-*}.${BUILD_TIMESTAMP#*-}.0 \
              output/$arch/$name/disk.raw
            fi
          done
          exit 0

      ## GCP
      - name: "[GCP] Install Deps"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'gcp' }}
        uses: google-github-actions/setup-gcloud@77e7a554d41e2ee56fc945c52dfd3f33d12def9a # v2.1.4
        with:
          project_id: wolfi-vm

      - name: "[GCP] Setup Creds"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'gcp' }}
        id: 'auth'
        uses: google-github-actions/auth@71f986410dfbc7added4569d411d040a91dc6935 # v2.1.8
        with:
          # No configuration for now as we dont need
          workload_identity_provider: 'projects/212178706475/locations/global/workloadIdentityPools/ci-pool/providers/ci-provider'
          service_account: 'github-ci@wolfi-vm.iam.gserviceaccount.com'

      - name: "[GCP] configure gcloud"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'gcp' }}
        run: |
          gcloud config set storage/parallel_composite_upload_enabled True

      - name: "[GCP] Publish images"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'gcp' }}
        run: |
          arch="${{ env.VM_ARCH }}"
          names=$(make list-gcp)
          [ -n "$names" ] || { echo "make list-gcp produced empty output"; exit 1; }

          [ "$arch" = "x86_64" ] && garch=amd64 || garch=arm64

          echo "publishing $names to wolfi-vm. arch=$arch garch=$garch timestamp=${BUILD_TIMESTAMP}"
          for name in $names; do
              ./tools/google-image-upload --family="chainguard-${name#gcp-}-$garch" \
                --name="chainguard-${name#gcp-}-$garch-${BUILD_TIMESTAMP}" \
                --arch="$garch" \
                output/$arch/$name/disk.raw wolfi-vm-images-workloads
          done
          # TODO Need test runs here!
          echo "publishing docker-dev to chainguard-vms-eap. arch=$arch garch=$garch timestamp=${BUILD_TIMESTAMP}"
          for name in $names; do
              # Only add docker-dev images to chainguard-vms-eap.
              if [ ${name} == "gcp-docker-dev" ]; then
                GCP_PROJECT="chainguard-vms-eap" ./tools/google-image-upload \
                  --family="chainguard-${name#gcp-}-$garch" \
                  --name="chainguard-${name#gcp-}-$garch-${BUILD_TIMESTAMP}" \
                  --arch="$garch" \
                  output/$arch/$name/disk.raw wolfi-vm-images-workloads
              fi
          done

      ## QEMU
      - name: "[QEMU] Publish images"
        if: ${{ (inputs.push_vms == true) && inputs.cloud_provider == 'qemu' }}
        run: |
          arch="${{ env.VM_ARCH }}"
          names=$(make list-qemu)
          [ -n "$names" ] || { echo "make list-qemu produced empty output"; exit 1; }

          echo "publishing $names to qemu. arch=$arch timestamp=${BUILD_TIMESTAMP}"
          find output/$arch/ -type f | xargs ls -l
          exit 0
