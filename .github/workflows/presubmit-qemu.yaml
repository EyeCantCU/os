# Copyright 2025 Chainguard, Inc.
# SPDX-License-Identifier: Apache-2.0

name: "QEMU Pre-submit check"

on:
  pull_request_target:
    branches:
      - "main"

permissions: {}

jobs:
  qemu-check:
    if: ${{ github.repository == 'chainguard-dev/wolfi-vm' }}

    runs-on: ubuntu-8-core

    permissions:
      id-token: write # federated identity
      contents: read # read repo

    strategy:
      fail-fast: false
      matrix:
        arch:
          - x86_64
          - aarch64

    steps:
      - uses: step-security/harden-runner@95d9a5deda9de15063e7595e9719c11c38c90ae2 # v2.13.2
        with:
          egress-policy: audit

      - uses: chainguard-dev/actions/apt-faster@abcc11e1cf9073eff6c69e91c49756c1430b094c # v1.5.8

      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false

      - uses: './.github/actions/build-disks'
        with:
          cg_identity: "ce2d1984a010471142503340d670612d63ffb9f6/bd9fd6bb14892849"
          vm_group: "qemu"
          arch: '${{ matrix.arch }}'

      - name: make firmware
        run: |
          make "builder/ovmf-${{ matrix.arch }}.fd"

      - name: "Skip -rc images in testing"
        run: |
          names=$(make list-qemu-nonrc | tr "\n" " ")
          [ -n "$names" ] || { echo "make list-qemu-nonrc produced empty output"; exit 1; }
          echo "QEMU_VMS=$names" >> "$GITHUB_ENV"

      - uses: './.github/actions/vms-test'
        with:
          cloud_provider: "qemu"
          vm_group: "qemu"
          arch: '${{ matrix.arch }}'

      - name: "Test qemu-rc, ignoring result"
        run: |
          rm -rf ./test-results # Drop existing test results
          make ARCH='${{ matrix.arch }}' test-qemu-rc || echo "::warning title=test-qemu-rc-failure::make test-qemu-rc failed vms-test. See logs for results."

      - uses: actions/upload-artifact@330a01c490aca151604b8cf639adc76d48f6c5d4 # v5.0.0
        with:
          name: test-results-${{ matrix.arch }}-qemu-rc
          path: ./test-results/
        if: ${{ !cancelled() }}
