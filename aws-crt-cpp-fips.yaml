package:
  name: aws-crt-cpp-fips
  version: "0.33.3"
  epoch: 0
  description: "C++ wrapper around the aws-c-* libraries. Provides Cross-Platform Transport Protocols and SSL/TLS implementations for C++ (FIPS)"
  copyright:
    - license: Apache-2.0
  dependencies:
    provides:
      - aws-crt-cpp=${{package.full-version}}
    provider-priority: 20

environment:
  contents:
    packages:
      - aws-c-auth-fips-dev
      - aws-c-cal-fips-dev
      - aws-c-common-dev
      - aws-c-compression-dev
      - aws-c-event-stream-fips-dev
      - aws-c-http-fips-dev
      - aws-c-io-fips-dev
      - aws-c-mqtt-fips-dev
      - aws-c-s3-fips-dev
      - aws-c-sdkutils-dev
      - aws-checksums-dev
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake
      - s2n-tls-fips-dev
      - samurai
  environment:
    CMAKE_PREFIX_PATH: /usr/lib/aws-lc
    CFLAGS: "-I/usr/include/aws-lc"

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/awslabs/aws-crt-cpp
      tag: v${{package.version}}
      expected-commit: 5718669c6f057455263771cc4a849cdfc2d3ca0a

  - uses: cmake/configure
    with:
      opts: |
        -DCMAKE_PREFIX_PATH=/usr \
        -DCMAKE_INSTALL_LIBDIR=lib \
        -DCMAKE_INSTALL_RPATH=/usr/lib/aws-lc \
        -DBUILD_SHARED_LIBS=True \
        -DCMAKE_BUILD_TYPE=None \
        -DBUILD_DEPS=OFF \
        -DBUILD_TESTING=OFF \
        -DCMAKE_SYSTEM_NAME=Linux \
        -DCMAKE_HOST_SYSTEM_NAME=Linux \
        -Dcrypto_INCLUDE_DIR=/usr/include/aws-lc \
        -Dcrypto_LIBRARY=/usr/lib/aws-lc/libcrypto.so

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

subpackages:
  - name: aws-crt-cpp-fips-dev
    description: aws-crt-cpp-fips dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - aws-crt-cpp-fips
        - aws-c-auth-fips-dev
        - aws-c-cal-fips-dev
        - aws-c-common-dev
        - aws-c-compression-dev
        - aws-c-event-stream-fips-dev
        - aws-c-http-fips-dev
        - aws-c-io-fips-dev
        - aws-c-mqtt-fips-dev
        - aws-c-s3-fips-dev
        - aws-c-sdkutils-dev
        - aws-checksums-dev
        - s2n-tls-fips-dev
      provides:
        - aws-crt-cpp-dev=${{package.full-version}}
      provider-priority: 20

update:
  enabled: true
  github:
    identifier: awslabs/aws-crt-cpp
    strip-prefix: v
    use-tag: true

test:
  pipeline:
    - uses: test/tw/ldd-check
