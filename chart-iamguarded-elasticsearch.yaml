package:
  name: chart-iamguarded-elasticsearch
  version: "22.1.6"
  epoch: 2
  description: Elasticsearch iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common
      - chart-iamguarded-kibana

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: elasticsearch/${{package.version}}
      expected-commit: 598214de2e85201aff28d4f1b2e1806b8e7d1bd1

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: patch
    with:
      patches: values.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/elasticsearch

  - uses: charts/package
    with:
      chart-path: iamguarded/elasticsearch

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: elasticsearch/
    use-tag: true
    tag-filter-prefix: elasticsearch/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /elasticsearch
