package:
  name: chart-iamguarded-cert-manager
  version: "1.5.6"
  epoch: 0
  description: cert-manager iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: cert-manager/${{package.version}}
      expected-commit: 897192d97cfa2637a31da13d1a65f4ff924a457d

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/cert-manager

  - uses: charts/package
    with:
      chart-path: iamguarded/cert-manager

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: cert-manager/
    use-tag: true
    tag-filter-prefix: cert-manager/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /cert-manager
