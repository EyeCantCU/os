package:
  name: wavefront-collector-for-kubernetes-1.12
  version: 1.12.1
  epoch: 7
  description: Monitoring Kubernetes Clusters using Wavefront
  copyright:
    - license: Apache-2.0
  dependencies:
    provides:
      - wavefront-collector-for-kubernetes=1.12

environment:
  contents:
    packages:
      - wolfi-baselayout
      - busybox
      - build-base
      - go
      - ca-certificates-bundle

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/wavefrontHQ/wavefront-collector-for-kubernetes
      tag: v${{package.version}}
      expected-commit: 946dbf43c8cb15dee95bdf0a6a54eca51a44a0a2

  - uses: go/bump
    with:
      deps: golang.org/x/crypto@v0.17.0 golang.org/x/net@v0.17.0
      replaces: golang.org/x/text=golang.org/x/text@v0.3.8

  - uses: go/build
    with:
      modroot: .
      packages: ./cmd/wavefront-collector
      output: wavefront-collector
      ldflags: -w -X main.version=$(cat ./release/VERSION) -X main.commit=$(git rev-parse --short HEAD)

  - uses: strip

update:
  enabled: false
