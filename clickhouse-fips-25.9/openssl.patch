Description: Use system OpenSSL and enable FIPS provider
 .
 This patch modifies ClickHouse to use system OpenSSL libraries instead of bundled ones
 for FIPS compliance. It includes:
 - Using system OpenSSL via find_package instead of contrib
 - Allowing dlopen() for FIPS provider module loading
 - Configuring OpenSSL to load FIPS provider
 - Replacing legacy provider with FIPS provider
 - Ensuring proper entropy sources for FIPS-compliant DRBG
Author: Michael Paul <michael.paul@chainguard.dev>
Forwarded: not-needed

diff --git a/CMakeLists.txt b/CMakeLists.txt
index 19292f9e29b..3d7f112f9d2 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -490,8 +490,31 @@ endif ()
 
 message (STATUS "Building for: ${CMAKE_SYSTEM} ${CMAKE_SYSTEM_PROCESSOR} ${CMAKE_LIBRARY_ARCHITECTURE}")
 
+# Use system OpenSSL for FIPS compliance
+if(ENABLE_OPENSSL_DYNAMIC OR ENABLE_SSL)
+    find_package(OpenSSL REQUIRED)
+    if(OpenSSL_FOUND)
+        message(STATUS "Using system OpenSSL: ${OPENSSL_VERSION}")
+        if(NOT TARGET OpenSSL::Crypto)
+            add_library(OpenSSL::Crypto INTERFACE IMPORTED)
+            set_target_properties(OpenSSL::Crypto PROPERTIES
+                INTERFACE_INCLUDE_DIRECTORIES "${OPENSSL_INCLUDE_DIR}"
+                INTERFACE_LINK_LIBRARIES "${OPENSSL_CRYPTO_LIBRARY}")
+        endif()
+        if(NOT TARGET OpenSSL::SSL)
+            add_library(OpenSSL::SSL INTERFACE IMPORTED)
+            set_target_properties(OpenSSL::SSL PROPERTIES
+                INTERFACE_INCLUDE_DIRECTORIES "${OPENSSL_INCLUDE_DIR}"
+                INTERFACE_LINK_LIBRARIES "${OPENSSL_SSL_LIBRARY}")
+        endif()
+    endif()
+endif()
+
 add_subdirectory (contrib EXCLUDE_FROM_ALL)
 
+# Add OpenSSL include paths to all targets that might need it
+include_directories(SYSTEM BEFORE ${OPENSSL_INCLUDE_DIR})
+
 # Sets dummy launchers to suppress linking. Used for clang-tidy builds.
 enable_dummy_launchers_if_needed()
 
diff --git a/contrib/CMakeLists.txt b/contrib/CMakeLists.txt
index 486e6a513f8..8b6ef81250b 100644
--- a/contrib/CMakeLists.txt
+++ b/contrib/CMakeLists.txt
@@ -41,8 +41,7 @@ endfunction()
 # Use the original launchers for the projects that are needed for source code generation:
 # protoc and llvm-tlbgen.
 disable_dummy_launchers_if_needed()
-# We need to add OpenSSL first so that all our projects use our own version instead of the system one.
-add_contrib (openssl-cmake openssl)  # Needed for delta-lake
+# Using system OpenSSL for FIPS compliance
 add_contrib (abseil-cpp-cmake abseil-cpp)
 add_contrib (zlib-ng-cmake zlib-ng)
 add_contrib (google-protobuf-cmake google-protobuf)
diff --git a/programs/keeper/keeper_main.cpp b/programs/keeper/keeper_main.cpp
index 285eed53d85..2c1ed50a3b0 100644
--- a/programs/keeper/keeper_main.cpp
+++ b/programs/keeper/keeper_main.cpp
@@ -1,5 +1,6 @@
 #include <unistd.h>
 #include <fcntl.h>
+#include <dlfcn.h>
 
 #include <new>
 #include <iostream>
@@ -96,12 +97,18 @@ static bool isClickhouseApp(std::string_view app_suffix, std::vector<char *> & a
 /// We absolutely discourage the ancient technique of loading
 /// 3rd-party uncontrolled dangerous libraries into the process address space,
 /// because it is insane.
+/// Exception: OpenSSL FIPS provider module which is required for FIPS compliance.
 
 #if !defined(USE_MUSL)
 extern "C"
 {
-    void * dlopen(const char *, int)
+    void * dlopen(const char * filename, int flags)
     {
+        if (filename && strstr(filename, "fips.so") && strstr(filename, "/ossl-modules/"))
+        {
+            void * (*real_dlopen)(const char *, int) = reinterpret_cast<void *(*)(const char *, int)>(dlsym(RTLD_NEXT, "dlopen"));
+            return real_dlopen ? real_dlopen(filename, flags) : nullptr;
+        }
         return nullptr;
     }
 
@@ -115,9 +122,10 @@ extern "C"
         return 0;
     }
 
-    const char * dlerror()
+    char * dlerror()
     {
-        return "ClickHouse does not allow dynamic library loading";
+        char * (*real_dlerror)() = reinterpret_cast<char *(*)()>(dlsym(RTLD_NEXT, "dlerror"));
+        return real_dlerror ? real_dlerror() : const_cast<char *>("ClickHouse does not allow dynamic library loading");
     }
 }
 #endif
diff --git a/programs/main.cpp b/programs/main.cpp
index 9a9267014c4..b46cd501fa5 100644
--- a/programs/main.cpp
+++ b/programs/main.cpp
@@ -14,6 +14,7 @@
 #include "config_tools.h"
 
 #include <unistd.h>
+#include <dlfcn.h>
 
 #include <filesystem>
 #include <iostream>
@@ -192,12 +193,18 @@ bool isClickhouseApp(std::string_view app_suffix, std::vector<char *> & argv)
 /// We absolutely discourage the ancient technique of loading
 /// 3rd-party uncontrolled dangerous libraries into the process address space,
 /// because it is insane.
+/// Exception: OpenSSL FIPS provider module which is required for FIPS compliance.
 
 #if !defined(USE_MUSL)
 extern "C"
 {
-    void * dlopen(const char *, int)
+    void * dlopen(const char * filename, int flags)
     {
+        if (filename && strstr(filename, "fips.so") && strstr(filename, "/ossl-modules/"))
+        {
+            void * (*real_dlopen)(const char *, int) = reinterpret_cast<void *(*)(const char *, int)>(dlsym(RTLD_NEXT, "dlopen"));
+            return real_dlopen ? real_dlopen(filename, flags) : nullptr;
+        }
         return nullptr;
     }
 
@@ -211,9 +218,10 @@ extern "C"
         return 0;
     }
 
-    const char * dlerror()
+    char * dlerror()
     {
-        return "ClickHouse does not allow dynamic library loading";
+        char * (*real_dlerror)() = reinterpret_cast<char *(*)()>(dlsym(RTLD_NEXT, "dlerror"));
+        return real_dlerror ? real_dlerror() : const_cast<char *>("ClickHouse does not allow dynamic library loading");
     }
 }
 #endif
diff --git a/src/Common/Crypto/OpenSSLInitializer.cpp b/src/Common/Crypto/OpenSSLInitializer.cpp
index 2d832a25269..fd5e0651161 100644
--- a/src/Common/Crypto/OpenSSLInitializer.cpp
+++ b/src/Common/Crypto/OpenSSLInitializer.cpp
@@ -9,6 +9,10 @@
 #    include <openssl/crypto.h>
 #    include <openssl/provider.h>
 #    include <openssl/ssl.h>
+#    include <openssl/rand.h>
+#    include <openssl/evp.h>
+#    include <openssl/err.h>
+#    include <openssl/conf.h>
 #endif
 
 
@@ -19,7 +23,7 @@ namespace DB
 std::atomic<bool> DB::OpenSSLInitializer::initialize_done{false};
 std::atomic<bool> DB::OpenSSLInitializer::cleanup_done{false};
 OSSL_PROVIDER * DB::OpenSSLInitializer::default_provider = nullptr;
-OSSL_PROVIDER * DB::OpenSSLInitializer::legacy_provider = nullptr;
+OSSL_PROVIDER * DB::OpenSSLInitializer::fips_provider = nullptr;
 #endif
 
 OpenSSLInitializer::OpenSSLInitializer()
@@ -44,30 +48,35 @@ void OpenSSLInitializer::initialize()
         // It may cause issues on shutdown, when some OpenSSL objects are still in use.
         auto openssl_flags = OPENSSL_INIT_NO_ATEXIT;
 
-        /// Loading OpenSSL config only if it is set explicitly.
-        ///
-        /// 'legacy' provider may be disabled in system config,
-        /// but we're compiling it statically in the binary, so it must be loadable in any case.
-        const char * env_openssl_conf = getenv("OPENSSL_CONF"); // NOLINT(concurrency-mt-unsafe)
-        if (env_openssl_conf)
-        {
-            openssl_flags |= OPENSSL_INIT_LOAD_CONFIG;
-        }
-        else
-        {
-            openssl_flags |= OPENSSL_INIT_NO_LOAD_CONFIG;
-        }
+        // Load OpenSSL config for proper DRBG configuration and entropy sources
+        openssl_flags |= OPENSSL_INIT_LOAD_CONFIG;
 
         if (OPENSSL_init_ssl(openssl_flags, nullptr) == 0)
             throw std::runtime_error(fmt::format("Failed to load OpenSSL config. {}", getOpenSSLErrors()));
 
+        OPENSSL_load_builtin_modules();
+        OPENSSL_config(nullptr);
+
         default_provider = OSSL_PROVIDER_load(nullptr, "default");
         if (!default_provider)
             throw std::runtime_error(fmt::format("Failed to load OpenSSL 'default' provider. {}", getOpenSSLErrors()));
 
-        legacy_provider = OSSL_PROVIDER_load(nullptr, "legacy");
-        if (!legacy_provider)
-            throw std::runtime_error(fmt::format("Failed to load OpenSSL 'legacy' provider. {}", getOpenSSLErrors()));
+        if (!(getenv("CLICKHOUSE_BUILD"))) {
+            fips_provider = OSSL_PROVIDER_load(nullptr, "fips");
+            if (!fips_provider)
+                throw std::runtime_error(fmt::format("Failed to load OpenSSL 'fips' provider. {}", getOpenSSLErrors()));
+            EVP_set_default_properties(nullptr, "fips=yes");
+        }
+        
+        if (RAND_status() != 1)
+        {
+            // Use RAND_poll to gather entropy from FIPS-approved sources
+            RAND_poll();
+
+            // Check again after polling
+            if (RAND_status() != 1)
+                throw std::runtime_error(fmt::format("OpenSSL random generator is not properly seeded. Check entropy sources in OpenSSL config. {}", getOpenSSLErrors()));
+        }
     }
 #endif
 }
@@ -85,10 +94,10 @@ void OpenSSLInitializer::cleanup()
     {
         cleanup_done = true;
 
-        if (legacy_provider)
+        if (fips_provider)
         {
-            chassert(OSSL_PROVIDER_unload(legacy_provider));
-            legacy_provider = nullptr;
+            chassert(OSSL_PROVIDER_unload(fips_provider));
+            fips_provider = nullptr;
         }
 
         if (default_provider)
diff --git a/src/Common/Crypto/OpenSSLInitializer.h b/src/Common/Crypto/OpenSSLInitializer.h
index 4c1c4f123a6..d9a0ea42b6a 100644
--- a/src/Common/Crypto/OpenSSLInitializer.h
+++ b/src/Common/Crypto/OpenSSLInitializer.h
@@ -32,7 +32,7 @@ private:
     static std::atomic<bool> initialize_done;
     static std::atomic<bool> cleanup_done;
 
-    static OSSL_PROVIDER * legacy_provider;
+    static OSSL_PROVIDER * fips_provider;
     static OSSL_PROVIDER * default_provider;
 #endif
 };
