package:
  name: amazon-ec2-net-utils
  version: "2.5.3"
  epoch: 0
  description: set of utilities for managing elastic network interfaces on Amazon EC2
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - udev
      - systemd
      - curl
      - iproute2

environment:
  contents:
    packages:
      - build-base
      - busybox
      - coreutils

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/amazonlinux/amazon-ec2-net-utils
      tag: v${{package.version}}
      expected-commit: 601bc3513fa7b8a6ab46d9496b233b079e55f2e9

  - uses: autoconf/make-install
    with:
      opts: PREFIX=/usr

test:
  pipeline:
    - runs: |
        # There are no help nor version options
        setup-policy-routes two args

update:
  enabled: true
  github:
    identifier: amazonlinux/amazon-ec2-net-utils
    strip-prefix: v
    tag-filter: v
