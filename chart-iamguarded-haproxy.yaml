package:
  name: chart-iamguarded-haproxy
  version: "2.2.21"
  epoch: 0
  description: Haproxy iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: haproxy/${{package.version}}
      expected-commit: 6aaad85480fdc3a9f10f30891e9de996a2760eb7

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/haproxy

  - uses: charts/package
    with:
      chart-path: iamguarded/haproxy

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: haproxy/
    use-tag: true
    tag-filter-prefix: haproxy/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /haproxy
