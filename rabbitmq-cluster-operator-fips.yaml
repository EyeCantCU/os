package:
  name: rabbitmq-cluster-operator-fips
  version: 2.9.0
  epoch: 0
  description: Open source RabbitMQ cluster operator. Kubernetes operator to deploy and manage RabbitMQ clusters.
  copyright:
    - license: MPL-2.0

environment:
  environment:
    GO111MODULE: on

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/rabbitmq/cluster-operator
      tag: v${{package.version}}
      expected-commit: dc28a8b680d8c1fbb1bbdf45abef706b4394b5cb

  - uses: go/build
    with:
      packages: .
      output: manager
      tags: timetzdata
      go-package: go-fips

test:
  pipeline:
    - uses: test/go-fips-check

update:
  enabled: true
  github:
    identifier: rabbitmq/cluster-operator
    strip-prefix: v
