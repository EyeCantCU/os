From 36258d22550110ab2226971702c15820a83abea0 Mon Sep 17 00:00:00 2001
From: Dimitri John Ledkov <dimitri.ledkov@surgut.co.uk>
Date: Wed, 19 Nov 2025 07:31:27 -0500
Subject: [PATCH] cmd: add support for portable libcrypto

---
 cmd/aws-vpc-cni-init/main.go | 26 ++++++++++++++++++++++++++
 cmd/aws-vpc-cni/main.go      | 26 ++++++++++++++++++++++++++
 2 files changed, 52 insertions(+)

diff --git a/cmd/aws-vpc-cni-init/main.go b/cmd/aws-vpc-cni-init/main.go
index 245ae382..b91aeb19 100644
--- a/cmd/aws-vpc-cni-init/main.go
+++ b/cmd/aws-vpc-cni-init/main.go
@@ -15,7 +15,9 @@
 package main
 
 import (
+	"fmt"
 	"os"
+	"path/filepath"
 
 	"github.com/aws/amazon-vpc-cni-k8s/pkg/netlinkwrapper"
 	"github.com/aws/amazon-vpc-cni-k8s/pkg/procsyswrapper"
@@ -160,6 +162,30 @@ func _main() int {
 	}
 	log.Infof("Copied all CNI plugin binaries to %s", hostCNIBinPath)
 
+	// Setup portable libcrypto
+	cgrDir := filepath.Join(hostCNIBinPath, ".cgr")
+	err = os.MkdirAll(cgrDir, 0o755)
+	if err != nil {
+		log.WithError(err).Errorf("Failed to create .cgr dir")
+		return 1
+	}
+	cgr_files := []string{
+		"/etc/ssl/fipsmodule.cnf",
+		"/etc/ssl/openssl.cnf",
+		"/usr/lib/libcrypto3/libcrypto.so.3",
+		"/usr/lib/ossl-modules/fips.so",
+	}
+	for _, cgr_file := range cgr_files {
+		target := filepath.Join(cgrDir, filepath.Base(cgr_file))
+		err := cp.CopyFile(cgr_file, target)
+		if err != nil {
+			log.WithError(err).Errorf("Failed to copy %s", target)
+			return 1
+		}
+		fmt.Printf("Installed %s\n", target)
+
+	}
+
 	var primaryIF string
 	primaryIF, err = getNodePrimaryIF()
 	if err != nil {
diff --git a/cmd/aws-vpc-cni/main.go b/cmd/aws-vpc-cni/main.go
index d0627c6e..bb262a1b 100644
--- a/cmd/aws-vpc-cni/main.go
+++ b/cmd/aws-vpc-cni/main.go
@@ -37,9 +37,11 @@ package main
 
 import (
 	"encoding/json"
+	"fmt"
 	"net"
 	"os"
 	"os/exec"
+	"path/filepath"
 	"strconv"
 	"strings"
 
@@ -429,6 +431,30 @@ func _main() int {
 		return 1
 	}
 
+	// Setup portable libcrypto
+	cgrDir := filepath.Join(hostCNIBinPath, ".cgr")
+	err = os.MkdirAll(cgrDir, 0o755)
+	if err != nil {
+		log.WithError(err).Errorf("Failed to create .cgr dir")
+		return 1
+	}
+	cgr_files := []string{
+		"/etc/ssl/fipsmodule.cnf",
+		"/etc/ssl/openssl.cnf",
+		"/usr/lib/libcrypto3/libcrypto.so.3",
+		"/usr/lib/ossl-modules/fips.so",
+	}
+	for _, cgr_file := range cgr_files {
+		target := filepath.Join(cgrDir, filepath.Base(cgr_file))
+		err := cp.CopyFile(cgr_file, target)
+		if err != nil {
+			log.WithError(err).Errorf("Failed to copy %s", target)
+			return 1
+		}
+		fmt.Printf("Installed %s\n", target)
+
+	}
+
 	log.Infof("Starting IPAM daemon... ")
 
 	cmd := "./aws-k8s-agent"
-- 
2.51.0

