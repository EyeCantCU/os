![Chainguard logo](https://avatars.githubusercontent.com/u/87436699?s=200&v=4)

# Chainguard Enterprise Wolfi Packages

This package repository will host all paid packages support by Chainguard but not made available
on [Wolfi](https://wolfi.dev/os).

New package requests are usually submitted as part of a customer request. Please visit
our '[Customer Engagements'](https://wiki.inky.wtf/docs/teams/engineering/images/customer-engagements/) page for more
details.

This repo is owned by the [Images Team](https://wiki.inky.wtf/docs/teams/engineering/images/chainguard-images/).

For help with patching CVEs and recording the associated advisory data, see the ["How To Patch CVEs"](https://github.com/wolfi-dev/os/blob/main/HOW_TO_PATCH_CVES.md) documentation in Wolfi.

# Contents

This repository is based on the open source [Wolfi OS repository](https://github.com/wolfi-dev/os) with a few differences.

### Add a new package

Tp add a new melange package, create a melange yaml file and make sure you add an entry to the Makefile that matches the `package.name` and `package.version`.

The main difference to Wolfi OS is that we need to specifiy the Wolfi OS repository and keyring so we can fetch dependencies when building.

i.e.

```yaml
environment:
  contents:
    repositories:
      - https://packages.wolfi.dev/os
    keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
```

Because of this difference, when validating lint locally you need to override two default rules...

```sh
wolfictl lint --skip-rule forbidden-repository-used --skip-rule forbidden-keyring-used
```

# Renovate tips

Renovate can be run as an installed github app, github action, self-hosted github app, or locally against the github hosted repository.

Using `--platform=local` is dryrun only and check dependencies and versions, but does not create any branches, does not do any changes to any files and does not open PRs.

## Get CLI tool

Get renovate CLI tool

```
$ make dev-container
$ apk add renovate
```

## Check tracked deps locally

To quickly show a summary of how many files are detected with managed updates run:

```
$ renovate --platform=local
...
 INFO: Dependency extraction complete (repository=local)
       "stats": {
         "managers": {"regex": {"fileCount": 1, "depCount": 1}},
         "total": {"fileCount": 1, "depCount": 1}
       }
...
```
As you can see only one manager is activate (regex), and it has one file to update, with a single dep.


## Get detailed information about deps locally

To see full details about each individual discovered dependency use (example below is maven dep in `bouncycastle-jmail-fips.yaml`):

```
$ LOG_LEVEL=debug renovate --platform=local
...
DEBUG: packageFiles with updates (repository=local)
       "config": {
         "regex": [
           {
             "deps": [
               {
                 "depName": "org.bouncycastle:bcjmail-fips",
                 "currentValue": "2.1.6",
                 "datasource": "maven",
                 "versioning": "maven",
                 "replaceString": "  # renovate: datasource=maven depName=org.bouncycastle:bcjmail-fips versioning=maven\n  version: \"2.1.6\"",
                 "updates": [],
                 "packageName": "org.bouncycastle:bcjmail-fips",
                 "warnings": [],
                 "registryUrl": "https://repo.maven.apache.org/maven2",
                 "homepage": "https://www.bouncycastle.org/download/bouncy-castle-java-fips",
                 "packageScope": "org.bouncycastle",
                 "currentVersion": "2.1.6",
                 "fixedVersion": "2.1.6"
               }
             ],
             "matchStrings": [
               "  # renovate: datasource=(?<datasource>[a-z-]+) depName=(?<depName>[a-z.:-]+)( versioning=(?<versioning>[a-z]+))?\\n  version: \"(?<currentValue>[\\d+.]*)\""
             ],
             "versioningTemplate": "{{#if versioning}}{{{versioning}}}{{else}}semver-coerced{{/if}}",
             "packageFile": "bouncycastle-jmail-fips.yaml"
           }
         ]
       }
DEBUG: detectSemanticCommits() (repository=local)
...
```

## Executed renovate against remote repository

Until renovate app is installed and running against the organization repositories, preffer to run renovate against own fork, review PRs it created, and then propose them to our main repository.

Generate github token:

```
$ gh auth token
gho_...
```

Also ensure your fork of enterprise-packages is up to date:

```
gh repo sync yourfork/enterprise-packages
```

Also export GIT variable to ensure commits generated by renovate, are attributed to it:

```
export GIT_AUTHOR_NAME="Renovate Bot"
export GIT_AUTHOR_EMAIL="renovate@example.com"
export GIT_COMMITTER_NAME="Renovate Bot"
export GIT_COMMITTER_EMAIL="renovate@example.com"
export RENOVATE_TOKEN=gho_...
renovate --platform=github yourfork/enterprise-packages
```

Once renovate finishes creating branches, they will need fixups for now such as:
- updating checksums
- resetting epoch to zero
- doing test builds

Thus for now really, it is just an equivalent of getting updates notifications.
