package:
  name: tekton-pipelines-fips
  version: 0.58.0
  epoch: 3
  description: A cloud-native Pipeline resource.
  copyright:
    - license: Apache-2.0

environment:
  environment:
    CGO_ENABLED: "1"
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go-fips

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/tektoncd/pipeline
      tag: v${{package.version}}
      expected-commit: f0a1d64aa88929f8915f208db65a8f731b5c92e2
      destination: tekton

  - uses: go/bump
    with:
      deps: github.com/docker/docker@v24.0.9 github.com/cloudflare/circl@v1.3.7 github.com/go-jose/go-jose/v3@v3.0.3 gopkg.in/go-jose/go-jose.v2@v2.6.3 github.com/cloudevents/sdk-go/v2@v2.15.2 google.golang.org/protobuf@v1.33.0 github.com/golang/protobuf@v1.5.4
      modroot: tekton

  - uses: go/build
    with:
      packages: ./cmd/controller
      output: tekton-pipelines-controller
      modroot: tekton
      vendor: true
      go-package: go-fips

data:
  - name: cmds
    items:
      # this must be statically linked to be able to work with Tekton deployment
      # entrypoint: entrypoint
      events: events
      nop: nop
      resolvers: resolvers
      sidecarlogresults: sidecarlogresults
      webhook: webhook
      workingdirinit: workingdirinit

subpackages:
  - range: cmds
    name: tekton-pipelines-${{range.key}}-fips
    description: tekton pipelines ${{range.key}}
    pipeline:
      - uses: go/build
        with:
          packages: ./cmd/${{range.key}}
          output: tekton-pipelines-${{range.key}}
          modroot: tekton
          subpackage: "true"
          go-package: go-fips

update:
  enabled: true
  github:
    identifier: tektoncd/pipeline
    strip-prefix: v
