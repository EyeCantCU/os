In Boost 1.88, `boost::process` was switched from defaulting to
`boost::process::v1` to `boost::process::v2`.  The `::v1` header file which
included various common headers was also removed at this time, meaning each
header file has to be included separately.
--
diff --git a/common/ChildProcess/ChildProcess.cpp b/common/ChildProcess/ChildProcess.cpp
index 54f85ae..20a8780 100644
--- a/common/ChildProcess/ChildProcess.cpp
+++ b/common/ChildProcess/ChildProcess.cpp
@@ -32,8 +32,14 @@
 #include <Defer.hpp>
 
 #include <atomic>
-#include <boost/process.hpp>
-#include <boost/process/extend.hpp>
+#include <boost/process/v1/args.hpp>
+#include <boost/process/v1/async_pipe.hpp>
+#include <boost/process/v1/child.hpp>
+#include <boost/process/v1/env.hpp>
+#include <boost/process/v1/environment.hpp>
+#include <boost/process/v1/extend.hpp>
+#include <boost/process/v1/io.hpp>
+#include <boost/process/v1/system.hpp>
 #include <latch>
 #include <thread>
 
@@ -156,7 +162,7 @@ struct ChildProcess::Impl
     }
 
     static auto StdLinesProcess(ChildProcess::Impl &self,
-                                boost::process::async_pipe &sourcePipe,
+                                boost::process::v1::async_pipe &sourcePipe,
                                 StdLines &target) -> boost::asio::awaitable<int>
     {
         auto cancellationState = co_await boost::asio::this_coro::cancellation_state;
@@ -202,7 +208,7 @@ struct ChildProcess::Impl
 
     void Run()
     {
-        namespace bp = boost::process;
+        namespace bp = boost::process::v1;
         std::latch launchProcessLatch(1);
         bool failedToLaunch = false;
         auto launchProcess  = [this, &launchProcessLatch, &failedToLaunch]() {
@@ -490,15 +496,15 @@ struct ChildProcess::Impl
 
     std::unique_ptr<ChannelDesc> fdChannelOpt;
 
-    boost::process::async_pipe stdOutPipe;
-    boost::process::async_pipe stdErrPipe;
+    boost::process::v1::async_pipe stdOutPipe;
+    boost::process::v1::async_pipe stdErrPipe;
 
-    boost::process::child process;
+    boost::process::v1::child process;
 
     boost::filesystem::path executable;
     std::vector<std::string> args;
     std::optional<std::string> userName;
-    boost::process::environment environment;
+    boost::process::v1::environment environment;
 
     StdLines stdOutLines;
     StdLines stdErrLines;
@@ -588,7 +594,7 @@ void ChildProcess::Create(IoContext &ioContext,
     }
     if (env.has_value())
     {
-        m_impl->environment = static_cast<boost::process::environment>(boost::this_process::environment());
+        m_impl->environment = static_cast<boost::process::v1::environment>(boost::this_process::environment());
         for (auto const &pair : env.value())
         {
             m_impl->environment.emplace(pair.first, pair.second);
diff --git a/common/RemoteConn/SSHTunnelManager.hpp b/common/RemoteConn/SSHTunnelManager.hpp
index 253fbe7..da9fc28 100644
--- a/common/RemoteConn/SSHTunnelManager.hpp
+++ b/common/RemoteConn/SSHTunnelManager.hpp
@@ -21,7 +21,7 @@
 #include <dcgm_structs_internal.h>
 
 #include <boost/filesystem/exception.hpp>
-#include <boost/process.hpp>
+#include <boost/process/v1.hpp>
 #include <fmt/format.h>
 
 #include <expected>
@@ -279,7 +279,7 @@ namespace detail
         SSHTunnelManagerBase()
         {
             std::vector<boost::filesystem::path> const binarySearchDirs = { "/usr/bin", "/usr/sbin" };
-            m_binaryPath = boost::process::search_path("ssh", std::move(binarySearchDirs));
+            m_binaryPath = boost::process::v1::search_path("ssh", std::move(binarySearchDirs));
             m_currentUid = getuid();
         }
 
