# For production use best to leave this unset
# and ensure on the host sysctl vm.max_map_count=262144 is set
# Read https://www.elastic.co/docs/deploy-manage/deploy/cloud-on-k8s/virtual-memory
# Which offers init containers, and daemonsets to set that
node.store.allow_mmap: false
cluster.name: "docker-cluster"
network.host: 0.0.0.0
