package:
  name: chart-iamguarded-fluentd
  version: "7.2.5"
  epoch: 2
  description: Fluentd iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: fluentd/${{package.version}}
      expected-commit: ba87a2d6ab278b768edb986e9745f785cd8a6a5e

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: patch
    with:
      patches: helpers.patch

  - uses: patch
    with:
      patches: values.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/fluentd

  - uses: charts/package
    with:
      chart-path: iamguarded/fluentd

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: fluentd/
    use-tag: true
    tag-filter-prefix: fluentd/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /fluentd
