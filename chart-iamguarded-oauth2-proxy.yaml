package:
  name: chart-iamguarded-oauth2-proxy
  version: "8.0.2"
  epoch: 0
  description: oauth2-proxy iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common
      - chart-iamguarded-redis

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: oauth2-proxy/${{package.version}}
      expected-commit: 03026b65c3e62cc53c725cd48e02d6fb554cc4b7

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/oauth2-proxy

  - uses: charts/package
    with:
      chart-path: iamguarded/oauth2-proxy

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: oauth2-proxy/
    use-tag: true
    tag-filter-prefix: oauth2-proxy/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /oauth2-proxy
