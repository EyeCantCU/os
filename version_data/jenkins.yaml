name: jenkins
additional_prompt: Jenkins maintains a main version stream '2', which has all the versions upto latest. Then it creates LTS version streams, every version with a patch version (e.g. 2.258.1) is a LTS version stream. Split this stream into the main stream and LTS streams
