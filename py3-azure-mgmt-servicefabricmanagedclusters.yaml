package:
  name: py3-azure-mgmt-servicefabricmanagedclusters
  version: 2.0.0
  epoch: 0
  description: This is the Microsoft Azure Service Fabric Managed Clusters Management Client Library.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-azure-common
      - py3-azure-mgmt-core
      - py3-msrest
      - python-3

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base

pipeline:
  - uses: git-checkout
    with:
      expected-commit: 166d044fd1249f4b941b8e77cd36ee2f3c32a18b
      repository: https://github.com/Azure/azure-sdk-for-python
      tag: azure-mgmt-servicefabricmanagedclusters_${{package.version}}
      destination: ./azure-sdk-for-python

  - runs: |
      mv ./azure-sdk-for-python/sdk/servicefabricmanagedclusters/azure-mgmt-servicefabricmanagedclusters/* .

  - name: Python Build
    uses: python/build-wheel

  - uses: strip

update:
  # The azure repo has some tags that have a bN suffix, which
  # is not a valid version, so ignore those.
  ignore-regex-patterns:
    - b
  enabled: true
  github:
    identifier: Azure/azure-sdk-for-python
    tag-filter-contains: azure-mgmt-servicefabricmanagedclusters_
    strip-prefix: azure-mgmt-servicefabricmanagedclusters_
    use-tag: true

test:
  environment:
    contents:
      packages:
        - wolfi-base
  pipeline:
    - runs: python -c 'import azure.mgmt.servicefabricmanagedclusters'
