diff --git a/src/frontends/onnx/onnx_common/CMakeLists.txt b/src/frontends/onnx/onnx_common/CMakeLists.txt
index 1234567..abcdefg 100644
--- a/src/frontends/onnx/onnx_common/CMakeLists.txt
+++ b/src/frontends/onnx/onnx_common/CMakeLists.txt
@@ -16,6 +16,10 @@ target_link_libraries(${TARGET_NAME} PRIVATE openvino::runtime openvino::util)
 
 ov_link_system_libraries(${TARGET_NAME} PUBLIC onnx_proto onnx)
 
+# Link against abseil vlog library required by protobuf
+# This fixes undefined symbol: _ZN4absl12lts_2025012712log_internal21UpdateGlobalVLogLevelEi which causes the provider to fail to load since 2025.03.0
+target_link_libraries(${TARGET_NAME} PUBLIC -labsl_vlog_config_internal)
+
 ov_add_clang_format_target(${TARGET_NAME}_clang FOR_TARGETS ${TARGET_NAME})
 
 ov_install_static_lib(${TARGET_NAME} ${OV_CPACK_COMP_CORE})
diff --git a/src/frontends/paddle/src/CMakeLists.txt b/src/frontends/paddle/src/CMakeLists.txt
index 1234567..abcdefg 100644
--- a/src/frontends/paddle/src/CMakeLists.txt
+++ b/src/frontends/paddle/src/CMakeLists.txt
@@ -10,3 +10,7 @@ ov_add_frontend(NAME paddle
                 LINK_LIBRARIES openvino::util openvino::core::dev)
 
 ov_build_target_faster(openvino_paddle_frontend PCH)
+
+# Link against abseil vlog library required by protobuf
+# This fixes undefined symbol: _ZN4absl12lts_2025012712log_internal21UpdateGlobalVLogLevelEi which causes the provider to fail to load since 2025.03.0
+target_link_libraries(${TARGET_NAME} PUBLIC -labsl_vlog_config_internal)
