package:
  name: chart-iamguarded-external-dns
  version: "8.8.7"
  epoch: 7
  description: external-dns iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: external-dns/8.8.2
      expected-commit: 79dbc6591763e8a23861d7b4318dda1268bf2e01

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/external-dns

  - uses: charts/package
    with:
      chart-path: iamguarded/external-dns

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: external-dns/
    use-tag: true
    tag-filter-prefix: external-dns/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /external-dns
