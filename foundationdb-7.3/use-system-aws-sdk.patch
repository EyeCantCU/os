--- a/fdbclient/CMakeLists.txt
+++ b/fdbclient/CMakeLists.txt
@@ -66,7 +66,18 @@ endif()
 if(WITH_AWS_BACKUP)
   add_compile_definitions(WITH_AWS_BACKUP)
-  include(awssdk)
+
+  # Use system AWS SDK instead of building from source
+  find_package(AWSSDK REQUIRED COMPONENTS s3 transfer core)
+
+  add_library(awssdk_target INTERFACE)
+  target_link_libraries(awssdk_target INTERFACE
+    aws-cpp-sdk-s3
+    aws-cpp-sdk-transfer
+    aws-cpp-sdk-core
+    aws-crt-cpp
+    aws-c-common
+  )
 endif()

 add_flow_target(STATIC_LIBRARY NAME fdbclient SRCS ${FDBCLIENT_SRCS} ADDL_SRCS ${options_srcs})

