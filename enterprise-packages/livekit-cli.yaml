package:
  name: livekit-cli
  version: "2.13.2"
  epoch: 5 # GHSA-9h8m-3fm2-qjrq
  description: Command line interface to LiveKit
  copyright:
    - license: Apache-2.0

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/livekit/livekit-cli
      tag: v${{package.version}}
      expected-commit: 5d2c9802ae6be67067a7e55dba37f5bf36017838

  - uses: go/bump
    with:
      deps: |-
        github.com/go-git/go-git/v5@v5.16.5
        github.com/pion/dtls/v3@v3.1.1
        github.com/cloudflare/circl@v1.6.3
        go.opentelemetry.io/otel/sdk@v1.40.0

  - uses: go/build
    with:
      packages: ./cmd/lk
      output: lk

update:
  enabled: true
  github:
    identifier: livekit/livekit-cli
    strip-prefix: v
    use-tag: true

test:
  pipeline:
    - name: "Binary checks"
      runs: |
        lk --version | grep "${{package.version}}"
        lk --help
    - name: "cli commands check"
      runs: |
        lk token --help
        lk room --help
        lk project --help
    - name: "Token generation"
      runs: |
        # token creation
        lk token create \
          --api-key API123456789 \
          --api-secret secret123456789 \
          --join \
          --room test-room \
          --identity test-user \
          --valid-for 10m
