package:
  name: chart-iamguarded-fluent-bit
  version: "3.1.13"
  epoch: 0
  description: Fluent Bit Iamguarded Chart
  copyright:
    - license: Apache-2.0
  dependencies:
    provides:
      - fluent-bit=${{package.full-version}}

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: fluent-bit/${{package.version}}
      expected-commit: ac8a2f14b8e13e82c62461c44087c14fd1240c61

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/fluent-bit

  - uses: charts/package
    with:
      chart-path: iamguarded/fluent-bit

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: fluent-bit/
    use-tag: true
    tag-filter-prefix: fluent-bit/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /fluent-bit
