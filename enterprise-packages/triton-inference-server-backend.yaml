#nolint:valid-pipeline-git-checkout-tag
package:
  name: triton-inference-server-backend
  version: "26.01"
  epoch: 0 # go/enterprise-rsc/triton-inference-server-backend
  description:
  copyright:
    - license: BSD-3-Clause
      license-path: backend.git/LICENSE
  resources:
    cpu: "2"

environment:
  contents:
    packages:
      - busybox

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/triton-inference-server/backend
      branch: r${{package.version}}
      expected-commit: a422e1e544f398eb267d7e40dcb41c31d055a758
      destination: backend.git

  # git-checkout branch will attempt to reset to the expected-commit
  # if the branch doesn't match. Here we want to fail the build
  # so we can validate and switch to the new commit.
  - name: check if git-checkout had to reset to expected-commit
    working-directory: backend.git
    runs: |
      [ "$(git rev-parse origin/r${{package.version}})" = "$(git rev-parse HEAD)" ]

  - runs: |
      mkdir -p "${{targets.contextdir}}/usr/src/triton-inference-server"
      cp -a backend.git "${{targets.contextdir}}/usr/src/triton-inference-server"

update:
  enabled: false
  exclude-reason: No support for untagged release branches yet

test:
  pipeline:
    - uses: test/tw/contains-files
      with:
        dir: "/usr/src/triton-inference-server/"
        name: "*"
