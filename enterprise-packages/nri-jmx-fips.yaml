package:
  name: nri-jmx-fips
  version: "3.11.4"
  epoch: 0 # CVE-2025-61731
  description: New Relic Infrastructure JMX Integration (FIPS)
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - nrjmx
      - openjdk-11-jre
      - openjdk-bcfips-policy-140-3-j17

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/newrelic/nri-jmx
      expected-commit: a66e4362d593ab3c7fbde6ba1360ec74d9ef9aef
      tag: v${{package.version}}

  - uses: go/build
    with:
      modroot: .
      packages: ./src/
      output: nri-jmx
      go-package: go-fips-md5
      ldflags: |
        -X main.integrationVersion=${{package.version}}
        -X main.gitCommit=$(git rev-parse HEAD)
        -X main.buildDate=$(date -u -d "@$SOURCE_DATE_EPOCH" "+%Y-%m-%dT%H:%M:%SZ")

subpackages:
  - name: ${{package.name}}-compat
    description: "Compatibility package to place binaries in the location expected by newrelic-infrastructure-bundle"
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/var/db/newrelic-infra/newrelic-integrations/bin
          ln -sf /usr/bin/nri-jmx ${{targets.subpkgdir}}/var/db/newrelic-infra/newrelic-integrations/bin/nri-jmx
          install -Dm644 legacy/jmx-definition.yml ${{targets.subpkgdir}}/var/db/newrelic-infra/newrelic-integrations/jmx-definition.yml
    test:
      environment:
        contents:
          packages:
            - ${{package.name}}
      pipeline:
        - uses: test/tw/symlink-check
          with:
            allow-absolute: "true"

update:
  enabled: true
  github:
    identifier: newrelic/nri-jmx
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - openjdk-11-default-jdk
    environment:
      JAVA_HOME: /usr/lib/jvm/java-11-openjdk
  pipeline:
    - uses: test/go-fips-check
    - uses: test/tw/help-check
      with:
        bins: nri-jmx
    - uses: test/tw/ver-check
      with:
        bins: nri-jmx
        version-flag: "-show_version"
    # This test assumes that no JMX server is running on localhost.
    # As a result, the connection attempt will fail, although we can confirm that the binary behaves as expected.
    - name: "Test binary execution"
      uses: test/daemon-check-output
      with:
        setup: |
          mkdir -p /tmp
          cat > /tmp/jmx-test.yml <<EOF
          collect:
            - domain: java.lang
              event_type: JVMSample
              beans:
                - query: type=Memory
                  attributes:
                    - HeapMemoryUsage.Used
          EOF
        start: |
          sh -c "COLLECTION_FILES=/tmp/jmx-test.yml nri-jmx"
        expected_output: |
          Connection refused to host
        error_strings: |
          FAIL
          Traceback.*most.recent.call
