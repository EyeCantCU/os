package:
  name: chart-iamguarded-memcached
  version: "7.9.7"
  epoch: 3 # go/enterprise-rsc/chart-iamguarded-memcached
  description: Memcached iamguarded chart
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "1"
    memory: 5Gi

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: memcached/${{package.version}}
      expected-commit: 94b3f6791929c3d3c10f2975b1510671f075e419

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/memcached

  - uses: charts/package
    with:
      chart-path: iamguarded/memcached

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: memcached/
    use-tag: true
    tag-filter-prefix: memcached/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /memcached
