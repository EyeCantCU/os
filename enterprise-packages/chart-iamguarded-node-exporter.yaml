package:
  name: chart-iamguarded-node-exporter
  version: "4.5.16"
  epoch: 0
  description: Node exporter iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: node-exporter/${{package.version}}
      expected-commit: 9c01956670c203b33b5e5d91a1b2927520669d7d

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/node-exporter

  - uses: charts/package
    with:
      chart-path: iamguarded/node-exporter

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: node-exporter/
    use-tag: true
    tag-filter-prefix: node-exporter/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /node-exporter
