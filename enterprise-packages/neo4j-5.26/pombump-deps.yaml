# Disabling this patch since it is incompatible with Neo4j v5.26.21 and causes a NoClassDefFoundError during Bolt transport initialization.
# This may be re-enabled if a future release of Neo4j resolves the incompatibility, or if
# bumping a compatible set of Netty artifacts (rather than only netty-codec-http) proves stable.
# patches:
#   - groupId: io.netty
#     artifactId: netty-codec-http
#     version: 4.1.130.Final
