package:
  name: loki-fips-3.6
  version: "3.6.4"
  epoch: 0
  description: Like Prometheus, but for logs.
  copyright:
    - license: AGPL-3.0-or-later
  dependencies:
    provides:
      - loki-fips=${{package.full-version}}

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go-fips-md5

var-transforms:
  - from: ${{package.version}}
    match: ^(\d+)\.\d+\.\d+$
    replace: "$1"
    to: major-version

pipeline:
  - uses: git-checkout
    with:
      expected-commit: d80526692af3bf28c35622a59a3231933b193bc2
      repository: https://github.com/grafana/loki
      tag: v${{package.version}}

  - uses: patch
    with:
      patches: Makefile.patch

  - uses: autoconf/make
    with:
      opts: |
        UNAME_S=$(uname -s)

  - runs: |
      mkdir -p ${{targets.contextdir}}/usr/bin
      mv cmd/loki/loki ${{targets.contextdir}}/usr/bin/loki

      # Add the default config
      mkdir -p ${{targets.contextdir}}/etc/loki
      mv local-config.yaml ${{targets.contextdir}}/etc/loki

  - uses: strip

subpackages:
  - name: ${{package.name}}-logcli
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/bin
          mv cmd/logcli/logcli ${{targets.contextdir}}/usr/bin/logcli
    dependencies:
      provides:
        - logcli-fips=${{package.full-version}}
    test:
      environment:
        contents:
          packages:
            - ${{package.name}}
            - curl
        environment:
          LOKI_PORT: "3100"
      pipeline:
        - uses: test/go-fips-check
        - name: Verify logcli can query logs from Loki
          uses: test/daemon-check-output
          with:
            setup: |-
              mkdir -p /tmp/loki/chunks /tmp/loki/rules
              tee /tmp/loki-config.yaml <<EOF
              auth_enabled: false
              server:
                http_listen_port: ${LOKI_PORT}
              common:
                path_prefix: /tmp/loki
                storage:
                  filesystem:
                    chunks_directory: /tmp/loki/chunks
                    rules_directory: /tmp/loki/rules
                replication_factor: 1
                ring:
                  kvstore:
                    store: inmemory
              schema_config:
                configs:
                  - from: 2020-10-24
                    store: tsdb
                    object_store: filesystem
                    schema: v13
                    index:
                      prefix: index_
                      period: 24h
              limits_config:
                allow_structured_metadata: false
              EOF
            start: loki -config.file=/tmp/loki-config.yaml -config.expand-env=true
            expected_output: "server listening on addresses"
            timeout: 60
            post: |-
              set -euo pipefail
              # Wait for Loki to be fully ready
              for i in $(seq 1 30); do
                if curl -sf "http://localhost:${LOKI_PORT}/ready"; then
                  break
                fi
                sleep 2
              done
              # Verify Loki is ready
              curl -f "http://localhost:${LOKI_PORT}/ready"
              # Push test logs to Loki
              curl -f -X POST "http://localhost:${LOKI_PORT}/loki/api/v1/push" \
                -H "Content-Type: application/json" \
                -d '{
                  "streams": [
                    {
                      "stream": {"job": "test", "level": "info"},
                      "values": [["'$(date +%s)000000000'", "test log message from logcli test"]]
                    }
                  ]
                }'
              # Verify logcli can connect
              logcli --addr="http://localhost:${LOKI_PORT}" --version

  - name: ${{package.name}}-promtail
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/bin
          mv clients/cmd/promtail/promtail ${{targets.contextdir}}/usr/bin/promtail

          mkdir -p ${{targets.contextdir}}/etc/promtail
          mv promtail-local-config.yaml ${{targets.contextdir}}/etc/promtail/config.yml
    dependencies:
      provides:
        - promtail-fips=${{package.full-version}}
    test:
      environment:
        contents:
          packages:
            - ${{package.name}}
            - curl
        environment:
          LOKI_PORT: "3100"
          PROMTAIL_PORT: "9080"
      pipeline:
        - uses: test/go-fips-check
        - name: Verify promtail can send logs to Loki
          uses: test/daemon-check-output
          with:
            setup: |-
              # Start Loki first
              mkdir -p /tmp/loki/chunks /tmp/loki/rules /tmp/promtail
              tee /tmp/loki-config.yaml <<EOF
              auth_enabled: false
              server:
                http_listen_port: ${LOKI_PORT}
              common:
                path_prefix: /tmp/loki
                storage:
                  filesystem:
                    chunks_directory: /tmp/loki/chunks
                    rules_directory: /tmp/loki/rules
                replication_factor: 1
                ring:
                  kvstore:
                    store: inmemory
              schema_config:
                configs:
                  - from: 2020-10-24
                    store: tsdb
                    object_store: filesystem
                    schema: v13
                    index:
                      prefix: index_
                      period: 24h
              limits_config:
                allow_structured_metadata: false
              EOF
              loki -config.file=/tmp/loki-config.yaml -config.expand-env=true > /tmp/loki.log 2>&1 &
              LOKI_PID=$!
              # Wait for Loki to be ready
              wait-for-it localhost:${LOKI_PORT} -t 60
              # Create test log file for promtail to read
              echo "test log line from promtail" > /tmp/promtail/test.log
              # Create promtail config
              tee /tmp/promtail-config.yaml <<EOF
              server:
                http_listen_port: ${PROMTAIL_PORT}
                grpc_listen_port: 0
              positions:
                filename: /tmp/promtail/positions.yaml
              clients:
                - url: http://localhost:${LOKI_PORT}/loki/api/v1/push
              scrape_configs:
                - job_name: test
                  static_configs:
                    - targets:
                        - localhost
                      labels:
                        job: promtail_test
                        __path__: /tmp/promtail/*.log
              EOF
            start: promtail -config.file=/tmp/promtail-config.yaml -config.expand-env=true
            expected_output: "Reloading configuration file"
            timeout: 60
            post: |-
              set -euo pipefail
              # Wait for promtail to start and scrape logs
              sleep 10
              # Verify promtail is running and responding
              curl -f "http://localhost:${PROMTAIL_PORT}/ready"

  - name: ${{package.name}}-loki-canary
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/bin
          mv cmd/loki-canary/loki-canary ${{targets.contextdir}}/usr/bin/loki-canary
    dependencies:
      provides:
        - loki-canary-fips=${{package.full-version}}
    test:
      environment:
        contents:
          packages:
            - ${{package.name}}
            - curl
        environment:
          LOKI_PORT: "3100"
          CANARY_PORT: "3500"
      pipeline:
        - uses: test/go-fips-check
        - name: Verify loki-canary can send and query logs
          uses: test/daemon-check-output
          with:
            setup: |-
              # Start Loki first
              mkdir -p /tmp/loki/chunks /tmp/loki/rules
              tee /tmp/loki-config.yaml <<EOF
              auth_enabled: false
              server:
                http_listen_port: ${LOKI_PORT}
              common:
                path_prefix: /tmp/loki
                storage:
                  filesystem:
                    chunks_directory: /tmp/loki/chunks
                    rules_directory: /tmp/loki/rules
                replication_factor: 1
                ring:
                  kvstore:
                    store: inmemory
              schema_config:
                configs:
                  - from: 2020-10-24
                    store: tsdb
                    object_store: filesystem
                    schema: v13
                    index:
                      prefix: index_
                      period: 24h
              limits_config:
                allow_structured_metadata: false
              EOF
              loki -config.file=/tmp/loki-config.yaml -config.expand-env=true > /tmp/loki.log 2>&1 &
              LOKI_PID=$!
              # Wait for Loki to be ready
              wait-for-it localhost:${LOKI_PORT} -t 60
            start: loki-canary -addr=http://localhost:${LOKI_PORT} -port=${CANARY_PORT} -labelname=canary -labelvalue=test
            expected_output: "waiting for probe"
            timeout: 60
            post: |-
              set -euo pipefail
              # Wait for canary to write some logs
              sleep 10
              # Verify canary metrics endpoint is responding
              curl -f "http://localhost:${CANARY_PORT}/metrics" | grep -F "loki_canary"

  - name: ${{package.name}}-iamguarded-compat
    dependencies:
      provides:
        - loki-fips-iamguarded-compat=${{package.full-version}}
    pipeline:
      - uses: iamguarded/build-compat
        with:
          package: grafana-loki
          version: ${{vars.major-version}}
      - runs: |
          mkdir -p "/opt/iamguarded/grafana-loki/bin"
          mkdir -p "/bitnami/grafana-loki/wal"
          mkdir -p "/bitnami/grafana-loki/data"
          mkdir -p "/bitnami/grafana-loki/loki"
          ln -s "/usr/bin/loki" "/opt/iamguarded/grafana-loki/bin/loki"
          ln -s "/usr/bin/logcli" "/opt/iamguarded/grafana-loki/bin/logcli"
          ln -s "/opt/iamguarded/grafana-loki/wal" "/wal"
          ln -s "/opt/iamguarded/grafana-loki/data" "/data"
          ln -s "/opt/iamguarded/grafana-loki/loki" "/loki"
      - uses: iamguarded/finalize-compat
        with:
          package: grafana-loki
          version: ${{vars.major-version}}
    test:
      pipeline:
        - uses: iamguarded/test-compat
          with:
            package: grafana-loki
            version: ${{vars.major-version}}
        - uses: test/tw/symlink-check
        - runs: |
            stat /opt/iamguarded/grafana-loki/bin/loki
            stat /opt/iamguarded/grafana-loki/bin/logcli
        - uses: test/virtualpackage
          with:
            virtual-pkg-name: ${{package.name}}
            real-pkg-name: loki-fips-iamguarded-compat

update:
  enabled: true
  github:
    identifier: grafana/loki
    strip-prefix: v
    tag-filter: v3.6

test:
  environment:
    contents:
      packages:
        - curl
    environment:
      LOKI_PORT: "3100"
  pipeline:
    - uses: test/go-fips-check
    - name: Verify Loki server starts and accepts logs
      uses: test/daemon-check-output
      with:
        setup: |-
          mkdir -p /tmp/loki/chunks /tmp/loki/rules
          tee /tmp/loki-config.yaml <<EOF
          auth_enabled: false
          server:
            http_listen_port: ${LOKI_PORT}
          common:
            path_prefix: /tmp/loki
            storage:
              filesystem:
                chunks_directory: /tmp/loki/chunks
                rules_directory: /tmp/loki/rules
            replication_factor: 1
            ring:
              kvstore:
                store: inmemory
          schema_config:
            configs:
              - from: 2020-10-24
                store: tsdb
                object_store: filesystem
                schema: v13
                index:
                  prefix: index_
                  period: 24h
          limits_config:
            allow_structured_metadata: false
          EOF
        start: loki -config.file=/tmp/loki-config.yaml -config.expand-env=true
        expected_output: "server listening on addresses"
        timeout: 60
        post: |-
          set -euo pipefail
          # Wait for Loki to be fully ready
          for i in $(seq 1 30); do
            if curl -sf "http://localhost:${LOKI_PORT}/ready"; then
              break
            fi
            sleep 2
          done
          # Verify Loki is ready
          curl -f "http://localhost:${LOKI_PORT}/ready"
          # Push test logs
          curl -f -X POST "http://localhost:${LOKI_PORT}/loki/api/v1/push" \
            -H "Content-Type: application/json" \
            -d '{
              "streams": [
                {
                  "stream": {"job": "test", "level": "info"},
                  "values": [["'$(date +%s)000000000'", "test log message"]]
                }
              ]
            }'
          # Verify metrics endpoint
          curl -f "http://localhost:${LOKI_PORT}/metrics" | grep -F "loki_"
