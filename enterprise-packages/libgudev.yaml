package:
  name: libgudev
  version: "238"
  epoch: 1
  description: GObject-based wrapper library for libudev
  copyright:
    - license: LGPL-2.1-or-later

environment:
  contents:
    packages:
      - build-base
      - busybox
      - eudev-dev
      - glib-dev
      - meson

pipeline:
  - uses: git-checkout
    with:
      repository: https://gitlab.gnome.org/GNOME/libgudev.git
      tag: ${{package.version}}
      expected-commit: df7c9c9940160307aaeb31347f4776a46f8736a9

  - uses: meson/configure
    with:
      opts: |
        -Dtests=disabled \
        -Dintrospection=disabled \
        -Dvapi=disabled \
        -Dgtk_doc=false

  - uses: meson/compile

  - uses: meson/install

  - uses: strip

subpackages:
  - name: ${{package.name}}-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - ${{package.name}}
        - libudev-dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

test:
  pipeline:
    - uses: test/tw/ldd-check
    - name: Verify libgudev library
      runs: |
        set -euo pipefail
        stat /usr/lib/libgudev-1.0.so*
        echo "libgudev library test passed"

update:
  enabled: true
  git:
    strip-prefix: ""
