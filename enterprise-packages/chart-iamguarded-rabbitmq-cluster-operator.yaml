package:
  name: chart-iamguarded-rabbitmq-cluster-operator
  version: "4.4.37"
  epoch: 0
  description: rabbitmq-cluster-operator iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: rabbitmq-cluster-operator/${{package.version}}
      expected-commit: 8b08cd4774e6847cf3e76f39622d033404de4a4b

  - uses: patch
    with:
      patches: iamguarded.patch

  # See https://github.com/chainguard-dev/internal-dev/issues/24524.
  # The cluster-operator cut over from endpoints to endpointslices but the upstream
  # chart hasn't caught up yet.
  - uses: patch
    with:
      patches: clusterrole.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/rabbitmq-cluster-operator

  - uses: charts/package
    with:
      chart-path: iamguarded/rabbitmq-cluster-operator

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: rabbitmq-cluster-operator/
    use-tag: true
    tag-filter-prefix: rabbitmq-cluster-operator/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: rabbitmq-cluster-operator
