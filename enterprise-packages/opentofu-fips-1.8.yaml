package:
  name: opentofu-fips-1.8
  version: "1.8.11"
  epoch: 12 # go/enterprise-rsc/opentofu-fips-1.8
  copyright:
    - license: MPL-2.0
  dependencies:
    provides:
      - opentofu-fips=${{package.full-version}}
  resources:
    cpu: "2"
    memory: 7Gi

pipeline:
  - uses: git-checkout
    with:
      expected-commit: c4585918fa4946dd4ac73c8145898101a1adbadd
      repository: https://github.com/opentofu/opentofu
      tag: v${{package.version}}

  - uses: go/bump
    with:
      deps: |-
        github.com/cloudflare/circl@v1.6.1
        github.com/hashicorp/go-getter@v1.7.9
        github.com/go-jose/go-jose/v3@v3.0.4
        github.com/go-viper/mapstructure/v2@v2.4.0
        github.com/golang-jwt/jwt/v4@v4.5.2
        github.com/ulikunitz/xz@v0.5.14
        golang.org/x/crypto@v0.45.0
        golang.org/x/oauth2@v0.27.0

  - uses: go/build
    with:
      go-package: go-fips
      ldflags: -X 'github.com/opentofu/opentofu/version.dev=no'
      modroot: .
      output: tofu
      packages: ./cmd/tofu

update:
  enabled: true
  ignore-regex-patterns:
    - "rc"
    - "beta"
  github:
    identifier: opentofu/opentofu
    strip-prefix: v
    use-tag: true
    tag-filter-prefix: v1.8

test:
  pipeline:
    - uses: test/go-fips-check
    - uses: test/tw/ver-check
      with:
        bins: tofu
    - uses: test/tw/help-check
      with:
        bins: tofu
    - name: tofu Initialization
      runs: |
        set -euo pipefail
        echo "provider \"local\" {}" > example.tf
        # Verify init succeeds (fails test if init fails)
        tofu init -input=false
    - name: tofu Plan Creation
      runs: |
        set -euo pipefail
        # Verify plan creation succeeds (fails test if plan fails)
        tofu plan -input=false -out=tfplan
    - name: tofu Apply Execution
      runs: |-
        set -euo pipefail
        # Verify apply succeeds (fails test if apply fails)
        tofu apply -input=false tfplan

environment:
  contents:
    packages:
      - go-fips-1.25
