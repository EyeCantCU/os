package:
  name: chart-iamguarded-kibana
  version: "12.1.9"
  epoch: 1
  description: Kibana iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: kibana/${{package.version}}
      expected-commit: b3130ee8577511a79b8f6457d3a2076fe7d47fdb

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/kibana

  - uses: charts/package
    with:
      chart-path: iamguarded/kibana

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: kibana/
    use-tag: true
    tag-filter-prefix: kibana/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /kibana
