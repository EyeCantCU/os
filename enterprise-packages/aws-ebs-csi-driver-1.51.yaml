package:
  name: aws-ebs-csi-driver-1.51
  version: "1.51.2"
  epoch: 2
  description: CSI driver for Amazon EBS.
  copyright:
    - license: Apache-2.0
  dependencies:
    provides:
      - aws-ebs-csi-driver=${{package.full-version}}
    runtime:
      - blkid
      - blockdev
      - ca-certificates
      - ca-certificates-bundle
      - e2fsprogs
      - e2fsprogs-extra
      - lsblk
      - merged-bin
      - mount
      - umount
      - util-linux
      - util-linux-misc
      - wolfi-baselayout
      - xfsprogs-5.10

environment:
  contents:
    packages:
      - build-base
      - busybox

pipeline:
  # We can't use go/install because this requires specific ldflags to set the version
  - uses: git-checkout
    with:
      repository: https://github.com/kubernetes-sigs/aws-ebs-csi-driver
      tag: v${{package.version}}
      expected-commit: d41d8a9bdd2cf465161ba47c4169ef0d604bcdbe

  - uses: go/build
    with:
      packages: ./cmd
      ldflags: |
        -X github.com/kubernetes-sigs/aws-ebs-csi-driver/pkg/driver.driverVersion=${{package.version}}
        -X github.com/kubernetes-sigs/aws-ebs-csi-driver/pkg/cloud.driverVersion=${{package.version}}
        -X github.com/kubernetes-sigs/aws-ebs-csi-driver/pkg/driver.gitCommit=$(git rev-parse HEAD)
        -X github.com/kubernetes-sigs/aws-ebs-csi-driver/pkg/driver.buildDate=$(date -u -d "@${SOURCE_DATE_EPOCH:-$(date +%s)}" "+%Y-%m-%dT%H:%M:%SZ")
      output: aws-ebs-csi-driver

update:
  enabled: true
  ignore-regex-patterns:
    - helm-chart-aws-ebs-csi-driver-
  github:
    identifier: kubernetes-sigs/aws-ebs-csi-driver
    strip-prefix: v
    tag-filter: v1.51

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/help-check
      with:
        bins: aws-ebs-csi-driver
    - uses: test/tw/ver-check
      with:
        bins: aws-ebs-csi-driver
    - name: Test controller mode initialization and CSI gRPC identity service
      uses: test/daemon-check-output
      with:
        setup: |
          set -euo pipefail
          # Create directory for CSI socket endpoint
          mkdir -p /tmp/csi-controller
          # Create a minimal kubeconfig file to bypass Kubernetes metadata retrieval
          mkdir -p /tmp/.kube
          tee /tmp/.kube/config << 'EOF'
          apiVersion: v1
          kind: Config
          clusters:
          - cluster:
              server: https://localhost:443
            name: test-cluster
          contexts:
          - context:
              cluster: test-cluster
              user: test-user
            name: test-context
          current-context: test-context
          users:
          - name: test-user
            user:
              token: test-token
          EOF
        start: env AWS_REGION=us-east-1 AWS_ACCESS_KEY_ID=test AWS_SECRET_ACCESS_KEY=test /usr/bin/aws-ebs-csi-driver controller --endpoint=unix:///tmp/csi-controller/csi.sock
        expected_output: "Driver Information.*ebs.csi.aws.com"
        timeout: "30"
        error_strings: ""
        post: |
          set -euo pipefail
          # Verify CSI socket was created
          stat /tmp/csi-controller/csi.sock
    - name: Test node subcommand exists
      runs: |
        set -euo pipefail
        # Since node mode requires complex metadata setup, just verify the subcommand responds to help
        /usr/bin/aws-ebs-csi-driver node --help
