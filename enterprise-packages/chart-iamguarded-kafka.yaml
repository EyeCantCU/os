package:
  name: chart-iamguarded-kafka
  version: "32.4.2"
  epoch: 2
  description: Apache Kafka iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: kafka/${{package.version}}
      expected-commit: 93465c0d2e5763aa1b0ab5dfcb4e8190be6ed004

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/kafka

  - uses: charts/package
    with:
      chart-path: iamguarded/kafka

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: kafka/
    use-tag: true
    tag-filter-prefix: kafka/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /kafka
