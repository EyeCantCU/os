package:
  name: chart-iamguarded-mysql
  version: "14.0.3"
  epoch: 1
  description: MySQL iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: mysql/${{package.version}}
      expected-commit: 79196dc5824d51adce926b809c6f7ecb7f9f0a43

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/mysql

  - uses: charts/package
    with:
      chart-path: iamguarded/mysql

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: mysql/
    use-tag: true
    tag-filter-prefix: mysql/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /mysql
