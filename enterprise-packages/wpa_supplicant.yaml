# Generated from https://git.alpinelinux.org/aports/plain/main/wpa_supplicant/APKBUILD
package:
  name: wpa_supplicant
  version: "2.11"
  epoch: 1
  description: utility providing key negotiation for WPA wireless networks
  copyright:
    - license: BSD-3-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - dbus-dev
      - libnl3-dev
      - linux-headers
      - openssl-dev
      - pcsc-lite-dev

pipeline:
  - uses: fetch
    with:
      expected-sha256: 912ea06f74e30a8e36fbb68064d6cdff218d8d591db0fc5d75dee6c81ac7fc0a
      uri: https://w1.fi/releases/wpa_supplicant-${{package.version}}.tar.gz

  - runs: cp .config wpa_supplicant/.config

  - working-directory: wpa_supplicant
    pipeline:
      - uses: autoconf/make
      - uses: autoconf/make-install

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/ver-check
      with:
        bins: wpa_supplicant

update:
  enabled: true
  release-monitor:
    identifier: 5146
