# Fix SslFactoryTest for BCFKS keystore compatibility:
# - BCFKS uses different alias naming than JKS/PKCS12; use aliases() to find entries
# - Use KeyStore.getDefaultType() instead of hardcoded "PKCS12"
# NOTE: CURRENTLY UNUSED IN BUILD BECAUSE OF SCRAM AND PEM TESTS
diff --git a/clients/src/test/java/org/apache/kafka/common/security/ssl/SslFactoryTest.java b/clients/src/test/java/org/apache/kafka/common/security/ssl/SslFactoryTest.java
index 1c82dc6..0ffd1b0 100644
--- a/clients/src/test/java/org/apache/kafka/common/security/ssl/SslFactoryTest.java
+++ b/clients/src/test/java/org/apache/kafka/common/security/ssl/SslFactoryTest.java
@@ -42,6 +42,7 @@ import java.security.GeneralSecurityException;
 import java.security.KeyPair;
 import java.security.KeyStore;
 import java.security.Security;
+import java.security.cert.Certificate;
 import java.security.cert.X509Certificate;
 import java.util.Arrays;
 import java.util.Map;
@@ -460,7 +461,10 @@ public abstract class SslFactoryTest {
         assertEquals(SslFactory.CertificateEntries.create(ks1), SslFactory.CertificateEntries.create(ks2));
 
         // Use different alias name, validation should succeed
-        ks2.setCertificateEntry("another", ks1.getCertificate("localhost"));
+        String existingAlias = ks1.aliases().nextElement();
+        Certificate cert = ks2.getCertificate(existingAlias);
+        ks2.deleteEntry(existingAlias);
+        ks2.setCertificateEntry("another", cert);
         assertEquals(SslFactory.CertificateEntries.create(ks1), SslFactory.CertificateEntries.create(ks2));
 
         KeyStore ks3 = sslKeyStore(newCnConfig);
@@ -581,7 +585,7 @@ public abstract class SslFactoryTest {
     private KeyStore createKeyStore(KeyPair keyPair, String commonName, String org, boolean utf8, String... dnsNames) throws Exception {
         X509Certificate cert = new TestSslUtils.CertificateBuilder().sanDnsNames(dnsNames)
                 .generate(commonName, org, utf8, keyPair);
-        KeyStore ks = KeyStore.getInstance("PKCS12");
+        KeyStore ks = KeyStore.getInstance(KeyStore.getDefaultType());
         ks.load(null, null);
         ks.setKeyEntry("kafka", keyPair.getPrivate(), null, new X509Certificate[] {cert});
         return ks;
