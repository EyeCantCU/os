package:
  name: chart-iamguarded-metrics-server
  version: "7.4.12"
  epoch: 3 # go/enterprise-rsc/chart-iamguarded-metrics-server
  description: metrics-server iamguarded chart
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "2"
    memory: 1Gi

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: metrics-server/${{package.version}}
      expected-commit: 0dcb0def8f957cefbfe4ddb0e4cc48cbfe03aeeb

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/metrics-server

  - uses: charts/package
    with:
      chart-path: iamguarded/metrics-server

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: metrics-server/
    use-tag: true
    tag-filter-prefix: metrics-server/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /metrics-server
