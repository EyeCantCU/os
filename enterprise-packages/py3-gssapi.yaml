package:
  name: py3-gssapi
  version: "1.10.1"
  epoch: 1
  description: Low-level and high level wrappers around the GSSAPI C libraries
  copyright:
    - license: ISC

vars:
  pypi-package: gssapi

environment:
  contents:
    packages:
      - krb5-dev
      - py3-supported-build-base-dev
      - py3-supported-cython

data:
  - name: py-versions
    items:
      3.10:
      3.11:
      3.12:
      3.13:

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pythongssapi/python-gssapi.git
      tag: v${{package.version}}
      expected-commit: 55df351f3c3e49266dda49327b28239b991c8246

subpackages:
  - range: py-versions
    name: py${{range.key}}-${{vars.pypi-package}}
    description: ${{vars.pypi-package}} installed for python${{range.key}}
    dependencies:
      runtime:
        - py${{range.key}}-decorator
    pipeline:
      - uses: py/pip-build-install
        with:
          python: python${{range.key}}
      - uses: strip
    test:
      pipeline:
        - uses: test/tw/pip-check
        - uses: python/import
          with:
            python: python${{range.key}}
            import: gssapi

update:
  enabled: true
  git:
    strip-prefix: v

test:
  pipeline:
    - uses: test/tw/byproductpackage
