package:
  name: jbang
  version: "0.135.1"
  epoch: 1 # go/enterprise-rsc/jbang
  description: "JBang - Unleash the power of Java for scripting and beyond"
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - bash
      - openjdk-${{vars.JAVA_VERSION}}-jre
  resources:
    cpu: "2"
    memory: 6Gi

vars:
  JAVA_VERSION: 11 # https://github.com/jbangdev/jbang/blob/main/build.gradle#L48

environment:
  contents:
    packages:
      - bash
      - busybox
      - openjdk-${{vars.JAVA_VERSION}}-default-jdk
  environment:
    JAVA_HOME: /usr/lib/jvm/java-${{vars.JAVA_VERSION}}-openjdk

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/jbangdev/jbang
      tag: v${{package.version}}
      expected-commit: ac71400a123f3e460e14e98c97ba873d678035cb
      destination: jbang

  - name: Build JBang from source
    working-directory: jbang
    runs: |
      # Set version explicitly since git-versioner plugin needs git tags

      sed -i '/versioner\.apply()/a project.version = "'${{package.version}}'"' build.gradle

      # Remove vendor restriction from Java toolchain to accept any Java 11
      sed -i '/vendor = JvmVendorSpec\.ADOPTIUM/d' build.gradle

      ./gradlew --no-daemon clean installDist -x test

      mkdir -p "${{targets.destdir}}/usr/share/jbang"
      cp -r build/install/jbang/* "${{targets.destdir}}/usr/share/jbang/"

      # Create symlink in /usr/bin
      mkdir -p "${{targets.destdir}}/usr/bin"
      ln -sf /usr/share/jbang/bin/jbang "${{targets.destdir}}/usr/bin/jbang"

update:
  enabled: true
  git:
    strip-prefix: v

test:
  environment:
    contents:
      packages:
        - openjdk-${{vars.JAVA_VERSION}}-default-jdk
    environment:
      JAVA_HOME: /usr/lib/jvm/java-${{vars.JAVA_VERSION}}-openjdk
  pipeline:
    - uses: test/tw/help-check
      with:
        bins: jbang
    - uses: test/tw/ver-check
      with:
        bins: jbang
    - name: Test JBang basic functionality
      runs: |
        set -euo pipefail
        cd "$(mktemp -d)"

        # Create a simple Hello World script
        tee hello.java <<'EOF'
        public class hello {
            public static void main(String[] args) {
                System.out.println("Hello from JBang!");
            }
        }
        EOF

        # Run the script
        jbang hello.java | grep -F "Hello from JBang!"
