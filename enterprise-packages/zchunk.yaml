package:
  name: zchunk
  version: "1.5.3"
  epoch: 0
  description: A file format designed for highly efficient deltas while maintaining good compression
  copyright:
    - license: BSD-2-Clause

environment:
  contents:
    packages:
      - build-base
      - busybox
      - cmake
      - curl-dev
      - meson
      - openssl-dev
      - zstd-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/zchunk/zchunk.git
      tag: ${{package.version}}
      expected-commit: 366cf22e725fc273abe0e082c707753cce76c453

  - uses: meson/configure

  - uses: meson/compile

  - uses: meson/install

subpackages:
  - name: ${{package.name}}-dev
    pipeline:
      - uses: split/dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

  - name: ${{package.name}}-doc
    pipeline:
      - uses: split/manpages
    test:
      pipeline:
        - uses: test/docs

test:
  pipeline:
    - uses: test/tw/ldd-check

update:
  enabled: true
  github:
    identifier: zchunk/zchunk
    use-tag: true
