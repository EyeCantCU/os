package:
  name: goose
  version: "3.27.0"
  epoch: 2
  description: "Database migration tool supporting SQL migrations and Go functions"
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - go-1.25

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pressly/goose
      tag: v${{package.version}}
      expected-commit: 7fb1461b2bca662c96ce736281a3fedd66bd0867

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0
      modroot: internal/testing

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/net@v0.51.0

  - uses: go/build
    with:
      packages: ./cmd/goose
      output: goose
      ldflags: -X main.version=${{package.version}}

  - uses: strip

test:
  pipeline:
    - uses: test/tw/ldd-check
    - uses: test/tw/help-check
      with:
        bins: goose
    - uses: test/tw/ver-check
      with:
        bins: goose
    - name: Test goose commands
      runs: |
        set -euo pipefail
        # Test create command (generates migration file)
        mkdir -p /tmp/migrations
        goose -dir /tmp/migrations create test_migration sql
        stat /tmp/migrations/*_test_migration.sql

        # Test status with sqlite
        goose -dir /tmp/migrations sqlite3 /tmp/test.db status

update:
  enabled: true
  github:
    identifier: pressly/goose
    strip-prefix: v
