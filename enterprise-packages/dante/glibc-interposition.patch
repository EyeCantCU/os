--- a/dlib/interposition.c	2026-01-29 01:04:41
+++ b/dlib/interposition.c	2026-01-29 01:05:17
@@ -53,11 +53,11 @@
 
 #if (defined __sun) && (defined _XPG4_2)
 
-HAVE_PROT_BIND_0
-bind(HAVE_PROT_BIND_1, HAVE_PROT_BIND_2, HAVE_PROT_BIND_3);
+int
+bind(int, const struct sockaddr *, socklen_t);
 
-HAVE_PROT_CONNECT_0
-connect(HAVE_PROT_CONNECT_1, HAVE_PROT_CONNECT_2, HAVE_PROT_CONNECT_3);
+int
+connect(int, const struct sockaddr *, socklen_t);
 
 HAVE_PROT_LISTEN_0
 listen(HAVE_PROT_LISTEN_1, HAVE_PROT_LISTEN_2);
@@ -68,26 +68,26 @@
 HAVE_PROT_SENDMSG_0
 sendmsg(HAVE_PROT_SENDMSG_1, HAVE_PROT_SENDMSG_2, HAVE_PROT_SENDMSG_3);
 
-HAVE_PROT_SENDTO_0
-sendto(HAVE_PROT_SENDTO_1, HAVE_PROT_SENDTO_2, HAVE_PROT_SENDTO_3,
-    HAVE_PROT_SENDTO_4, HAVE_PROT_SENDTO_5, HAVE_PROT_SENDTO_6);
+ssize_t
+sendto(int, const void *, size_t,
+    int, const struct sockaddr *, socklen_t);
 
 #endif /* (defined __sun) && (defined _XPG4_2) */
 
 #if HAVE_DARWIN
 
-HAVE_PROT_CONNECT_0
-connect$NOCANCEL(HAVE_PROT_CONNECT_1, HAVE_PROT_CONNECT_2,
-       HAVE_PROT_CONNECT_3);
+int
+connect$NOCANCEL(int, const struct sockaddr *,
+       socklen_t);
 HAVE_PROT_READ_0
 read$NOCANCEL(HAVE_PROT_READ_1, HAVE_PROT_READ_2, HAVE_PROT_READ_3);
-HAVE_PROT_RECVFROM_0
-recvfrom$NOCANCEL(HAVE_PROT_RECVFROM_1, HAVE_PROT_RECVFROM_2,
-  HAVE_PROT_RECVFROM_3, HAVE_PROT_RECVFROM_4,
-        HAVE_PROT_RECVFROM_5, HAVE_PROT_RECVFROM_6);
-HAVE_PROT_SENDTO_0
-sendto$NOCANCEL(HAVE_PROT_SENDTO_1, HAVE_PROT_SENDTO_2, HAVE_PROT_SENDTO_3,
-      HAVE_PROT_SENDTO_4, HAVE_PROT_SENDTO_5, HAVE_PROT_SENDTO_6);
+ssize_t
+recvfrom$NOCANCEL(int, void *,
+  size_t, int,
+        struct sockaddr *, socklen_t *);
+ssize_t
+sendto$NOCANCEL(int, const void *, size_t,
+      int, const struct sockaddr *, socklen_t);
 HAVE_PROT_WRITE_0
 write$NOCANCEL(HAVE_PROT_WRITE_1, HAVE_PROT_WRITE_2, HAVE_PROT_WRITE_3);
 
@@ -448,8 +448,7 @@
 }
 
 void
-socks_syscall_start(s)
-   const int s;
+socks_syscall_start(const int s)
 {
    socksfd_t *p;
    addrlockopaque_t opaque;
@@ -484,8 +483,7 @@
 }
 
 void
-socks_syscall_end(s)
-   const int s;
+socks_syscall_end(const int s)
 {
    addrlockopaque_t opaque;
    socksfd_t socksfd, *p;
@@ -777,16 +775,13 @@
 
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_ACCEPT_0
-sys_accept(s, addr, addrlen)
-   HAVE_PROT_ACCEPT_1 s;
-   HAVE_PROT_ACCEPT_2 addr;
-   HAVE_PROT_ACCEPT_3 addrlen;
+int
+sys_accept(int s, struct sockaddr * addr, socklen_t * addrlen)
 {
    int rc;
-   typedef HAVE_PROT_ACCEPT_0 (*ACCEPT_FUNC_T)(HAVE_PROT_ACCEPT_1,
-                                               HAVE_PROT_ACCEPT_2,
-                                               HAVE_PROT_ACCEPT_3);
+   typedef int (*ACCEPT_FUNC_T)(int,
+                                               struct sockaddr *,
+                                               socklen_t *);
    ACCEPT_FUNC_T function = (ACCEPT_FUNC_T)symbolfunction(SYMBOL_ACCEPT);
    const int tagged = (doing_addrinit ? 0 : 1);
 
@@ -803,16 +798,13 @@
 #endif /* !HAVE_EXTRA_OSF_SYMBOLS */
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_BIND_0
-sys_bind(s, name, namelen)
-   HAVE_PROT_BIND_1 s;
-   HAVE_PROT_BIND_2 name;
-   HAVE_PROT_BIND_3 namelen;
+int
+sys_bind(int s, const struct sockaddr * name, socklen_t namelen)
 {
    int rc;
-   typedef HAVE_PROT_BIND_0 (*BIND_FUNC_T)(HAVE_PROT_BIND_1,
-                                           HAVE_PROT_BIND_2,
-                                           HAVE_PROT_BIND_3);
+   typedef int (*BIND_FUNC_T)(int,
+                                           const struct sockaddr *,
+                                           socklen_t);
    BIND_FUNC_T function = (BIND_FUNC_T)symbolfunction(SYMBOL_BIND);
    const int tagged = (doing_addrinit ? 0 : 1);
 
@@ -851,16 +843,13 @@
 }
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_CONNECT_0
-sys_connect(s, name, namelen)
-   HAVE_PROT_CONNECT_1 s;
-   HAVE_PROT_CONNECT_2 name;
-   HAVE_PROT_CONNECT_3 namelen;
+int
+sys_connect(int s, const struct sockaddr * name, socklen_t namelen)
 {
    int rc;
-   typedef HAVE_PROT_CONNECT_0 (*CONNECT_FUNC_T)(HAVE_PROT_CONNECT_1,
-                                                 HAVE_PROT_CONNECT_2,
-                                                 HAVE_PROT_CONNECT_3);
+   typedef int (*CONNECT_FUNC_T)(int,
+                                                 const struct sockaddr *,
+                                                 socklen_t);
    CONNECT_FUNC_T function = (CONNECT_FUNC_T)symbolfunction(SYMBOL_CONNECT);
    const int tagged = (doing_addrinit ? 0 : 1);
 
@@ -877,17 +866,14 @@
 #endif /* !HAVE_EXTRA_OSF_SYMBOLS */
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_GETPEERNAME_0
-sys_getpeername(s, name, namelen)
-   HAVE_PROT_GETPEERNAME_1 s;
-   HAVE_PROT_GETPEERNAME_2 name;
-   HAVE_PROT_GETPEERNAME_3 namelen;
+int
+sys_getpeername(int s, struct sockaddr * name, socklen_t * namelen)
 {
    int rc;
-   typedef HAVE_PROT_GETPEERNAME_0
-       (*GETPEERNAME_FUNC_T)(HAVE_PROT_GETPEERNAME_1,
-                             HAVE_PROT_GETPEERNAME_2,
-                             HAVE_PROT_GETPEERNAME_3);
+   typedef int
+       (*GETPEERNAME_FUNC_T)(int,
+                             struct sockaddr *,
+                             socklen_t *);
    GETPEERNAME_FUNC_T function
    = (GETPEERNAME_FUNC_T)symbolfunction(SYMBOL_GETPEERNAME);
    const int tagged = (doing_addrinit ? 0 : 1);
@@ -905,17 +891,14 @@
 #endif /* !HAVE_EXTRA_OSF_SYMBOLS */
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_GETSOCKNAME_0
-sys_getsockname(s, name, namelen)
-   HAVE_PROT_GETSOCKNAME_1 s;
-   HAVE_PROT_GETSOCKNAME_2 name;
-   HAVE_PROT_GETSOCKNAME_3 namelen;
+int
+sys_getsockname(int s, struct sockaddr * name, socklen_t * namelen)
 {
    int rc;
-   typedef HAVE_PROT_GETSOCKNAME_0
-       (*GETSOCKNAME_FUNC_T)(HAVE_PROT_GETSOCKNAME_1,
-                             HAVE_PROT_GETSOCKNAME_2,
-                             HAVE_PROT_GETSOCKNAME_3);
+   typedef int
+       (*GETSOCKNAME_FUNC_T)(int,
+                             struct sockaddr *,
+                             socklen_t *);
    GETSOCKNAME_FUNC_T function
    = (GETSOCKNAME_FUNC_T)symbolfunction(SYMBOL_GETSOCKNAME);
    const int tagged = (doing_addrinit ? 0 : 1);
@@ -931,16 +914,13 @@
    return rc;
 }
 
-HAVE_PROT_GETSOCKNAME_0
-sys_getsockname_notracking(s, name, namelen)
-   HAVE_PROT_GETSOCKNAME_1 s;
-   HAVE_PROT_GETSOCKNAME_2 name;
-   HAVE_PROT_GETSOCKNAME_3 namelen;
+int
+sys_getsockname_notracking(int s, struct sockaddr * name, socklen_t * namelen)
 {
-   typedef HAVE_PROT_GETSOCKNAME_0
-       (*GETSOCKNAME_FUNC_T)(HAVE_PROT_GETSOCKNAME_1,
-                             HAVE_PROT_GETSOCKNAME_2,
-                             HAVE_PROT_GETSOCKNAME_3);
+   typedef int
+       (*GETSOCKNAME_FUNC_T)(int,
+                             struct sockaddr *,
+                             socklen_t *);
    GETSOCKNAME_FUNC_T function
    = (GETSOCKNAME_FUNC_T)symbolfunction(SYMBOL_GETSOCKNAME);
 
@@ -951,12 +931,7 @@
 #endif /* !HAVE_EXTRA_OSF_SYMBOLS */
 
 HAVE_PROT_GETSOCKOPT_0
-sys_getsockopt(s, level, optname, optval, optlen)
-   HAVE_PROT_GETSOCKOPT_1 s;
-   HAVE_PROT_GETSOCKOPT_2 level;
-   HAVE_PROT_GETSOCKOPT_3 optname;
-   HAVE_PROT_GETSOCKOPT_4 optval;
-   HAVE_PROT_GETSOCKOPT_5 optlen;
+sys_getsockopt(HAVE_PROT_GETSOCKOPT_1 s, HAVE_PROT_GETSOCKOPT_2 level, HAVE_PROT_GETSOCKOPT_3 optname, HAVE_PROT_GETSOCKOPT_4 optval, HAVE_PROT_GETSOCKOPT_5 optlen)
 {
    int rc;
    typedef HAVE_PROT_GETSOCKOPT_0
@@ -981,9 +956,7 @@
 }
 
 HAVE_PROT_LISTEN_0
-sys_listen(s, backlog)
-   HAVE_PROT_LISTEN_1 s;
-   HAVE_PROT_LISTEN_2 backlog;
+sys_listen(HAVE_PROT_LISTEN_1 s, HAVE_PROT_LISTEN_2 backlog)
 {
    HAVE_PROT_LISTEN_0 rc;
    typedef HAVE_PROT_LISTEN_0 (*LISTEN_FUNC_T)(HAVE_PROT_LISTEN_1,
@@ -1003,10 +976,7 @@
 }
 
 HAVE_PROT_READ_0
-sys_read(d, buf, nbytes)
-   HAVE_PROT_READ_1 d;
-   HAVE_PROT_READ_2 buf;
-   HAVE_PROT_READ_3 nbytes;
+sys_read(HAVE_PROT_READ_1 d, HAVE_PROT_READ_2 buf, HAVE_PROT_READ_3 nbytes)
 {
    ssize_t rc;
    typedef HAVE_PROT_READ_0 (*READ_FUNC_T)(HAVE_PROT_READ_1,
@@ -1029,10 +999,7 @@
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
 HAVE_PROT_READV_0
-sys_readv(d, iov, iovcnt)
-   HAVE_PROT_READV_1 d;
-   HAVE_PROT_READV_2 iov;
-   HAVE_PROT_READV_3 iovcnt;
+sys_readv(HAVE_PROT_READV_1 d, HAVE_PROT_READV_2 iov, HAVE_PROT_READV_3 iovcnt)
 {
    ssize_t rc;
    typedef HAVE_PROT_READV_0 (*READV_FUNC_T)(HAVE_PROT_READV_1,
@@ -1054,11 +1021,7 @@
 #endif /* HAVE_EXTRA_OSF_SYMBOLS */
 
 HAVE_PROT_RECV_0
-sys_recv(s, buf, len, flags)
-   HAVE_PROT_RECV_1 s;
-   HAVE_PROT_RECV_2 buf;
-   HAVE_PROT_RECV_3 len;
-   HAVE_PROT_RECV_4 flags;
+sys_recv(HAVE_PROT_RECV_1 s, HAVE_PROT_RECV_2 buf, HAVE_PROT_RECV_3 len, HAVE_PROT_RECV_4 flags)
 {
    ssize_t rc;
    typedef HAVE_PROT_RECV_0 (*RECV_FUNC_T)(HAVE_PROT_RECV_1,
@@ -1080,22 +1043,16 @@
 }
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_RECVFROM_0
-sys_recvfrom(s, buf, len, flags, from, fromlen)
-   HAVE_PROT_RECVFROM_1 s;
-   HAVE_PROT_RECVFROM_2 buf;
-   HAVE_PROT_RECVFROM_3 len;
-   HAVE_PROT_RECVFROM_4 flags;
-   HAVE_PROT_RECVFROM_5 from;
-   HAVE_PROT_RECVFROM_6 fromlen;
+ssize_t
+sys_recvfrom(int s, void * buf, size_t len, int flags, struct sockaddr * from, socklen_t * fromlen)
 {
-   HAVE_PROT_RECVFROM_0 rc;
-   typedef HAVE_PROT_RECVFROM_0 (*RECVFROM_FUNC_T)(HAVE_PROT_RECVFROM_1,
-                                                   HAVE_PROT_RECVFROM_2,
-                                                   HAVE_PROT_RECVFROM_3,
-                                                   HAVE_PROT_RECVFROM_4,
-                                                   HAVE_PROT_RECVFROM_5,
-                                                   HAVE_PROT_RECVFROM_6);
+   ssize_t rc;
+   typedef ssize_t (*RECVFROM_FUNC_T)(int,
+                                                   void *,
+                                                   size_t,
+                                                   int,
+                                                   struct sockaddr *,
+                                                   socklen_t *);
    RECVFROM_FUNC_T function = (RECVFROM_FUNC_T)symbolfunction(SYMBOL_RECVFROM);
    const int tagged = (doing_addrinit ? 0 : 1);
 
@@ -1113,10 +1070,7 @@
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
 HAVE_PROT_RECVMSG_0
-sys_recvmsg(s, msg, flags)
-   HAVE_PROT_RECVMSG_1 s;
-   HAVE_PROT_RECVMSG_2 msg;
-   HAVE_PROT_RECVMSG_3 flags;
+sys_recvmsg(HAVE_PROT_RECVMSG_1 s, HAVE_PROT_RECVMSG_2 msg, HAVE_PROT_RECVMSG_3 flags)
 {
    ssize_t rc;
    typedef HAVE_PROT_RECVMSG_0 (*RECVMSG_FUNC_T)(HAVE_PROT_RECVMSG_1,
@@ -1151,11 +1105,7 @@
 #endif /* HAVE_RRESVPORT */
 
 HAVE_PROT_SEND_0
-sys_send(s, msg, len, flags)
-   HAVE_PROT_SEND_1 s;
-   HAVE_PROT_SEND_2 msg;
-   HAVE_PROT_SEND_3 len;
-   HAVE_PROT_SEND_4 flags;
+sys_send(HAVE_PROT_SEND_1 s, HAVE_PROT_SEND_2 msg, HAVE_PROT_SEND_3 len, HAVE_PROT_SEND_4 flags)
 {
    ssize_t rc;
    typedef HAVE_PROT_SEND_0 (*SEND_FUNC_T)(HAVE_PROT_SEND_1,
@@ -1178,10 +1128,7 @@
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
 HAVE_PROT_SENDMSG_0
-sys_sendmsg(s, msg, flags)
-   HAVE_PROT_SENDMSG_1 s;
-   HAVE_PROT_SENDMSG_2 msg;
-   HAVE_PROT_SENDMSG_3 flags;
+sys_sendmsg(HAVE_PROT_SENDMSG_1 s, HAVE_PROT_SENDMSG_2 msg, HAVE_PROT_SENDMSG_3 flags)
 {
    ssize_t rc;
    typedef HAVE_PROT_SENDMSG_0 (*SENDMSG_FUNC_T)(HAVE_PROT_SENDMSG_1,
@@ -1203,22 +1150,16 @@
 #endif /* HAVE_EXTRA_OSF_SYMBOLS */
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_SENDTO_0
-sys_sendto(s, msg, len, flags, to, tolen)
-   HAVE_PROT_SENDTO_1 s;
-   HAVE_PROT_SENDTO_2 msg;
-   HAVE_PROT_SENDTO_3 len;
-   HAVE_PROT_SENDTO_4 flags;
-   HAVE_PROT_SENDTO_5 to;
-   HAVE_PROT_SENDTO_6 tolen;
+ssize_t
+sys_sendto(int s, const void * msg, size_t len, int flags, const struct sockaddr * to, socklen_t tolen)
 {
    ssize_t rc;
-   typedef HAVE_PROT_SENDTO_0 (*SENDTO_FUNC_T)(HAVE_PROT_SENDTO_1,
-                                               HAVE_PROT_SENDTO_2,
-                                               HAVE_PROT_SENDTO_3,
-                                               HAVE_PROT_SENDTO_4,
-                                               HAVE_PROT_SENDTO_5,
-                                               HAVE_PROT_SENDTO_6);
+   typedef ssize_t (*SENDTO_FUNC_T)(int,
+                                               const void *,
+                                               size_t,
+                                               int,
+                                               const struct sockaddr *,
+                                               socklen_t);
    SENDTO_FUNC_T function = (SENDTO_FUNC_T)symbolfunction(SYMBOL_SENDTO);
    const int tagged = (doing_addrinit ? 0 : 1);
 
@@ -1235,10 +1176,7 @@
 #endif /* !HAVE_EXTRA_OSF_SYMBOLS */
 
 HAVE_PROT_WRITE_0
-sys_write(d, buf, nbytes)
-   HAVE_PROT_WRITE_1 d;
-   HAVE_PROT_WRITE_2 buf;
-   HAVE_PROT_WRITE_3 nbytes;
+sys_write(HAVE_PROT_WRITE_1 d, HAVE_PROT_WRITE_2 buf, HAVE_PROT_WRITE_3 nbytes)
 {
    ssize_t rc;
    typedef HAVE_PROT_WRITE_0 (*WRITE_FUNC_T)(HAVE_PROT_WRITE_1,
@@ -1262,10 +1200,7 @@
 HAVE_PROT_WRITE_0 syssys_write(HAVE_PROT_WRITE_1 d, HAVE_PROT_WRITE_2 buf,
                                 HAVE_PROT_WRITE_3 nbytes);
 HAVE_PROT_WRITE_0
-syssys_write(d, buf, nbytes)
-   HAVE_PROT_WRITE_1 d;
-   HAVE_PROT_WRITE_2 buf;
-   HAVE_PROT_WRITE_3 nbytes;
+syssys_write(HAVE_PROT_WRITE_1 d, HAVE_PROT_WRITE_2 buf, HAVE_PROT_WRITE_3 nbytes)
 {
    typedef HAVE_PROT_WRITE_0 (*WRITE_FUNC_T)(HAVE_PROT_WRITE_1,
                                              HAVE_PROT_WRITE_2,
@@ -1279,10 +1214,7 @@
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
 HAVE_PROT_WRITEV_0
-sys_writev(d, iov, iovcnt)
-   HAVE_PROT_WRITEV_1 d;
-   HAVE_PROT_WRITEV_2 iov;
-   HAVE_PROT_WRITEV_3 iovcnt;
+sys_writev(HAVE_PROT_WRITEV_1 d, HAVE_PROT_WRITEV_2 iov, HAVE_PROT_WRITEV_3 iovcnt)
 {
    ssize_t rc;
    typedef HAVE_PROT_WRITEV_0 (*WRITEV_FUNC_T)(HAVE_PROT_WRITEV_1,
@@ -1305,8 +1237,7 @@
 
 #if HAVE_GSSAPI && HAVE_LINUX_GLIBC_WORKAROUND
 HAVE_PROT_GETC_0
-sys_getc(stream)
-   HAVE_PROT_GETC_1 stream;
+sys_getc(HAVE_PROT_GETC_1 stream)
 {
    const int d = fileno(stream);
    HAVE_PROT_GETC_0 rc;
@@ -1326,8 +1257,7 @@
 }
 
 HAVE_PROT_FGETC_0
-sys_fgetc(stream)
-   HAVE_PROT_FGETC_1 stream;
+sys_fgetc(HAVE_PROT_FGETC_1 stream)
 {
    const int d = fileno(stream);
    HAVE_PROT_FGETC_0 rc;
@@ -1347,8 +1277,7 @@
 }
 
 HAVE_PROT_GETS_0
-sys_gets(buf)
-   HAVE_PROT_GETS_1 buf;
+sys_gets(HAVE_PROT_GETS_1 buf)
 {
    const int d = fileno(stdin);
    HAVE_PROT_GETS_0 rv;
@@ -1368,10 +1297,7 @@
 }
 
 HAVE_PROT_FGETS_0
-sys_fgets(buf, size, stream)
-   HAVE_PROT_FGETS_1 buf;
-   HAVE_PROT_FGETS_2 size;
-   HAVE_PROT_FGETS_3 stream;
+sys_fgets(HAVE_PROT_FGETS_1 buf, HAVE_PROT_FGETS_2 size, HAVE_PROT_FGETS_3 stream)
 {
    const int d = fileno(stream);
    HAVE_PROT_FGETS_0 rc;
@@ -1393,9 +1319,7 @@
 }
 
 HAVE_PROT_PUTC_0
-sys_putc(c, stream)
-   HAVE_PROT_PUTC_1 c;
-   HAVE_PROT_PUTC_2 stream;
+sys_putc(HAVE_PROT_PUTC_1 c, HAVE_PROT_PUTC_2 stream)
 {
    const int d = fileno(stream);
    HAVE_PROT_PUTC_0 rc;
@@ -1415,9 +1339,7 @@
 }
 
 HAVE_PROT_FPUTC_0
-sys_fputc(c, stream)
-   HAVE_PROT_FPUTC_1 c;
-   HAVE_PROT_FPUTC_2 stream;
+sys_fputc(HAVE_PROT_FPUTC_1 c, HAVE_PROT_FPUTC_2 stream)
 {
    const int d = fileno(stream);
    HAVE_PROT_FPUTC_0 rc;
@@ -1438,8 +1360,7 @@
 }
 
 HAVE_PROT_PUTS_0
-sys_puts(buf)
-   HAVE_PROT_PUTS_1 buf;
+sys_puts(HAVE_PROT_PUTS_1 buf)
 {
    const int d = fileno(stdout);
    HAVE_PROT_PUTS_0 rc;
@@ -1459,9 +1380,7 @@
 }
 
 HAVE_PROT_FPUTS_0
-sys_fputs(buf, stream)
-   HAVE_PROT_FPUTS_1 buf;
-   HAVE_PROT_FPUTS_2 stream;
+sys_fputs(HAVE_PROT_FPUTS_1 buf, HAVE_PROT_FPUTS_2 stream)
 {
    const int d = fileno(stream);
    HAVE_PROT_FPUTS_0 rc;
@@ -1482,8 +1401,7 @@
 }
 
 HAVE_PROT_FFLUSH_0
-sys_fflush(stream)
-   HAVE_PROT_FFLUSH_1 stream;
+sys_fflush(HAVE_PROT_FFLUSH_1 stream)
 {
    HAVE_PROT_FFLUSH_0 rc;
    typedef HAVE_PROT_FFLUSH_0 (*FFLUSH_FUNC_T)(HAVE_PROT_FFLUSH_1);
@@ -1502,8 +1420,7 @@
 }
 
 HAVE_PROT_FCLOSE_0
-sys_fclose(stream)
-   HAVE_PROT_FCLOSE_1 stream;
+sys_fclose(HAVE_PROT_FCLOSE_1 stream)
 {
    const int d = fileno(stream);
    HAVE_PROT_FCLOSE_0 rc;
@@ -1535,9 +1452,7 @@
 }
 
 HAVE_PROT_VPRINTF_0
-sys_vprintf(format, ap)
-   HAVE_PROT_VPRINTF_1 format;
-   HAVE_PROT_VPRINTF_2 ap;
+sys_vprintf(HAVE_PROT_VPRINTF_1 format, HAVE_PROT_VPRINTF_2 ap)
 {
    const int d = fileno(stdout);
    HAVE_PROT_VFPRINTF_0 rc;
@@ -1570,10 +1485,7 @@
 }
 
 HAVE_PROT_VFPRINTF_0
-sys_vfprintf(stream, format, ap)
-   HAVE_PROT_VFPRINTF_1 stream;
-   HAVE_PROT_VFPRINTF_2 format;
-   HAVE_PROT_VFPRINTF_3 ap;
+sys_vfprintf(HAVE_PROT_VFPRINTF_1 stream, HAVE_PROT_VFPRINTF_2 format, HAVE_PROT_VFPRINTF_3 ap)
 {
    HAVE_PROT_VFPRINTF_0 rc;
    int d = fileno(stream);
@@ -1595,11 +1507,7 @@
 }
 
 HAVE_PROT_FWRITE_0
-sys_fwrite(ptr, size, nmb, stream)
-   HAVE_PROT_FWRITE_1 ptr;
-   HAVE_PROT_FWRITE_2 size;
-   HAVE_PROT_FWRITE_3 nmb;
-   HAVE_PROT_FWRITE_4 stream;
+sys_fwrite(HAVE_PROT_FWRITE_1 ptr, HAVE_PROT_FWRITE_2 size, HAVE_PROT_FWRITE_3 nmb, HAVE_PROT_FWRITE_4 stream)
 {
    const int d = fileno(stream);
    HAVE_PROT_FWRITE_0 rc;
@@ -1622,11 +1530,7 @@
 }
 
 HAVE_PROT_FREAD_0
-sys_fread(ptr, size, nmb, stream)
-   HAVE_PROT_FREAD_1 ptr;
-   HAVE_PROT_FREAD_2 size;
-   HAVE_PROT_FREAD_3 nmb;
-   HAVE_PROT_FREAD_4 stream;
+sys_fread(HAVE_PROT_FREAD_1 ptr, HAVE_PROT_FREAD_2 size, HAVE_PROT_FREAD_3 nmb, HAVE_PROT_FREAD_4 stream)
 {
    const int d = fileno(stream);
    HAVE_PROT_FREAD_0 rc;
@@ -1654,11 +1558,8 @@
     */
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_ACCEPT_0
-accept(s, addr, addrlen)
-   HAVE_PROT_ACCEPT_1 s;
-   HAVE_PROT_ACCEPT_2 addr;
-   HAVE_PROT_ACCEPT_3 addrlen;
+int
+accept(int s, struct sockaddr * addr, socklen_t * addrlen)
 {
    if (socks_issyscall(s, SYMBOL_ACCEPT))
       return sys_accept(s, addr, addrlen);
@@ -1667,11 +1568,8 @@
 #endif /* HAVE_EXTRA_OSF_SYMBOLS */
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_BIND_0
-bind(s, name, namelen)
-   HAVE_PROT_BIND_1 s;
-   HAVE_PROT_BIND_2 name;
-   HAVE_PROT_BIND_3 namelen;
+int
+bind(int s, const struct sockaddr * name, socklen_t namelen)
 {
    if (socks_issyscall(s, SYMBOL_BIND))
       return sys_bind(s, name, namelen);
@@ -1690,11 +1588,8 @@
 }
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_CONNECT_0
-connect(s, name, namelen)
-   HAVE_PROT_CONNECT_1 s;
-   HAVE_PROT_CONNECT_2 name;
-   HAVE_PROT_CONNECT_3 namelen;
+int
+connect(int s, const struct sockaddr * name, socklen_t namelen)
 {
    if (socks_issyscall(s, SYMBOL_CONNECT))
       return sys_connect(s, name, namelen);
@@ -1703,11 +1598,8 @@
 #endif /* HAVE_EXTRA_OSF_SYMBOLS */
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_GETPEERNAME_0
-getpeername(s, name, namelen)
-   HAVE_PROT_GETPEERNAME_1 s;
-   HAVE_PROT_GETPEERNAME_2 name;
-   HAVE_PROT_GETPEERNAME_3 namelen;
+int
+getpeername(int s, struct sockaddr * name, socklen_t * namelen)
 {
    if (socks_issyscall(s, SYMBOL_GETPEERNAME))
       return sys_getpeername(s, name, namelen);
@@ -1716,11 +1608,8 @@
 #endif /* HAVE_EXTRA_OSF_SYMBOLS */
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_GETSOCKNAME_0
-getsockname(s, name, namelen)
-   HAVE_PROT_GETSOCKNAME_1 s;
-   HAVE_PROT_GETSOCKNAME_2 name;
-   HAVE_PROT_GETSOCKNAME_3 namelen;
+int
+getsockname(int s, struct sockaddr * name, socklen_t * namelen)
 {
    if (socks_issyscall(s, SYMBOL_GETSOCKNAME))
       return sys_getsockname(s, name, namelen);
@@ -1729,12 +1618,7 @@
 #endif /* HAVE_EXTRA_OSF_SYMBOLS */
 
 HAVE_PROT_GETSOCKOPT_0
-getsockopt(s, level, optname, optval, optlen)
-   HAVE_PROT_GETSOCKOPT_1 s;
-   HAVE_PROT_GETSOCKOPT_2 level;
-   HAVE_PROT_GETSOCKOPT_3 optname;
-   HAVE_PROT_GETSOCKOPT_4 optval;
-   HAVE_PROT_GETSOCKOPT_5 optlen;
+getsockopt(HAVE_PROT_GETSOCKOPT_1 s, HAVE_PROT_GETSOCKOPT_2 level, HAVE_PROT_GETSOCKOPT_3 optname, HAVE_PROT_GETSOCKOPT_4 optval, HAVE_PROT_GETSOCKOPT_5 optlen)
 {
    if (socks_issyscall(s, SYMBOL_GETSOCKNAME))
       return sys_getsockopt(s, level, optname, optval, optlen);
@@ -1742,9 +1626,7 @@
 }
 
 HAVE_PROT_LISTEN_0
-listen(s, backlog)
-   HAVE_PROT_LISTEN_1 s;
-   HAVE_PROT_LISTEN_2 backlog;
+listen(HAVE_PROT_LISTEN_1 s, HAVE_PROT_LISTEN_2 backlog)
 {
    if (socks_issyscall(s, SYMBOL_LISTEN))
       return sys_listen(s, backlog);
@@ -1752,10 +1634,7 @@
 }
 
 HAVE_PROT_READ_0
-read(d, buf, nbytes)
-   HAVE_PROT_READ_1 d;
-   HAVE_PROT_READ_2 buf;
-   HAVE_PROT_READ_3 nbytes;
+read(HAVE_PROT_READ_1 d, HAVE_PROT_READ_2 buf, HAVE_PROT_READ_3 nbytes)
 {
    if (socks_issyscall(d, SYMBOL_READ))
       return sys_read(d, buf, nbytes);
@@ -1764,10 +1643,7 @@
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
 HAVE_PROT_READV_0
-readv(d, iov, iovcnt)
-   HAVE_PROT_READV_1 d;
-   HAVE_PROT_READV_2 iov;
-   HAVE_PROT_READV_3 iovcnt;
+readv(HAVE_PROT_READV_1 d, HAVE_PROT_READV_2 iov, HAVE_PROT_READV_3 iovcnt)
 {
    if (socks_issyscall(d, SYMBOL_READV))
       return sys_readv(d, iov, iovcnt);
@@ -1776,11 +1652,7 @@
 #endif /* HAVE_EXTRA_OSF_SYMBOLS */
 
 HAVE_PROT_RECV_0
-recv(s, msg, len, flags)
-   HAVE_PROT_RECV_1 s;
-   HAVE_PROT_RECV_2 msg;
-   HAVE_PROT_RECV_3 len;
-   HAVE_PROT_RECV_4 flags;
+recv(HAVE_PROT_RECV_1 s, HAVE_PROT_RECV_2 msg, HAVE_PROT_RECV_3 len, HAVE_PROT_RECV_4 flags)
 {
    if (socks_issyscall(s, SYMBOL_RECV))
       return sys_recv(s, msg, len, flags);
@@ -1788,14 +1660,8 @@
 }
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_RECVFROM_0
-recvfrom(s, buf, len, flags, from, fromlen)
-   HAVE_PROT_RECVFROM_1 s;
-   HAVE_PROT_RECVFROM_2 buf;
-   HAVE_PROT_RECVFROM_3 len;
-   HAVE_PROT_RECVFROM_4 flags;
-   HAVE_PROT_RECVFROM_5 from;
-   HAVE_PROT_RECVFROM_6 fromlen;
+ssize_t
+recvfrom(int s, void * buf, size_t len, int flags, struct sockaddr * from, socklen_t * fromlen)
 {
    if (socks_issyscall(s, SYMBOL_RECVFROM))
       return sys_recvfrom(s, buf, len, flags, from, fromlen);
@@ -1805,10 +1671,7 @@
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
 HAVE_PROT_RECVMSG_0
-recvmsg(s, msg, flags)
-   HAVE_PROT_RECVMSG_1 s;
-   HAVE_PROT_RECVMSG_2 msg;
-   HAVE_PROT_RECVMSG_3 flags;
+recvmsg(HAVE_PROT_RECVMSG_1 s, HAVE_PROT_RECVMSG_2 msg, HAVE_PROT_RECVMSG_3 flags)
 {
    if (socks_issyscall(s, SYMBOL_RECVMSG))
       return sys_recvmsg(s, msg, flags);
@@ -1826,10 +1689,7 @@
 #endif /* HAVE_RRESVPORT */
 
 HAVE_PROT_WRITE_0
-write(d, buf, nbytes)
-   HAVE_PROT_WRITE_1 d;
-   HAVE_PROT_WRITE_2 buf;
-   HAVE_PROT_WRITE_3 nbytes;
+write(HAVE_PROT_WRITE_1 d, HAVE_PROT_WRITE_2 buf, HAVE_PROT_WRITE_3 nbytes)
 {
    if (socks_issyscall(d, SYMBOL_WRITE))
       return sys_write(d, buf, nbytes);
@@ -1838,10 +1698,7 @@
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
 HAVE_PROT_WRITEV_0
-writev(d, iov, iovcnt)
-   HAVE_PROT_WRITEV_1 d;
-   HAVE_PROT_WRITEV_2 iov;
-   HAVE_PROT_WRITEV_3 iovcnt;
+writev(HAVE_PROT_WRITEV_1 d, HAVE_PROT_WRITEV_2 iov, HAVE_PROT_WRITEV_3 iovcnt)
 {
    if (socks_issyscall(d, SYMBOL_WRITEV))
       return sys_writev(d, iov, iovcnt);
@@ -1850,11 +1707,7 @@
 #endif /* HAVE_EXTRA_OSF_SYMBOLS */
 
 HAVE_PROT_SEND_0
-send(s, msg, len, flags)
-   HAVE_PROT_SEND_1 s;
-   HAVE_PROT_SEND_2 msg;
-   HAVE_PROT_SEND_3 len;
-   HAVE_PROT_SEND_4 flags;
+send(HAVE_PROT_SEND_1 s, HAVE_PROT_SEND_2 msg, HAVE_PROT_SEND_3 len, HAVE_PROT_SEND_4 flags)
 {
    if (socks_issyscall(s, SYMBOL_SEND))
       return sys_send(s, msg, len, flags);
@@ -1863,10 +1716,7 @@
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
 HAVE_PROT_SENDMSG_0
-sendmsg(s, msg, flags)
-   HAVE_PROT_SENDMSG_1 s;
-   HAVE_PROT_SENDMSG_2 msg;
-   HAVE_PROT_SENDMSG_3 flags;
+sendmsg(HAVE_PROT_SENDMSG_1 s, HAVE_PROT_SENDMSG_2 msg, HAVE_PROT_SENDMSG_3 flags)
 {
    if (socks_issyscall(s, SYMBOL_SENDMSG))
       return sys_sendmsg(s, msg, flags);
@@ -1875,14 +1725,8 @@
 #endif /* HAVE_EXTRA_OSF_SYMBOLS */
 
 #if !HAVE_EXTRA_OSF_SYMBOLS
-HAVE_PROT_SENDTO_0
-sendto(s, msg, len, flags, to, tolen)
-   HAVE_PROT_SENDTO_1 s;
-   HAVE_PROT_SENDTO_2 msg;
-   HAVE_PROT_SENDTO_3 len;
-   HAVE_PROT_SENDTO_4 flags;
-   HAVE_PROT_SENDTO_5 to;
-   HAVE_PROT_SENDTO_6 tolen;
+ssize_t
+sendto(int s, const void * msg, size_t len, int flags, const struct sockaddr * to, socklen_t tolen)
 {
    if (socks_issyscall(s, SYMBOL_SENDTO))
       return sys_sendto(s, msg, len, flags, to, tolen);
@@ -1893,16 +1737,13 @@
 #ifdef __sun
 /* __xnet_foo variants of some functions exist on Solaris if _XPG4_2 is set */
 
-HAVE_PROT_BIND_0
-sys_xnet_bind(s, name, namelen)
-   HAVE_PROT_BIND_1 s;
-   HAVE_PROT_BIND_2 name;
-   HAVE_PROT_BIND_3 namelen;
+int
+sys_xnet_bind(int s, const struct sockaddr * name, socklen_t namelen)
 {
    int rc;
-   typedef HAVE_PROT_BIND_0 (*BIND_FUNC_T)(HAVE_PROT_BIND_1,
-                                           HAVE_PROT_BIND_2,
-                                           HAVE_PROT_BIND_3);
+   typedef int (*BIND_FUNC_T)(int,
+                                           const struct sockaddr *,
+                                           socklen_t);
    BIND_FUNC_T function = (BIND_FUNC_T)symbolfunction(SYMBOL_XNET_BIND);
    const int tagged = (doing_addrinit ? 0 : 1);
 
@@ -1917,27 +1758,21 @@
    return rc;
 }
 
-HAVE_PROT_BIND_0
-__xnet_bind(s, name, namelen)
-   HAVE_PROT_BIND_1 s;
-   HAVE_PROT_BIND_2 name;
-   HAVE_PROT_BIND_3 namelen;
+int
+__xnet_bind(int s, const struct sockaddr * name, socklen_t namelen)
 {
    if (socks_issyscall(s, SYMBOL_XNET_BIND))
       return sys_xnet_bind(s, name, namelen);
    return Rbind(s, name, namelen);
 }
 
-HAVE_PROT_CONNECT_0
-sys_xnet_connect(s, name, namelen)
-   HAVE_PROT_CONNECT_1 s;
-   HAVE_PROT_CONNECT_2 name;
-   HAVE_PROT_CONNECT_3 namelen;
+int
+sys_xnet_connect(int s, const struct sockaddr * name, socklen_t namelen)
 {
    int rc;
-   typedef HAVE_PROT_CONNECT_0 (*CONNECT_FUNC_T)(HAVE_PROT_CONNECT_1,
-                                                 HAVE_PROT_CONNECT_2,
-                                                 HAVE_PROT_CONNECT_3);
+   typedef int (*CONNECT_FUNC_T)(int,
+                                                 const struct sockaddr *,
+                                                 socklen_t);
    CONNECT_FUNC_T function
    = (CONNECT_FUNC_T)symbolfunction(SYMBOL_XNET_CONNECT);
    const int tagged = (doing_addrinit ? 0 : 1);
@@ -1953,11 +1788,8 @@
    return rc;
 }
 
-HAVE_PROT_CONNECT_0
-__xnet_connect(s, name, namelen)
-   HAVE_PROT_CONNECT_1 s;
-   HAVE_PROT_CONNECT_2 name;
-   HAVE_PROT_CONNECT_3 namelen;
+int
+__xnet_connect(int s, const struct sockaddr * name, socklen_t namelen)
 {
    if (socks_issyscall(s, SYMBOL_XNET_CONNECT))
       return sys_xnet_connect(s, name, namelen);
@@ -1965,9 +1797,7 @@
 }
 
 HAVE_PROT_LISTEN_0
-sys_xnet_listen(s, backlog)
-   HAVE_PROT_LISTEN_1 s;
-   HAVE_PROT_LISTEN_2 backlog;
+sys_xnet_listen(HAVE_PROT_LISTEN_1 s, HAVE_PROT_LISTEN_2 backlog)
 {
    HAVE_PROT_LISTEN_0 rc;
    typedef HAVE_PROT_LISTEN_0 (*LISTEN_FUNC_T)(HAVE_PROT_LISTEN_1,
@@ -1987,9 +1817,7 @@
 }
 
 HAVE_PROT_LISTEN_0
-__xnet_listen(s, backlog)
-   HAVE_PROT_LISTEN_1 s;
-   HAVE_PROT_LISTEN_2 backlog;
+__xnet_listen(HAVE_PROT_LISTEN_1 s, HAVE_PROT_LISTEN_2 backlog)
 {
    if (socks_issyscall(s, SYMBOL_XNET_LISTEN))
       return sys_xnet_listen(s, backlog);
@@ -1997,10 +1825,7 @@
 }
 
 HAVE_PROT_RECVMSG_0
-sys_xnet_recvmsg(s, msg, flags)
-   HAVE_PROT_RECVMSG_1 s;
-   HAVE_PROT_RECVMSG_2 msg;
-   HAVE_PROT_RECVMSG_3 flags;
+sys_xnet_recvmsg(HAVE_PROT_RECVMSG_1 s, HAVE_PROT_RECVMSG_2 msg, HAVE_PROT_RECVMSG_3 flags)
 {
    ssize_t rc;
    typedef HAVE_PROT_RECVMSG_0 (*RECVMSG_FUNC_T)(HAVE_PROT_RECVMSG_1,
@@ -2022,10 +1847,7 @@
 }
 
 HAVE_PROT_RECVMSG_0
-__xnet_recvmsg(s, msg, flags)
-   HAVE_PROT_RECVMSG_1 s;
-   HAVE_PROT_RECVMSG_2 msg;
-   HAVE_PROT_RECVMSG_3 flags;
+__xnet_recvmsg(HAVE_PROT_RECVMSG_1 s, HAVE_PROT_RECVMSG_2 msg, HAVE_PROT_RECVMSG_3 flags)
 {
    if (socks_issyscall(s, SYMBOL_XNET_RECVMSG))
       return sys_xnet_recvmsg(s, msg, flags);
@@ -2033,10 +1855,7 @@
 }
 
 HAVE_PROT_SENDMSG_0
-sys_xnet_sendmsg(s, msg, flags)
-   HAVE_PROT_SENDMSG_1 s;
-   HAVE_PROT_SENDMSG_2 msg;
-   HAVE_PROT_SENDMSG_3 flags;
+sys_xnet_sendmsg(HAVE_PROT_SENDMSG_1 s, HAVE_PROT_SENDMSG_2 msg, HAVE_PROT_SENDMSG_3 flags)
 {
    ssize_t rc;
    typedef HAVE_PROT_SENDMSG_0 (*SENDMSG_FUNC_T)(HAVE_PROT_SENDMSG_1,
@@ -2058,32 +1877,23 @@
 }
 
 HAVE_PROT_SENDMSG_0
-__xnet_sendmsg(s, msg, flags)
-   HAVE_PROT_SENDMSG_1 s;
-   HAVE_PROT_SENDMSG_2 msg;
-   HAVE_PROT_SENDMSG_3 flags;
+__xnet_sendmsg(HAVE_PROT_SENDMSG_1 s, HAVE_PROT_SENDMSG_2 msg, HAVE_PROT_SENDMSG_3 flags)
 {
    if (socks_issyscall(s, SYMBOL_XNET_SENDMSG))
       return sys_xnet_sendmsg(s, msg, flags);
    return Rsendmsg(s, msg, flags);
 }
 
-HAVE_PROT_SENDTO_0
-sys_xnet_sendto(s, msg, len, flags, to, tolen)
-   HAVE_PROT_SENDTO_1 s;
-   HAVE_PROT_SENDTO_2 msg;
-   HAVE_PROT_SENDTO_3 len;
-   HAVE_PROT_SENDTO_4 flags;
-   HAVE_PROT_SENDTO_5 to;
-   HAVE_PROT_SENDTO_6 tolen;
+ssize_t
+sys_xnet_sendto(int s, const void * msg, size_t len, int flags, const struct sockaddr * to, socklen_t tolen)
 {
    ssize_t rc;
-   typedef HAVE_PROT_SENDTO_0 (*SENDTO_FUNC_T)(HAVE_PROT_SENDTO_1,
-                                               HAVE_PROT_SENDTO_2,
-                                               HAVE_PROT_SENDTO_3,
-                                               HAVE_PROT_SENDTO_4,
-                                               HAVE_PROT_SENDTO_5,
-                                               HAVE_PROT_SENDTO_6);
+   typedef ssize_t (*SENDTO_FUNC_T)(int,
+                                               const void *,
+                                               size_t,
+                                               int,
+                                               const struct sockaddr *,
+                                               socklen_t);
    SENDTO_FUNC_T function = (SENDTO_FUNC_T)symbolfunction(SYMBOL_XNET_SENDTO);
    const int tagged = (doing_addrinit ? 0 : 1);
 
@@ -2098,14 +1908,8 @@
    return rc;
 }
 
-HAVE_PROT_SENDTO_0
-__xnet_sendto(s, msg, len, flags, to, tolen)
-   HAVE_PROT_SENDTO_1 s;
-   HAVE_PROT_SENDTO_2 msg;
-   HAVE_PROT_SENDTO_3 len;
-   HAVE_PROT_SENDTO_4 flags;
-   HAVE_PROT_SENDTO_5 to;
-   HAVE_PROT_SENDTO_6 tolen;
+ssize_t
+__xnet_sendto(int s, const void * msg, size_t len, int flags, const struct sockaddr * to, socklen_t tolen)
 {
    if (socks_issyscall(s, SYMBOL_XNET_SENDTO))
       return sys_xnet_sendto(s, msg, len, flags, to, tolen);
@@ -2115,55 +1919,40 @@
 #endif /* __sun */
 
 #ifdef __FreeBSD__
-HAVE_PROT_ACCEPT_0
-_accept(s, addr, addrlen)
-   HAVE_PROT_ACCEPT_1 s;
-   HAVE_PROT_ACCEPT_2 addr;
-   HAVE_PROT_ACCEPT_3 addrlen;
+int
+_accept(int s, struct sockaddr * addr, socklen_t * addrlen)
 {
    if (socks_issyscall(s, SYMBOL_ACCEPT))
       return sys_accept(s, addr, addrlen);
    return Raccept(s, addr, (socklen_t *)addrlen);
 }
 
-HAVE_PROT_BIND_0
-_bind(s, name, namelen)
-   HAVE_PROT_BIND_1 s;
-   HAVE_PROT_BIND_2 name;
-   HAVE_PROT_BIND_3 namelen;
+int
+_bind(int s, const struct sockaddr * name, socklen_t namelen)
 {
    if (socks_issyscall(s, SYMBOL_BIND))
       return sys_bind(s, name, namelen);
    return Rbind(s, name, namelen);
 }
 
-HAVE_PROT_CONNECT_0
-_connect(s, name, namelen)
-   HAVE_PROT_CONNECT_1 s;
-   HAVE_PROT_CONNECT_2 name;
-   HAVE_PROT_CONNECT_3 namelen;
+int
+_connect(int s, const struct sockaddr * name, socklen_t namelen)
 {
    if (socks_issyscall(s, SYMBOL_CONNECT))
       return sys_connect(s, name, namelen);
    return Rconnect(s, name, namelen);
 }
 
-HAVE_PROT_GETPEERNAME_0
-_getpeername(s, name, namelen)
-   HAVE_PROT_GETPEERNAME_1 s;
-   HAVE_PROT_GETPEERNAME_2 name;
-   HAVE_PROT_GETPEERNAME_3 namelen;
+int
+_getpeername(int s, struct sockaddr * name, socklen_t * namelen)
 {
    if (socks_issyscall(s, SYMBOL_GETPEERNAME))
       return sys_getpeername(s, name, namelen);
    return Rgetpeername(s, name, namelen);
 }
 
-HAVE_PROT_GETSOCKNAME_0
-_getsockname(s, name, namelen)
-   HAVE_PROT_GETSOCKNAME_1 s;
-   HAVE_PROT_GETSOCKNAME_2 name;
-   HAVE_PROT_GETSOCKNAME_3 namelen;
+int
+_getsockname(int s, struct sockaddr * name, socklen_t * namelen)
 {
    if (socks_issyscall(s, SYMBOL_GETSOCKNAME))
       return sys_getsockname(s, name, namelen);
@@ -2171,9 +1960,7 @@
 }
 
 HAVE_PROT_LISTEN_0
-_listen(s, backlog)
-   HAVE_PROT_LISTEN_1 s;
-   HAVE_PROT_LISTEN_2 backlog;
+_listen(HAVE_PROT_LISTEN_1 s, HAVE_PROT_LISTEN_2 backlog)
 {
    if (socks_issyscall(s, SYMBOL_LISTEN))
       return sys_listen(s, backlog);
@@ -2181,10 +1968,7 @@
 }
 
 HAVE_PROT_READ_0
-_read(d, buf, nbytes)
-   HAVE_PROT_READ_1 d;
-   HAVE_PROT_READ_2 buf;
-   HAVE_PROT_READ_3 nbytes;
+_read(HAVE_PROT_READ_1 d, HAVE_PROT_READ_2 buf, HAVE_PROT_READ_3 nbytes)
 {
    if (socks_issyscall(d, SYMBOL_READ))
       return sys_read(d, buf, nbytes);
@@ -2192,10 +1976,7 @@
 }
 
 HAVE_PROT_READV_0
-_readv(d, iov, iovcnt)
-   HAVE_PROT_READV_1 d;
-   HAVE_PROT_READV_2 iov;
-   HAVE_PROT_READV_3 iovcnt;
+_readv(HAVE_PROT_READV_1 d, HAVE_PROT_READV_2 iov, HAVE_PROT_READV_3 iovcnt)
 {
    if (socks_issyscall(d, SYMBOL_READV))
       return sys_readv(d, iov, iovcnt);
@@ -2203,25 +1984,15 @@
 }
 
 HAVE_PROT_RECV_0
-_recv(s, msg, len, flags)
-   HAVE_PROT_RECV_1 s;
-   HAVE_PROT_RECV_2 msg;
-   HAVE_PROT_RECV_3 len;
-   HAVE_PROT_RECV_4 flags;
+_recv(HAVE_PROT_RECV_1 s, HAVE_PROT_RECV_2 msg, HAVE_PROT_RECV_3 len, HAVE_PROT_RECV_4 flags)
 {
    if (socks_issyscall(s, SYMBOL_RECV))
       return sys_recv(s, msg, len, flags);
    return Rrecv(s, msg, len, flags);
 }
 
-HAVE_PROT_RECVFROM_0
-_recvfrom(s, buf, len, flags, from, fromlen)
-   HAVE_PROT_RECVFROM_1 s;
-   HAVE_PROT_RECVFROM_2 buf;
-   HAVE_PROT_RECVFROM_3 len;
-   HAVE_PROT_RECVFROM_4 flags;
-   HAVE_PROT_RECVFROM_5 from;
-   HAVE_PROT_RECVFROM_6 fromlen;
+ssize_t
+_recvfrom(int s, void * buf, size_t len, int flags, struct sockaddr * from, socklen_t * fromlen)
 {
    if (socks_issyscall(s, SYMBOL_RECVFROM))
       return sys_recvfrom(s, buf, len, flags, from, fromlen);
@@ -2229,10 +2000,7 @@
 }
 
 HAVE_PROT_RECVMSG_0
-_recvmsg(s, msg, flags)
-   HAVE_PROT_RECVMSG_1 s;
-   HAVE_PROT_RECVMSG_2 msg;
-   HAVE_PROT_RECVMSG_3 flags;
+_recvmsg(HAVE_PROT_RECVMSG_1 s, HAVE_PROT_RECVMSG_2 msg, HAVE_PROT_RECVMSG_3 flags)
 {
    if (socks_issyscall(s, SYMBOL_RECVMSG))
       return sys_recvmsg(s, msg, flags);
@@ -2240,10 +2008,7 @@
 }
 
 HAVE_PROT_WRITE_0
-_write(d, buf, nbytes)
-   HAVE_PROT_WRITE_1 d;
-   HAVE_PROT_WRITE_2 buf;
-   HAVE_PROT_WRITE_3 nbytes;
+_write(HAVE_PROT_WRITE_1 d, HAVE_PROT_WRITE_2 buf, HAVE_PROT_WRITE_3 nbytes)
 {
    if (socks_issyscall(d, SYMBOL_WRITE))
       return sys_write(d, buf, nbytes);
@@ -2251,10 +2016,7 @@
 }
 
 HAVE_PROT_WRITEV_0
-_writev(d, iov, iovcnt)
-   HAVE_PROT_WRITEV_1 d;
-   HAVE_PROT_WRITEV_2 iov;
-   HAVE_PROT_WRITEV_3 iovcnt;
+_writev(HAVE_PROT_WRITEV_1 d, HAVE_PROT_WRITEV_2 iov, HAVE_PROT_WRITEV_3 iovcnt)
 {
    if (socks_issyscall(d, SYMBOL_WRITEV))
       return sys_writev(d, iov, iovcnt);
@@ -2262,11 +2024,7 @@
 }
 
 HAVE_PROT_SEND_0
-_send(s, msg, len, flags)
-   HAVE_PROT_SEND_1 s;
-   HAVE_PROT_SEND_2 msg;
-   HAVE_PROT_SEND_3 len;
-   HAVE_PROT_SEND_4 flags;
+_send(HAVE_PROT_SEND_1 s, HAVE_PROT_SEND_2 msg, HAVE_PROT_SEND_3 len, HAVE_PROT_SEND_4 flags)
 {
    if (socks_issyscall(s, SYMBOL_SEND))
       return sys_send(s, msg, len, flags);
@@ -2274,24 +2032,15 @@
 }
 
 HAVE_PROT_SENDMSG_0
-_sendmsg(s, msg, flags)
-   HAVE_PROT_SENDMSG_1 s;
-   HAVE_PROT_SENDMSG_2 msg;
-   HAVE_PROT_SENDMSG_3 flags;
+_sendmsg(HAVE_PROT_SENDMSG_1 s, HAVE_PROT_SENDMSG_2 msg, HAVE_PROT_SENDMSG_3 flags)
 {
    if (socks_issyscall(s, SYMBOL_SENDMSG))
       return sys_sendmsg(s, msg, flags);
    return Rsendmsg(s, msg, flags);
 }
 
-HAVE_PROT_SENDTO_0
-_sendto(s, msg, len, flags, to, tolen)
-   HAVE_PROT_SENDTO_1 s;
-   HAVE_PROT_SENDTO_2 msg;
-   HAVE_PROT_SENDTO_3 len;
-   HAVE_PROT_SENDTO_4 flags;
-   HAVE_PROT_SENDTO_5 to;
-   HAVE_PROT_SENDTO_6 tolen;
+ssize_t
+_sendto(int s, const void * msg, size_t len, int flags, const struct sockaddr * to, socklen_t tolen)
 {
    if (socks_issyscall(s, SYMBOL_SENDTO))
       return sys_sendto(s, msg, len, flags, to, tolen);
@@ -2321,10 +2070,7 @@
 }
 
 HAVE_PROT_GETHOSTBYADDR_0
-gethostbyaddr(addr, len, af)
-   HAVE_PROT_GETHOSTBYADDR_1 addr;
-   HAVE_PROT_GETHOSTBYADDR_2 len;
-   HAVE_PROT_GETHOSTBYADDR_3 af;
+gethostbyaddr(HAVE_PROT_GETHOSTBYADDR_1 addr, HAVE_PROT_GETHOSTBYADDR_2 len, HAVE_PROT_GETHOSTBYADDR_3 af)
 {
 
    if (socks_shouldcallasnative(SYMBOL_GETHOSTBYADDR))
@@ -2421,14 +2167,7 @@
 #if HAVE_GETNAMEINFO
 
 HAVE_PROT_GETNAMEINFO_0
-sys_getnameinfo(sa, salen, host, hostlen, serv, servlen, flags)
-   HAVE_PROT_GETNAMEINFO_1 sa;
-   HAVE_PROT_GETNAMEINFO_2 salen;
-   HAVE_PROT_GETNAMEINFO_3 host;
-   HAVE_PROT_GETNAMEINFO_4 hostlen;
-   HAVE_PROT_GETNAMEINFO_5 serv;
-   HAVE_PROT_GETNAMEINFO_6 servlen;
-   HAVE_PROT_GETNAMEINFO_7 flags;
+sys_getnameinfo(HAVE_PROT_GETNAMEINFO_1 sa, HAVE_PROT_GETNAMEINFO_2 salen, HAVE_PROT_GETNAMEINFO_3 host, HAVE_PROT_GETNAMEINFO_4 hostlen, HAVE_PROT_GETNAMEINFO_5 serv, HAVE_PROT_GETNAMEINFO_6 servlen, HAVE_PROT_GETNAMEINFO_7 flags)
 {
    typedef HAVE_PROT_GETNAMEINFO_0 (*GETNAMEINFO_FUNC_T)(
                                     HAVE_PROT_GETNAMEINFO_1,
@@ -2563,8 +2302,7 @@
 #endif /* getc */
 
 HAVE_PROT_GETC_0
-getc(stream)
-   HAVE_PROT_GETC_1 stream;
+getc(HAVE_PROT_GETC_1 stream)
 {
    const int d = fileno(stream);
 
@@ -2575,8 +2313,7 @@
 
 #if HAVE__IO_GETC
 HAVE_PROT__IO_GETC_0
-_IO_getc(stream)
-   HAVE_PROT__IO_GETC_1 stream;
+_IO_getc(HAVE_PROT__IO_GETC_1 stream)
 {
    const int d = fileno(stream);
 
@@ -2587,8 +2324,7 @@
 #endif /* HAVE__IO_GETC */
 
 HAVE_PROT_FGETC_0
-fgetc(stream)
-   HAVE_PROT_FGETC_1 stream;
+fgetc(HAVE_PROT_FGETC_1 stream)
 {
    const int d = fileno(stream);
 
@@ -2598,8 +2334,7 @@
 }
 
 HAVE_PROT_GETS_0
-gets(buf)
-   HAVE_PROT_GETS_1 buf;
+gets(HAVE_PROT_GETS_1 buf)
 {
    const int d = fileno(stdin);
 
@@ -2609,10 +2344,7 @@
 }
 
 HAVE_PROT_FGETS_0
-fgets(buf, size, stream)
-   HAVE_PROT_FGETS_1 buf;
-   HAVE_PROT_FGETS_2 size;
-   HAVE_PROT_FGETS_3 stream;
+fgets(HAVE_PROT_FGETS_1 buf, HAVE_PROT_FGETS_2 size, HAVE_PROT_FGETS_3 stream)
 {
    const int d = fileno(stream);
 
@@ -2622,9 +2354,7 @@
 }
 
 HAVE_PROT_PUTC_0
-putc(c, stream)
-   HAVE_PROT_PUTC_1 c;
-   HAVE_PROT_PUTC_2 stream;
+putc(HAVE_PROT_PUTC_1 c, HAVE_PROT_PUTC_2 stream)
 {
    const int d = fileno(stream);
 
@@ -2635,9 +2365,7 @@
 
 #if HAVE__IO_PUTC
 HAVE_PROT__IO_PUTC_0
-_IO_putc(c, stream)
-   HAVE_PROT__IO_PUTC_1 c;
-   HAVE_PROT__IO_PUTC_2 stream;
+_IO_putc(HAVE_PROT__IO_PUTC_1 c, HAVE_PROT__IO_PUTC_2 stream)
 {
    const int d = fileno(stream);
 
@@ -2648,9 +2376,7 @@
 #endif /* HAVE__IO_PUTC */
 
 HAVE_PROT_FPUTC_0
-fputc(c, stream)
-   HAVE_PROT_FPUTC_1 c;
-   HAVE_PROT_FPUTC_2 stream;
+fputc(HAVE_PROT_FPUTC_1 c, HAVE_PROT_FPUTC_2 stream)
 {
    const int d = fileno(stream);
 
@@ -2660,8 +2386,7 @@
 }
 
 HAVE_PROT_PUTS_0
-puts(buf)
-   HAVE_PROT_PUTS_1 buf;
+puts(HAVE_PROT_PUTS_1 buf)
 {
    const int d = fileno(stdout);
 
@@ -2671,9 +2396,7 @@
 }
 
 HAVE_PROT_FPUTS_0
-fputs(buf, stream)
-   HAVE_PROT_FPUTS_1 buf;
-   HAVE_PROT_FPUTS_2 stream;
+fputs(HAVE_PROT_FPUTS_1 buf, HAVE_PROT_FPUTS_2 stream)
 {
    const int d = fileno(stream);
 
@@ -2683,8 +2406,7 @@
 }
 
 HAVE_PROT_FFLUSH_0
-fflush(stream)
-   HAVE_PROT_FFLUSH_1 stream;
+fflush(HAVE_PROT_FFLUSH_1 stream)
 {
 
    if (!sockscf.state.havegssapisockets
@@ -2695,8 +2417,7 @@
 }
 
 HAVE_PROT_FCLOSE_0
-fclose(stream)
-   HAVE_PROT_FCLOSE_1 stream;
+fclose(HAVE_PROT_FCLOSE_1 stream)
 {
    const int d = fileno(stream);
 
@@ -2771,9 +2492,7 @@
 #endif /* HAVE___FPRINTF_CHK */
 
 HAVE_PROT_VPRINTF_0
-vprintf(format, ap)
-   HAVE_PROT_VPRINTF_1 format;
-   HAVE_PROT_VPRINTF_2 ap;
+vprintf(HAVE_PROT_VPRINTF_1 format, HAVE_PROT_VPRINTF_2 ap)
 {
    const int d = fileno(stdout);
 
@@ -2783,10 +2502,7 @@
 }
 
 HAVE_PROT_VFPRINTF_0
-vfprintf(stream, format, ap)
-   HAVE_PROT_VFPRINTF_1 stream;
-   HAVE_PROT_VFPRINTF_2 format;
-   HAVE_PROT_VFPRINTF_3 ap;
+vfprintf(HAVE_PROT_VFPRINTF_1 stream, HAVE_PROT_VFPRINTF_2 format, HAVE_PROT_VFPRINTF_3 ap)
 {
    const int d = fileno(stream);
 
@@ -2797,11 +2513,7 @@
 
 #if HAVE___VFPRINTF_CHK
 HAVE_PROT_VFPRINTF_0
-__vfprintf_chk(stream, dummy, format, ap)
-   HAVE_PROT_VFPRINTF_1 stream;
-   int                  dummy;
-   HAVE_PROT_VFPRINTF_2 format;
-   HAVE_PROT_VFPRINTF_3 ap;
+__vfprintf_chk(HAVE_PROT_VFPRINTF_1 stream, int dummy, HAVE_PROT_VFPRINTF_2 format, HAVE_PROT_VFPRINTF_3 ap)
 {
    const int d = fileno(stream);
 
@@ -2813,11 +2525,7 @@
 #endif /* HAVE___VFPRINTF_CHK */
 
 HAVE_PROT_FWRITE_0
-fwrite(ptr, size, nmb, stream)
-   HAVE_PROT_FWRITE_1 ptr;
-   HAVE_PROT_FWRITE_2 size;
-   HAVE_PROT_FWRITE_3 nmb;
-   HAVE_PROT_FWRITE_4 stream;
+fwrite(HAVE_PROT_FWRITE_1 ptr, HAVE_PROT_FWRITE_2 size, HAVE_PROT_FWRITE_3 nmb, HAVE_PROT_FWRITE_4 stream)
 {
    const int d = fileno(stream);
 
@@ -2827,11 +2535,7 @@
 }
 
 HAVE_PROT_FREAD_0
-fread(ptr, size, nmb, stream)
-   HAVE_PROT_FREAD_1 ptr;
-   HAVE_PROT_FREAD_2 size;
-   HAVE_PROT_FREAD_3 nmb;
-   HAVE_PROT_FREAD_4 stream;
+fread(HAVE_PROT_FREAD_1 ptr, HAVE_PROT_FREAD_2 size, HAVE_PROT_FREAD_3 nmb, HAVE_PROT_FREAD_4 stream)
 {
    const int d = fileno(stream);
 
@@ -2842,11 +2546,7 @@
 
 #if HAVE___READ_CHK
 HAVE_PROT__READ_CHK_0
-__read_chk(d, buf, nbytes, buflen)
-   HAVE_PROT__READ_CHK_1 d;
-   HAVE_PROT__READ_CHK_2 buf;
-   HAVE_PROT__READ_CHK_3 nbytes;
-   HAVE_PROT__READ_CHK_4 buflen;
+__read_chk(HAVE_PROT__READ_CHK_1 d, HAVE_PROT__READ_CHK_2 buf, HAVE_PROT__READ_CHK_3 nbytes, HAVE_PROT__READ_CHK_4 buflen)
 {
    SASSERTX(nbytes <= buflen);
 
@@ -2863,10 +2563,7 @@
 #if HAVE_DARWIN
 
 HAVE_PROT_READ_0
-sys_read_nocancel(d, buf, nbytes)
-   HAVE_PROT_READ_1 d;
-   HAVE_PROT_READ_2 buf;
-   HAVE_PROT_READ_3 nbytes;
+sys_read_nocancel(HAVE_PROT_READ_1 d, HAVE_PROT_READ_2 buf, HAVE_PROT_READ_3 nbytes)
 {
    ssize_t rc;
    typedef HAVE_PROT_READ_0 (*READ_FUNC_T)(HAVE_PROT_READ_1,
@@ -2886,16 +2583,13 @@
    return rc;
 }
 
-HAVE_PROT_CONNECT_0
-sys_connect_nocancel(s, name, namelen)
-   HAVE_PROT_CONNECT_1 s;
-   HAVE_PROT_CONNECT_2 name;
-   HAVE_PROT_CONNECT_3 namelen;
+int
+sys_connect_nocancel(int s, const struct sockaddr * name, socklen_t namelen)
 {
    int rc;
-   typedef HAVE_PROT_CONNECT_0 (*CONNECT_FUNC_T)(HAVE_PROT_CONNECT_1,
-                                                 HAVE_PROT_CONNECT_2,
-                                                 HAVE_PROT_CONNECT_3);
+   typedef int (*CONNECT_FUNC_T)(int,
+                                                 const struct sockaddr *,
+                                                 socklen_t);
    CONNECT_FUNC_T function =
       (CONNECT_FUNC_T)symbolfunction(SYMBOL_CONNECT_NOCANCEL);
    const int tagged = (doing_addrinit ? 0 : 1);
@@ -2911,22 +2605,16 @@
    return rc;
 }
 
-HAVE_PROT_RECVFROM_0
-sys_recvfrom_nocancel(s, buf, len, flags, from, fromlen)
-   HAVE_PROT_RECVFROM_1 s;
-   HAVE_PROT_RECVFROM_2 buf;
-   HAVE_PROT_RECVFROM_3 len;
-   HAVE_PROT_RECVFROM_4 flags;
-   HAVE_PROT_RECVFROM_5 from;
-   HAVE_PROT_RECVFROM_6 fromlen;
+ssize_t
+sys_recvfrom_nocancel(int s, void * buf, size_t len, int flags, struct sockaddr * from, socklen_t * fromlen)
 {
    int rc;
-   typedef HAVE_PROT_RECVFROM_0 (*RECVFROM_FUNC_T)(HAVE_PROT_RECVFROM_1,
-                                                   HAVE_PROT_RECVFROM_2,
-                                                   HAVE_PROT_RECVFROM_3,
-                                                   HAVE_PROT_RECVFROM_4,
-                                                   HAVE_PROT_RECVFROM_5,
-                                                   HAVE_PROT_RECVFROM_6);
+   typedef ssize_t (*RECVFROM_FUNC_T)(int,
+                                                   void *,
+                                                   size_t,
+                                                   int,
+                                                   struct sockaddr *,
+                                                   socklen_t *);
    RECVFROM_FUNC_T function =
       (RECVFROM_FUNC_T)symbolfunction(SYMBOL_RECVFROM_NOCANCEL);
    const int tagged = (doing_addrinit ? 0 : 1);
@@ -2942,22 +2630,16 @@
    return rc;
 }
 
-HAVE_PROT_SENDTO_0
-sys_sendto_nocancel(s, msg, len, flags, to, tolen)
-   HAVE_PROT_SENDTO_1 s;
-   HAVE_PROT_SENDTO_2 msg;
-   HAVE_PROT_SENDTO_3 len;
-   HAVE_PROT_SENDTO_4 flags;
-   HAVE_PROT_SENDTO_5 to;
-   HAVE_PROT_SENDTO_6 tolen;
+ssize_t
+sys_sendto_nocancel(int s, const void * msg, size_t len, int flags, const struct sockaddr * to, socklen_t tolen)
 {
    ssize_t rc;
-   typedef HAVE_PROT_SENDTO_0 (*SENDTO_FUNC_T)(HAVE_PROT_SENDTO_1,
-                                               HAVE_PROT_SENDTO_2,
-                                               HAVE_PROT_SENDTO_3,
-                                               HAVE_PROT_SENDTO_4,
-                                               HAVE_PROT_SENDTO_5,
-                                               HAVE_PROT_SENDTO_6);
+   typedef ssize_t (*SENDTO_FUNC_T)(int,
+                                               const void *,
+                                               size_t,
+                                               int,
+                                               const struct sockaddr *,
+                                               socklen_t);
    SENDTO_FUNC_T function =
       (SENDTO_FUNC_T)symbolfunction(SYMBOL_SENDTO_NOCANCEL);
    const int tagged = (doing_addrinit ? 0 : 1);
@@ -2974,10 +2656,7 @@
 }
 
 HAVE_PROT_WRITE_0
-sys_write_nocancel(s, buf, nbytes)
-   HAVE_PROT_WRITE_1 s;
-   HAVE_PROT_WRITE_2 buf;
-   HAVE_PROT_WRITE_3 nbytes;
+sys_write_nocancel(HAVE_PROT_WRITE_1 s, HAVE_PROT_WRITE_2 buf, HAVE_PROT_WRITE_3 nbytes)
 {
    ssize_t rc;
    typedef HAVE_PROT_WRITE_0 (*WRITE_FUNC_T)(HAVE_PROT_WRITE_1,
@@ -2997,11 +2676,8 @@
    return rc;
 }
 
-HAVE_PROT_CONNECT_0
-connect$NOCANCEL(s, name, namelen)
-   HAVE_PROT_CONNECT_1 s;
-   HAVE_PROT_CONNECT_2 name;
-   HAVE_PROT_CONNECT_3 namelen;
+int
+connect$NOCANCEL(int s, const struct sockaddr * name, socklen_t namelen)
 {
    if (!sockscf.state.havegssapisockets
    || socks_issyscall(s, SYMBOL_CONNECT_NOCANCEL))
@@ -3012,10 +2688,7 @@
 
 #if 1
 HAVE_PROT_READ_0
-read$NOCANCEL(d, buf, nbytes)
-   HAVE_PROT_READ_1 d;
-   HAVE_PROT_READ_2 buf;
-   HAVE_PROT_READ_3 nbytes;
+read$NOCANCEL(HAVE_PROT_READ_1 d, HAVE_PROT_READ_2 buf, HAVE_PROT_READ_3 nbytes)
 {
    if (!sockscf.state.havegssapisockets
    || socks_issyscall(d, SYMBOL_READ_NOCANCEL))
@@ -3025,14 +2698,8 @@
 }
 #endif
 
-HAVE_PROT_RECVFROM_0
-recvfrom$NOCANCEL(s, buf, len, flags, from, fromlen)
-   HAVE_PROT_RECVFROM_1 s;
-   HAVE_PROT_RECVFROM_2 buf;
-   HAVE_PROT_RECVFROM_3 len;
-   HAVE_PROT_RECVFROM_4 flags;
-   HAVE_PROT_RECVFROM_5 from;
-   HAVE_PROT_RECVFROM_6 fromlen;
+ssize_t
+recvfrom$NOCANCEL(int s, void * buf, size_t len, int flags, struct sockaddr * from, socklen_t * fromlen)
 {
    if (!sockscf.state.havegssapisockets
    || socks_issyscall(s, SYMBOL_RECVFROM_NOCANCEL))
@@ -3041,14 +2708,8 @@
    return Rrecvfrom(s, buf, len, flags, from, fromlen);
 }
 
-HAVE_PROT_SENDTO_0
-sendto$NOCANCEL(s, msg, len, flags, to, tolen)
-   HAVE_PROT_SENDTO_1 s;
-   HAVE_PROT_SENDTO_2 msg;
-   HAVE_PROT_SENDTO_3 len;
-   HAVE_PROT_SENDTO_4 flags;
-   HAVE_PROT_SENDTO_5 to;
-   HAVE_PROT_SENDTO_6 tolen;
+ssize_t
+sendto$NOCANCEL(int s, const void * msg, size_t len, int flags, const struct sockaddr * to, socklen_t tolen)
 {
    if (!sockscf.state.havegssapisockets
    || socks_issyscall(s, SYMBOL_SENDTO_NOCANCEL))
@@ -3059,10 +2720,7 @@
 
 #if 1
 HAVE_PROT_WRITE_0
-write$NOCANCEL(d, buf, nbytes)
-   HAVE_PROT_WRITE_1 d;
-   HAVE_PROT_WRITE_2 buf;
-   HAVE_PROT_WRITE_3 nbytes;
+write$NOCANCEL(HAVE_PROT_WRITE_1 d, HAVE_PROT_WRITE_2 buf, HAVE_PROT_WRITE_3 nbytes)
 {
    if (!sockscf.state.havegssapisockets
    || socks_issyscall(d, SYMBOL_WRITE_NOCANCEL))
