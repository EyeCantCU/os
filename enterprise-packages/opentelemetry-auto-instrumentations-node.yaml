package:
  name: opentelemetry-auto-instrumentations-node
  description: OpenTelemetry instrumentation for JavaScript modules - auto-instrumentations-node component
  version: "0.69.0"
  epoch: 1 # go/enterprise-rsc/opentelemetry-auto-instrumentations-node
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "3"
    memory: 9Gi

environment:
  contents:
    packages:
      - build-base
      - busybox
      - node-gyp
      - nodejs
      - npm
      - py${{vars.python-version}}-setuptools
      - python-${{vars.python-version}}

vars:
  python-version: 3.13

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/open-telemetry/opentelemetry-js-contrib
      tag: auto-instrumentations-node-v${{package.version}}
      expected-commit: 7a5f3c0a09b6a2d32c712b2962b95137c906a016

  - runs: |
      npm install ./packages/auto-instrumentations-node @opentelemetry/api
      DESTDIR=${{targets.destdir}}/usr/share/${{package.name}}
      mkdir -p "$DESTDIR"
      cp -rf ./packages/auto-instrumentations-node/src/** "${DESTDIR}"
      cp -rf ./node_modules package.json "${DESTDIR}"
      npm -C "${DESTDIR}" prune --omit=dev --no-package-lock
      chmod -R go+r "${DESTDIR}"
      cp -rf "${DESTDIR}/node_modules/@opentelemetry/auto-instrumentations-node/build/src"/. "${DESTDIR}"
      ln -s "register.js" "${DESTDIR}/autoinstrumentation.js"

subpackages:
  - name: ${{package.name}}-compat
    description: Compatibility subpackage for ${{package.name}}
    dependencies:
      runtime:
        - ${{package.name}}
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}"
          ln -s ./usr/share/${{package.name}} "${{targets.subpkgdir}}/autoinstrumentation"
    test:
      pipeline:
        - uses: test/tw/symlink-check

update:
  enabled: true
  github:
    identifier: open-telemetry/opentelemetry-js-contrib
    tag-filter: auto-instrumentations-node-v
    strip-prefix: auto-instrumentations-node-v

test:
  environment:
    contents:
      packages:
        - nodejs
  pipeline:
    - uses: test/tw/symlink-check
    - runs: |
        OTEL_LOG_LEVEL=debug node /usr/share/opentelemetry-auto-instrumentations-node/autoinstrumentation.js 2>&1 > log.txt || true
        grep -F -e "OpenTelemetry automatic instrumentation started successfully" ./log.txt
        # this is not expected to work, we are not running under a supported environment.
        # This build is meant to be run as a companion container to another program that uses the autoinstrumentation framework from OpenTelemetry, we need both the daemon to be running successfully and a program that supports this.
