package:
  name: flux-image-reflector-controller-fips
  version: "1.1.0"
  epoch: 1
  description: GitOps Toolkit controller that scans container registries
  copyright:
    - license: Apache-2.0
  resources:
    cpu: "7"
    memory: 13Gi

var-transforms:
  - from: ${{package.version}}
    match: ^(\d+).*
    replace: $1
    to: major-version

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/fluxcd/image-reflector-controller
      tag: v${{package.version}}
      expected-commit: 588857a9f0de2676b904f7a334296f4295c8b603

  - runs: |
      sed -i 's/go 1\.[0-9]\+\(\.[0-9]\+\)\?/go 1.25/' go.mod
      sed -i '/toolchain /d' go.mod
      go mod tidy

  - uses: go/build
    with:
      packages: .
      output: image-reflector-controller
      go-package: go-fips

  - uses: strip

update:
  enabled: true
  ignore-regex-patterns:
    - api/
  github:
    identifier: fluxcd/image-reflector-controller
    strip-prefix: v
    use-tag: true
    tag-filter: v

test:
  environment:
    contents:
      packages:
        - ${{package.name}}
        - curl
  pipeline:
    - uses: test/go-fips-check
    - uses: test/tw/help-check
      with:
        bins: image-reflector-controller
    - uses: test/kwok/cluster
    - name: Launch controller with kwok cluster
      uses: test/daemon-check-output
      with:
        setup: |
          set -euo pipefail
          kubectl apply -f https://github.com/fluxcd/image-reflector-controller/releases/download/v${{package.version}}/image-reflector-controller.crds.yaml
          kubectl wait --for=condition=Ready nodes --all
        start: image-reflector-controller --health-addr :8081 --metrics-addr :8080
        timeout: 30
        expected_output: |
          Starting Controller
          Starting workers
        post: |
          set -euo pipefail
          curl --silent --fail localhost:8080/metrics
          curl --silent --fail localhost:8081/healthz

environment:
  contents:
    packages:
      - go-fips-1.25
