# sshd_config(5) %u=username
# root is necessary for reading dmi tables from /sys/firmware/dmi/tables
# specifically smbios_entry_point and DMI
AuthorizedKeysCommand /usr/bin/qemu-guesthelper fetch-keys %u
AuthorizedKeysCommandUser root
