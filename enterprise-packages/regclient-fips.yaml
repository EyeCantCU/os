package:
  name: regclient-fips
  version: "0.11.1"
  epoch: 1 # CVE-2025-61731
  description: Docker and OCI Registry Client in Go and tooling using those libraries
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - ca-certificates-bundle
      - regclient-regctl-fips # For backwards compatibility

data:
  - name: binaries
    items:
      regbot:
      regctl:
      regsync:

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/regclient/regclient
      tag: v${{package.version}}
      expected-commit: bf3bcfc47173b49ee8000d1d3a1ac15036e83cf0

update:
  enabled: true
  github:
    identifier: regclient/regclient
    strip-prefix: v

subpackages:
  - range: binaries
    name: "regclient-${{range.key}}-fips"
    description: ${{range.value}}
    dependencies:
      provides:
        - ${{range.key}}-fips=${{package.full-version}}
    pipeline:
      - uses: go/build
        with:
          go-package: go-fips
          packages: ./cmd/${{range.key}}
          output: ${{range.key}}
          ldflags: |
            -X github.com/regclient/regclient/internal/version.vcsTag=${{package.version}}
    test:
      pipeline:
        - uses: test/go-fips-check
        - runs: |
            ${{range.key}} --help
            ${{range.key}} version

test:
  pipeline:
    - uses: test/emptypackage
