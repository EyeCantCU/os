package:
  name: chart-iamguarded-thanos
  version: "17.3.4"
  epoch: 1
  description: thanos iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common
      - chart-iamguarded-minio

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: thanos/${{package.version}}
      expected-commit: ac5177e824beaeedd694d0a0edcaaf5a02fddb9b

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: patch
    with:
      patches: values.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/thanos

  - uses: charts/package
    with:
      chart-path: iamguarded/thanos

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: thanos/
    use-tag: true
    tag-filter-prefix: thanos/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /thanos
