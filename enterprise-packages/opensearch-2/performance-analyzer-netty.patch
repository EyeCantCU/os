--- a/build.gradle	2026-01-13 14:55:12
+++ b/build.gradle	2026-01-14 21:07:08
@@ -273,7 +273,7 @@
 
     def log4jVersion = "${versions.log4j}"
     def protobufVersion = "${versions.protobuf}"
-    def nettyVersion = "${versions.netty}"
+    def nettyVersion = "4.1.130.Final"
 
     configurations {
         // jarHell reports class name conflicts between securemock and mockito-core
@@ -301,6 +301,16 @@
             force "org.apache.httpcomponents:httpcore:${versions.httpcore}"
             force "commons-codec:commons-codec:${versions.commonscodec}"
             force "io.netty:netty-codec-http2:${nettyVersion}"
+            force "io.netty:netty-buffer:${nettyVersion}"
+            force "io.netty:netty-codec:${nettyVersion}"
+            force "io.netty:netty-codec-http:${nettyVersion}"
+            force "io.netty:netty-common:${nettyVersion}"
+            force "io.netty:netty-handler:${nettyVersion}"
+            force "io.netty:netty-resolver:${nettyVersion}"
+            force "io.netty:netty-transport:${nettyVersion}"
+            force "io.netty:netty-transport-native-unix-common:${nettyVersion}"
+            force "io.netty:netty-codec-socks:${nettyVersion}"
+            force "io.netty:netty-handler-proxy:${nettyVersion}"
         }
     }
 
@@ -492,8 +502,41 @@
     }
 }
 
+// CVE-2025-67735: Patch performance-analyzer-rca build.gradle to force netty 4.1.130.Final
+task patchRcaNetty {
+    dependsOn cloneRcaGitRepo
+    doLast {
+        def rcaBuildFile = file("${rcaProjectDir}/build.gradle")
+        if (rcaBuildFile.exists()) {
+            def content = rcaBuildFile.text
+            // Add netty force declarations after the existing resolutionStrategy block
+            def nettyForceBlock = '''
+// CVE-2025-67735 GHSA-84h7-rjj3-6jx4 - force netty 4.1.130.Final
+configurations.all {
+    resolutionStrategy {
+        force 'io.netty:netty-buffer:4.1.130.Final'
+        force 'io.netty:netty-codec:4.1.130.Final'
+        force 'io.netty:netty-codec-http:4.1.130.Final'
+        force 'io.netty:netty-codec-http2:4.1.130.Final'
+        force 'io.netty:netty-common:4.1.130.Final'
+        force 'io.netty:netty-handler:4.1.130.Final'
+        force 'io.netty:netty-resolver:4.1.130.Final'
+        force 'io.netty:netty-transport:4.1.130.Final'
+        force 'io.netty:netty-transport-native-unix-common:4.1.130.Final'
+        force 'io.netty:netty-codec-socks:4.1.130.Final'
+        force 'io.netty:netty-handler-proxy:4.1.130.Final'
+    }
+}
+'''
+            // Append the force block at the end of the file
+            rcaBuildFile.append(nettyForceBlock)
+            println "Patched RCA build.gradle with netty 4.1.130.Final force declarations"
+        }
+    }
+}
+
 task buildRca() {
-    dependsOn(cloneRcaGitRepo)
+    dependsOn(patchRcaNetty)
 
     doFirst {
         logger.info("Building performance-analyzer-rca project in ${rcaProjectDir} with -Dopensearch.version=${opensearch_version}")
