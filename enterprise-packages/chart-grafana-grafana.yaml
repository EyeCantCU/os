package:
  name: chart-grafana-grafana
  version: "10.5.13"
  epoch: 0
  description: The leading tool for querying and visualizing time series and metrics.
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - busybox
      - helm

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/grafana/helm-charts
      tag: grafana-${{package.version}}
      expected-commit: 80d4a6612a2078f6d1cf36b3cb8c16037c43ba1c

  # Fix hardcoded sha256: prefix in templates - the .sha field should accept the full digest
  - runs: |
      find charts/grafana/templates -type f \( -name '*.yaml' -o -name '*.tpl' \) \
        -exec sed -i 's/@sha256:{{ \.Values\./@{{ .Values./g' {} +

  - uses: charts/package
    with:
      chart-path: charts/grafana
      images: |
        grafana:
          values:
            image:
              registry: ${registry}
              repository: ${repo}
              tag: ${tag}
              sha: ${digest}
        downloadDashboardsImage:
          values:
            downloadDashboardsImage:
              registry: ${registry}
              repository: ${repo}
              tag: ${tag}
              sha: ${digest}
        initChownData:
          values:
            initChownData:
              image:
                registry: ${registry}
                repository: ${repo}
                tag: ${tag}
                sha: ${digest}
        sidecar:
          values:
            sidecar:
              image:
                registry: ${registry}
                repository: ${repo}
                tag: ${tag}
                sha: ${digest}
        imageRenderer:
          values:
            imageRenderer:
              image:
                registry: ${registry}
                repository: ${repo}
                tag: ${tag}
                sha: ${digest}
      test: |
        cases:
          - name: default
            images: [grafana]
          - name: with-persistence
            images: [grafana, initChownData]
            values:
              persistence:
                enabled: true
          - name: with-sidecar
            images: [grafana, sidecar]
            values:
              sidecar:
                dashboards:
                  enabled: true
          - name: with-image-renderer
            images: [grafana, imageRenderer]
            values:
              imageRenderer:
                enabled: true

test:
  pipeline:
    - uses: test/chart-standards

update:
  enabled: true
  github:
    identifier: grafana/helm-charts
    strip-prefix: grafana-
    use-tag: true
    tag-filter-prefix: grafana-
