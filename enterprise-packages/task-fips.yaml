package:
  name: task-fips
  version: "3.48.0"
  epoch: 4 # GHSA-9h8m-3fm2-qjrq
  description: A task runner / simpler Make alternative written in Go
  copyright:
    - license: MIT
  resources:
    cpu: "3"
    memory: 8Gi

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/go-task/task
      tag: v${{package.version}}
      expected-commit: 09e7247d0552ccb48146994d817e6d585c70ab0f

  - uses: go/bump
    with:
      deps: |-
        go.opentelemetry.io/otel/sdk@v1.40.0

  - uses: go/build
    with:
      packages: ./cmd/task
      modroot: .
      go-package: go-fips
      output: task

update:
  enabled: true
  github:
    identifier: go-task/task
    strip-prefix: v

test:
  pipeline:
    - uses: test/go-fips-check
    - uses: test/tw/help-check
      with:
        bins: task
    - uses: test/tw/ver-check
      with:
        bins: task
    - name: demo taskfile
      runs: |
        set -euo pipefail
        tee Taskfile.yml <<EOF
        version: '3'
        tasks:
          hello:
            cmds:
              - echo "Hello, world!"
        EOF
        task hello
        task --list-all
