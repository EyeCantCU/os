package:
  name: consul-fips-1.22
  version: "1.22.3"
  epoch: 2 # CVE-2025-61732
  description: Consul is a distributed, highly available, and data center aware solution to connect and configure applications across dynamic, distributed infrastructure.
  copyright:
    - license: BUSL-1.1
  dependencies:
    provides:
      - consul-fips=${{package.full-version}}
    runtime:
      - merged-bin
      - wolfi-baselayout
  resources:
    cpu: "16"
    memory: 32Gi

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go-fips
      - nodejs-20
      - nofips-node
      - pnpm
      - yarn

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/hashicorp/consul
      tag: v${{package.version}}
      expected-commit: cc9bcd61f4d23a9d9a454e221c65893f4c5720f8
      destination: ${{package.name}}

  - working-directory: ${{package.name}}
    pipeline:
      - uses: go/remove-cgo-enabled-0
      - runs: |
          make ui-regen linux
      - runs: |
          mkdir -p ${{targets.destdir}}/usr/bin

          # The docker-entrypoint.sh expects the binary to be in /bin so put it there
          mv ./pkg/bin/linux_*/consul ${{targets.destdir}}/usr/bin/consul
      - uses: strip

subpackages:
  - name: ${{package.name}}-oci-entrypoint
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}/usr/bin
          mv ${{package.name}}/.release/docker/docker-entrypoint.sh "${{targets.subpkgdir}}/usr/bin/"
    dependencies:
      provides:
        - consul-fips-oci-entrypoint=${{package.full-version}}
      runtime:
        - ${{package.name}}
        - busybox
        - dumb-init
        - libcap-utils
        - merged-bin
        - su-exec
        - wolfi-baselayout
    test:
      pipeline:
        - name: "Verify entrypoint file by executing consul for version check"
          runs: CONSUL_DISABLE_PERM_MGMT=1 /usr/bin/docker-entrypoint.sh --version | grep ${{package.version}}

  - name: ${{package.name}}-oci-entrypoint-compat
    pipeline:
      - runs: |
          mkdir -p "${{targets.subpkgdir}}/usr/local/bin"
          ln -s /usr/bin/docker-entrypoint.sh "${{targets.subpkgdir}}/usr/local/bin/"
    dependencies:
      provides:
        - consul-fips-oci-entrypoint-compat=${{package.full-version}}
      runtime:
        - ${{package.name}}-oci-entrypoint
        - merged-bin
        - wolfi-baselayout
    test:
      pipeline:
        - name: "Verify symlink exists"
          runs: test -L /usr/local/bin/docker-entrypoint.sh

test:
  pipeline:
    - uses: test/go-fips-check

update:
  enabled: true
  git:
    strip-prefix: v
    tag-filter-prefix: v1.22. # Keeping it this because we have other consul tagged versions separately like 1.1
