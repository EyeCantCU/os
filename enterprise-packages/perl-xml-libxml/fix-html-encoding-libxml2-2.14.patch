diff --git a/t/12html.t b/t/12html.t
index 7181d26..c41985a 100644
--- a/t/12html.t
+++ b/t/12html.t
@@ -89,7 +89,7 @@ SKIP:
     }
     use utf8;
 
-    my $utf_str = "ěščř";
+    my $utf_str = "café";
 
     # w/o 'meta' charset
     $strhref = <<EOHTML;
@@ -120,10 +120,10 @@ EOHTML
     is($htmldoc->findvalue('//p/text()'), $utf_str, ' TODO : Add test name');
 
 
-    my $iso_str = Encode::encode('iso-8859-2', $strhref);
+    my $iso_str = Encode::encode('iso-8859-1', $strhref);
     $htmldoc = $parser->parse_html_string( $iso_str,
         {
-            encoding => 'iso-8859-2'
+            encoding => 'iso-8859-1'
         }
     );
     # TEST
@@ -136,7 +136,7 @@ EOHTML
 <html>
   <head>
     <meta http-equiv="Content-Type" content="text/html;
-      charset=iso-8859-2">
+      charset=iso-8859-1">
   </head>
   <body>
     <p>$utf_str</p>
@@ -150,14 +150,14 @@ EOHTML
     # TEST
     is($htmldoc->findvalue('//p/text()'), $utf_str, ' TODO : Add test name');
 
-    $iso_str = Encode::encode('iso-8859-2', $strhref);
+    $iso_str = Encode::encode('iso-8859-1', $strhref);
     $htmldoc = $parser->parse_html_string( $iso_str );
     # TEST
     ok( $htmldoc && $htmldoc->getDocumentElement, ' TODO : Add test name' );
     # TEST
     is($htmldoc->findvalue('//p/text()'), $utf_str, ' TODO : Add test name');
 
-    $htmldoc = $parser->parse_html_string( $iso_str, { encoding => 'iso-8859-2',
+    $htmldoc = $parser->parse_html_string( $iso_str, { encoding => 'iso-8859-1',
             URI => 'foo'
         } );
     # TEST
@@ -170,7 +170,12 @@ EOHTML
 
 # parse example/enc_latin2.html
 # w/ 'meta' charset
+# libxml2 2.14+ removed iso-8859-2 support in HTML mode (HTML5 compliance)
+SKIP:
 {
+    if (XML::LibXML::LIBXML_VERSION() >= 21400) {
+        skip("iso-8859-2 not supported in HTML mode for libxml2 >= 2.14.0", 13);
+    }
     use utf8;
     my $utf_str = "ěščř";
     my $test_file = 'example/enc_latin2.html';
@@ -244,7 +249,12 @@ EOHTML
 
 # parse example/enc2_latin2.html
 # w/o 'meta' charset
+# libxml2 2.14+ removed iso-8859-2 support in HTML mode (HTML5 compliance)
+SKIP:
 {
+    if (XML::LibXML::LIBXML_VERSION() >= 21400) {
+        skip("iso-8859-2 not supported in HTML mode for libxml2 >= 2.14.0", 5);
+    }
     use utf8;
     my $utf_str = "ěščř";
     my $test_file = 'example/enc2_latin2.html';
