package:
  name: hugo-fips
  version: "0.146.7"
  epoch: 0
  description: The world's fastest framework for building websites.
  copyright:
    - license: Apache-2.0

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/gohugoio/hugo
      tag: v${{package.version}}
      expected-commit: 1ad3d39dc4693434505fc81f91eed57333017e93

  - uses: go/build
    with:
      packages: .
      output: hugo
      tags: extended
      go-package: go-fips

update:
  enabled: true
  github:
    identifier: gohugoio/hugo
    strip-prefix: v

test:
  pipeline:
    - uses: test/go-fips-check
    - name: Hugo help/version check
      runs: |
        hugo help
        hugo version | grep ${{package.version}}
    - name: Create a new Hugo site
      runs: hugo new site test-site
    - uses: test/daemon-check-output
      working-directory: test-site
      with:
        start: /usr/bin/hugo serve
        expected_output: "Web Server is available at"
        timeout: 10
