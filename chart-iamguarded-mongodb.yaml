package:
  name: chart-iamguarded-mongodb
  version: "16.5.43"
  epoch: 1
  description: mongodb iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: mongodb/${{package.version}}
      expected-commit: fbb67fcd71e9845b0943e0d8058eae009510cb82

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/mongodb

  - uses: charts/package
    with:
      chart-path: iamguarded/mongodb

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: mongodb/
    use-tag: true
    tag-filter-prefix: mongodb/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /mongodb
