package:
  name: chart-iamguarded-rabbitmq-cluster-operator
  version: "4.4.10"
  epoch: 1
  description: rabbitmq-cluster-operator iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: rabbitmq-cluster-operator/${{package.version}}
      expected-commit: ee56fcc8cc520b6c5731bb0c265ccca6b88cff23

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/rabbitmq-cluster-operator

  - uses: charts/package
    with:
      chart-path: iamguarded/rabbitmq-cluster-operator

update:
  enabled: false
  manual: true
  schedule:
    period: daily
    reason: Update automation doesn't yet support automating from private repos.
  git:

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: rabbitmq-cluster-operator
