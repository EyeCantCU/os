diff --git a/aten/src/ATen/CMakeLists.txt b/aten/src/ATen/CMakeLists.txt
index a3e841ca22..7380ecd64e 100644
--- a/aten/src/ATen/CMakeLists.txt
+++ b/aten/src/ATen/CMakeLists.txt
@@ -358,7 +358,10 @@ IF(USE_FBGEMM_GENAI)
   endif()
 
   # Ensure torch_cpu is built before fbgemm_genai to avoid race conditions
-  add_dependencies(fbgemm_genai torch_cpu)
+  # Only add dependency if torch_cpu target exists (not in BUILD_LIBTORCHLESS mode)
+  if(TARGET torch_cpu)
+    add_dependencies(fbgemm_genai torch_cpu)
+  endif()
 endif()
 
 # XNNPACK
