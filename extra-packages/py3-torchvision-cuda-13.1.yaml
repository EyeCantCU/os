package:
  name: py3-torchvision-cuda-13.1
  version: "0.25.0"
  epoch: 0
  description: Datasets, Transforms and Models specific to Computer Vision
  copyright:
    - license: BSD-3-Clause
  resources:
    cpu: 16
    memory: 48Gi
  options:
    no-depends: true

vars:
  cudnn-version: 9
  pypi-package: torchvision
  import: torchvision

var-transforms:
  - from: ${{package.name}}
    match: '.*cuda-(\d+\.\d+).*'
    replace: '$1'
    to: cuda-version
  - from: ${{package.name}}
    match: '.*cuda-(\d+)\.\d+$'
    replace: '$1'
    to: cuda-major-version
  - from: ${{package.name}}
    match: '.*cuda-(\d+)\.(\d+)$'
    replace: 'cu$1$2'
    to: cuda-pyvariant

data:
  - name: py-versions
    items:
      3.11:
      3.12:
      3.13:

environment:
  contents:
    packages:
      - busybox
      - cmake
      - cuda-toolkit-${{vars.cuda-version}}-dev
      - libjpeg-dev
      - libjpeg-turbo-dev
      - libpng-dev
      - libpng-utils
      - nvcc-${{vars.cuda-version}}-gcc-default
      - py3-supported-build-base-dev
      - py3-supported-torch-cuda-${{vars.cuda-version}}-dev
      - samurai
  environment:
    BUILD_VERSION: ${{package.version}}+${{vars.cuda-pyvariant}}
    CUDA_HOME: /usr/local/cuda-${{vars.cuda-version}}
    CUDA_NVCC_EXECUTABLE: /usr/local/cuda-${{vars.cuda-version}}/bin/nvcc
    CUDA_VERSION: ${{vars.cuda-version}}
    CMAKE_CUDA_ARCHITECTURES: all
    FORCE_CUDA: 1
    USE_CUDA: 1
    USE_CUDNN: 1
    TORCH_CUDA_ARCH_LIST: "7.5 8.0 8.6 9.0 10.0 12.0+PTX"
    TORCH_NVCC_FLAGS: -Xfatbin -compress-all
    VERBOSE: 1

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pytorch/vision.git
      tag: v${{package.version}}
      expected-commit: 8ac84ee75afb1c327902156b5336f56ad63b7e2f

  - name: prepare sdist in advance to avoid creating a new one for each build variant
    runs: |
      set -oue pipefail

      # Find latest Python
      # This prevents us from having to point at a pinned version of Python
      python_latest_version=""
      for python_bin in /usr/bin/python3.[0-9][0-9]; do
        # Extract Python version from the path
        python_version=${python_bin#/usr/bin/python}

        # Normalize version to fixed width so that we can compare versions that are different lengths
        # I.E., we'd never want Python 3.11.10 to be higher than Python 3.12.1
        python_normalized_version=$(echo "$python_version" | awk -F '.' '{ printf("%03d%03d%03d", $1,$2,$3) }')

        # If the latest version has yet to be set, assume the current version is latest
        # Otherwise, compare the latest version to the current normalized version
        if [ -z "$python_latest_version" ] || [ "$python_normalized_version" -gt "$python_latest_version" ]; then
          python_latest_bin="$python_bin"
          python_latest_version="$python_normalized_version"
        fi
      done

      $python_latest_bin -m build --sdist -n -x

test:
  pipeline:
    - uses: test/emptypackage

subpackages:
  - name: libtorchvision-cuda-${{vars.cuda-version}}
    description: C++ torchvision library
    dependencies:
      runtime:
        - libtorch-cuda-${{vars.cuda-version}}
    pipeline:
      - name: Build libtorchvision.so
        runs: |
          export PATH="$PATH:$CUDA_HOME/bin"

          mkdir -p build && cd build
          cmake .. \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_INSTALL_PREFIX=/usr \
            -DCUDA_TOOLKIT_ROOT_DIR=/usr/local/cuda-${{vars.cuda-version}} \
            -DUSE_SYSTEM_NVTX=1 \
            -DCMAKE_CUDA_ARCHITECTURES="${CMAKE_CUDA_ARCHITECTURES}"
          make -j$(nproc)
          make install DESTDIR="${{targets.contextdir}}"
      - uses: strip
    test:
      pipeline:
        - uses: test/tw/ldd-check

  - name: libtorchvision-cuda-${{vars.cuda-version}}-dev
    description: C++ torchvision development files
    dependencies:
      runtime:
        - libtorchvision-cuda-${{vars.cuda-version}}
    pipeline:
      - uses: split/dev
        with:
          package: libtorchvision-cuda-${{vars.cuda-version}}

  - range: py-versions
    name: py${{range.key}}-${{vars.pypi-package}}-cuda-${{vars.cuda-version}}
    description: python${{range.key}} version of ${{vars.pypi-package}} for CUDA ${{vars.cuda-version}}
    dependencies:
      runtime:
        - cuda-toolkit-${{vars.cuda-version}}
        - nvidia-cudnn-${{vars.cudnn-version}}-cuda-${{vars.cuda-major-version}}
        - nvidia-nccl-cuda-${{vars.cuda-version}}
        - py${{range.key}}-pillow
        - py${{range.key}}-pyyaml
        - py${{range.key}}-torch-cuda-${{vars.cuda-version}}
        - py${{range.key}}-typing-extensions
    options:
      no-depends: true
    pipeline:
      - name: Python Build
        runs: |
          python${{range.key}} -m pip install --verbose \
            --no-index --no-build-isolation --no-deps \
            --force-reinstall --no-compile --prefix=/usr "--root=${{targets.contextdir}}" dist/torchvision-${{package.version}}+${{vars.cuda-pyvariant}}.tar.gz
          sitepkgd=$(python${{range.key}} -c 'import site; print(site.getsitepackages()[0])')
          python${{range.key}} -m compileall --invalidation-mode=unchecked-hash -r100 ${{targets.contextdir}}/$sitepkgd
    test:
      pipeline:
        - uses: test/tw/ldd-check
        - uses: test/tw/pip-check
        - uses: python/import
          with:
            python: python${{range.key}}
            import: ${{vars.import}}
        - runs: |
            python${{range.key}} ./quickstart.py > output.out 2>&1 &
            grep -E "ERROR|None" output.out && exit 1

  - name: py3-supported-${{vars.pypi-package}}-cuda-${{vars.cuda-version}}
    description: meta package providing ${{vars.pypi-package}} for supported python versions.
    dependencies:
      runtime:
        - py3.11-${{vars.pypi-package}}-cuda-${{vars.cuda-version}}
        - py3.12-${{vars.pypi-package}}-cuda-${{vars.cuda-version}}
        - py3.13-${{vars.pypi-package}}-cuda-${{vars.cuda-version}}
    test:
      pipeline:
        - uses: test/metapackage

update:
  enabled: true
  github:
    identifier: pytorch/vision
    strip-prefix: v
    tag-filter: v
