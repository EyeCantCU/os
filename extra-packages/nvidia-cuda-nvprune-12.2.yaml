package:
  name: nvidia-cuda-nvprune-12.2
  version: 12.2.140
  epoch: 3
  description: CUDA nvprune
  copyright:
    - license: LicenseRef-NVIDIA-CUDA-nvprune
      license-path: LICENSE
  dependencies:
    provides:
      - nvidia-cuda-nvprune=${{package.full-version}}

environment:
  contents:
    packages:
      - busybox

var-transforms:
  - from: ${{package.name}}
    match: '.*-(\d+\.\d+).*'
    replace: '$1'
    to: cuda-version

pipeline:
  - assertions:
      required-steps: 1
    pipeline:
      - if: ${{build.arch}} == 'aarch64'
        uses: fetch
        with:
          # SBSA = ARM Server Base System Architecture (now called SystemReady)
          uri: "https://developer.download.nvidia.com/compute/cuda/redist/cuda_nvprune/linux-sbsa/cuda_nvprune-linux-sbsa-${{package.version}}-archive.tar.xz"
          expected-sha256: "4e434e9ced2efb9a3bc83edca93262eaa54546cf74783658239f2a05b119eb47"
          timeout: 30
      - if: ${{build.arch}} == 'x86_64'
        uses: fetch
        with:
          uri: "https://developer.download.nvidia.com/compute/cuda/redist/cuda_nvprune/linux-x86_64/cuda_nvprune-linux-x86_64-${{package.version}}-archive.tar.xz"
          expected-sha256: "f5cf2db9d3214d540490c663691b171b11531be98611b4e6b86b208c7ee4f5cf"
          timeout: 30

  # Setup version symlinks
  - runs: |
      mkdir -p ${{targets.destdir}}/usr/local/

  - runs: |
      mkdir -p ${{targets.destdir}}/usr/local/cuda-${{vars.cuda-version}}/
      mv bin ${{targets.destdir}}/usr/local/cuda-${{vars.cuda-version}}/
      cp LICENSE ${{targets.destdir}}/usr/local/cuda-${{vars.cuda-version}}/${{package.name}}.LICENSE

update:
  enabled: false

test:
  pipeline:
    - runs: |
        /usr/local/cuda-${{vars.cuda-version}}/bin/nvprune --version
    - uses: test/virtualpackage
      with:
        virtual-pkg-name: nvidia-cuda-nvprune
        real-pkg-name: ${{package.name}}
