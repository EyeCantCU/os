#nolint:git-checkout-must-use-github-updates
package:
  name: ffnvcodec
  version: "13.0.19.0"
  epoch: 1 # go/extras-rsc/ffnvcodec
  description: ffmpeg nvidia codec sdk wrapper
  copyright:
    - license: MIT
  resources:
    cpu: "1"
    memory: 11Gi

environment:
  contents:
    packages:
      - build-base
      - nvidia-codec-sdk
      - wolfi-base
  environment:
    LIBRARY_PATH: /usr/lib/nvidia-codec-sdk/stubs

pipeline:
  - uses: git-checkout
    with:
      repository: https://git.videolan.org/git/ffmpeg/nv-codec-headers.git
      expected-commit: e844e5b26f46bb77479f063029595293aa8f812d
      tag: n${{package.version}}
      depth: 1

  - runs: |
      make install PREFIX=/usr DESTDIR="${{targets.destdir}}"

  - uses: strip

test:
  pipeline:
    - uses: test/pkgconf

update:
  enabled: true
  release-monitor:
    identifier: 223796
