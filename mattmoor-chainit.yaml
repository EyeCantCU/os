package:
  name: mattmoor-chainit
  version: 0.0.2
  epoch: 0
  description: Experimental init system implementing apko semantics.
  copyright:
    - license: Apache-2.0

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/mattmoor/chainit
      tag: v${{package.version}}
      expected-commit: abea6114cf24651c859887cf9ce6d3e1b8075e53

  - uses: go/build
    with:
      packages: .
      output: chainit

  - uses: strip

  - runs: |
      mkdir -p ${{targets.destdir}}/etc
      ln -s /usr/bin/chainit ${{targets.destdir}}/init

update:
  enabled: true
  github:
    identifier: mattmoor/chainit
    strip-prefix: v
