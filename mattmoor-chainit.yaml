package:
  name: mattmoor-chainit
  version: 0.0.1
  epoch: 0
  description: Experimental init system implementing apko semantics.
  copyright:
    - license: Apache-2.0

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/mattmoor/chainit
      tag: v${{package.version}}
      expected-commit: a2a8d66a52a7d1d81c9f7533e6711f4d0b72f18a

  - uses: go/build
    with:
      packages: .
      output: chainit

  - uses: strip

  - runs: |
      mkdir -p ${{targets.destdir}}/etc
      ln -s /usr/bin/chainit ${{targets.destdir}}/init

update:
  enabled: true
  github:
    identifier: mattmoor/chainit
    strip-prefix: v
