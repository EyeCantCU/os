package:
  name: rdma-core
  version: 58.0
  epoch: 0
  description: userspace components for the Linux Kernel's drivers/infiniband subsystem
  copyright:
    - license: BSD-2-Clause
      license-path: COPYING.BSD_FB
    - license: Linux-OpenIB
      license-path: COPYING.BSD_MIT
    - license: GPL-2.0
      license-path: COPYING.GPL2
    - license: Linux-OpenIB
      license-path: debian/copyright
    - license: MIT
      license-path: debian/copyright
    - license: CPL-1.0
      license-path: debian/copyright
    - license: BSD-2-Clause
      license-path: debian/copyright
    - license: BSD-3-Clause
      license-path: debian/copyright
    - license: CC0-1.0
      license-path: debian/copyright
    - license: Linux-OpenIB
      license-path: providers/ipathverbs/COPYING
    - license: CC0-1.0
      license-path: ccan/LICENSE.CCO
    - license: MIT
      license-path: ccan/LICENSE.MIT

vars:
  py-version: 3.13

environment:
  contents:
    packages:
      - bash
      - build-base
      - busybox
      - cmake
      - cmd:rst2man
      - libnl3-dev
      - python-${{vars.py-version}}
      - samurai

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/linux-rdma/rdma-core
      tag: v${{package.version}}
      expected-commit: 6697a72f6cfa2a5895cdf796d45744bbb778723c

  - uses: cmake/configure
    with:
      opts: |
        -DPYTHON_EXECUTABLE="python${{vars.py-version}}" \
        -DCMAKE_INSTALL_SBINDIR="bin" \
        -DNO_MAN_PAGES=ON  # needs pandoc, which we don't have

  - uses: cmake/build

  - uses: cmake/install

  - uses: strip

subpackages:
  - name: rdma-core-dev
    pipeline:
      - uses: split/dev
    dependencies:
      runtime:
        - rdma-core
    description: rdma-core dev
    test:
      pipeline:
        - uses: test/pkgconf
        - uses: test/tw/ldd-check

update:
  enabled: true
  github:
    identifier: linux-rdma/rdma-core
    strip-prefix: v

test:
  pipeline:
    - uses: test/tw/ldd-check
