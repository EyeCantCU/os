package:
  name: chart-iamguarded-flux
  version: "2.4.26"
  epoch: 3
  description: flux iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: flux/2.4.12
      expected-commit: c1db9da7567881f995727d6dc8402ea9d647f56a

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/flux

  - uses: charts/package
    with:
      chart-path: iamguarded/flux

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: flux/
    use-tag: true
    tag-filter-prefix: flux/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /flux
