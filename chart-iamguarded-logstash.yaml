package:
  name: chart-iamguarded-logstash
  version: 7.0.2
  epoch: 0
  description: logstash iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: logstash/7.0.2
      expected-commit: dd6eb3cb6d0381c03a9febfca500c905cd911f86

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/logstash

  - uses: charts/package
    with:
      chart-path: iamguarded/logstash

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: logstash/
    use-tag: true
    tag-filter-prefix: logstash/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /logstash
