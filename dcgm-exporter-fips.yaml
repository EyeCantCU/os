package:
  name: dcgm-exporter-fips
  version: "4.4.2.4.7.1"
  epoch: 0 # GHSA-j5w8-q4qc-rx2x
  description: NVIDIA GPU metrics exporter for Prometheus leveraging DCGM
  copyright:
    - license: Apache-2.0
  dependencies:
    runtime:
      - DCGM

environment:
  contents:
    packages:
      - libcap-utils
  environment:
    # See https://github.com/wolfi-dev/os/issues/34568
    GCC_SPEC_FILE: /home/build/openssf.spec

var-transforms:
  - from: ${{package.version}}
    match: '^(\d+).(\d+).(\d+).(\d+).(\d+).(\d+)$'
    replace: '$1.$2.$3-$4.$5.$6'
    to: mangled-package-version

vars:
  generate-openssf-spec-script: |
    gccdir="$(GCC_SPEC_FILE=/dev/null gcc --print-search-dirs | grep ^install: | cut -d' ' -f2)"
    sed -r 's/-z now//' < "$gccdir/openssf.spec" > /home/build/openssf.spec

pipeline:
  - uses: git-checkout
    with:
      expected-commit: b921c57eb70015d779d1e8237d609925614d7965
      repository: https://github.com/NVIDIA/dcgm-exporter
      tag: ${{vars.mangled-package-version}}

  - uses: go/bump
    with:
      deps: |-
        golang.org/x/crypto@v0.45.0

  - runs: |
      ${{vars.generate-openssf-spec-script}}

  - runs: |
      make generate update-version

  - uses: go/build
    with:
      ldflags: -X main.BuildVersion=${{vars.mangled-package-version}}
      output: dcgm-exporter
      packages: ./cmd/dcgm-exporter
      go-package: go-fips

  - runs: |
      mkdir -p ${{targets.contextdir}}/etc
      cp -r etc ${{targets.contextdir}}/etc/dcgm-exporter

subpackages:
  - name: dcgm-exporter-privileged-fips
    description: "entrypoint compat package for dcgm-exporter"
    dependencies:
      replaces:
        - ${{package.name}}
    options:
      no-depends: true
      no-provides: true
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/bin
          cp ${{targets.destdir}}/usr/bin/dcgm-exporter ${{targets.contextdir}}/usr/bin/dcgm-exporter
          # this avoids need of shipping shell utils entrypoint etc.
          setcap cap_sys_admin=+ep ${{targets.contextdir}}/usr/bin/dcgm-exporter
    test:
      pipeline:
        - uses: test/tw/contains-files
          with:
            files: |
              /usr/bin/dcgm-exporter

update:
  enabled: true
  github:
    identifier: NVIDIA/dcgm-exporter
  version-transform:
    - match: ^(\d+(\.\d+)*)-(\d+(\.\d+)*)$
      replace: ${1}.${3}

test:
  pipeline:
    - runs: |
        ${{vars.generate-openssf-spec-script}}
    - uses: test/go-fips-check
    - runs: stat /usr/bin/dcgm-exporter
