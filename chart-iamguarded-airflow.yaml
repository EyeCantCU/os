package:
  name: chart-iamguarded-airflow
  version: "22.7.3"
  epoch: 2
  description: Airflow iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common
      - chart-iamguarded-postgresql
      - chart-iamguarded-redis

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: airflow/${{package.version}}
      expected-commit: c62ad9b97dad33d09d2888d21066d9d25cafeed5

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/airflow

  - uses: charts/package
    with:
      chart-path: iamguarded/airflow

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: airflow/
    use-tag: true
    tag-filter-prefix: airflow/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /airflow
