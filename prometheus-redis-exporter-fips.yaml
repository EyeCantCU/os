package:
  name: prometheus-redis-exporter-fips
  version: 1.63.0
  epoch: 0
  description: Prometheus Exporter for Redis Metrics.
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go-fips
      - wolfi-baselayout

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/oliver006/redis_exporter
      tag: v${{package.version}}
      expected-commit: 3526bf717ef97617132b64701cd112f21be13cd0

  - uses: go/build
    with:
      packages: .
      output: redis_exporter
      go-package: go-fips
      ldflags: |
        -X main.BuildVersion=v${{package.version}}
        -X main.BuildCommitSha=$(git rev-parse HEAD)
        -X main.BuildDate=$(date +%F-%T)

test:
  pipeline:
    - uses: test/go-fips-check

update:
  enabled: true
  github:
    identifier: oliver006/redis_exporter
    strip-prefix: v
    tag-filter: v
