package:
  name: cluster-autoscaler-fips-1.25
  version: 1.25.3
  epoch: 6
  description: Autoscaling components for Kubernetes
  copyright:
    - license: Apache-2.0
  dependencies:
    provides:
      - cluster-autoscaler-fips=${{package.full-version}}
    runtime:
      # either melange SCA should provide these
      # or we should make go-fips shared link on these
      - so:libcrypto.so.3
      - so:libssl.so.3
      # for fips config & fips-provider
      - openssl-config-fipshardened

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/kubernetes/autoscaler
      tag: cluster-autoscaler-${{package.version}}
      expected-commit: 5a4d7d25a3c902a664fac39360b3e4635a74fdbd

  - uses: go/bump
    with:
      deps: google.golang.org/grpc@v1.56.3 k8s.io/kubernetes@v1.25.16 k8s.io/apiserver@v0.25.16 golang.org/x/crypto@v0.17.0 google.golang.org/protobuf@v1.33.0
      modroot: cluster-autoscaler

  - uses: go/build
    with:
      modroot: cluster-autoscaler
      packages: .
      output: cluster-autoscaler
      # CVE-2023-39325 and CVE-2023-3978
      deps: github.com/docker/distribution@v2.8.2 golang.org/x/net@v0.17.0
      vendor: true
      go-package: go-fips

subpackages:
  - name: ${{package.name}}-compat
    pipeline:
      - runs: |
          mkdir -p ${{targets.subpkgdir}}
          # The upstream helm chart assumes the binary is in /.
          ln -sf /usr/bin/cluster-autoscaler ${{targets.subpkgdir}}/cluster-autoscaler
    dependencies:
      provides:
        - cluster-autoscaler-fips-compat=${{package.full-version}}

update:
  enabled: true
  github:
    identifier: kubernetes/autoscaler
    strip-prefix: cluster-autoscaler-
    use-tag: true
    # There are other tags like "cluster-autoscaler-chart-FOO"
    tag-filter: cluster-autoscaler-1.25.
