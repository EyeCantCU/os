package:
  name: chart-iamguarded-keycloak
  version: "25.0.2"
  epoch: 1
  description: keycloak iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common
      - chart-iamguarded-postgresql

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: keycloak/${{package.version}}
      expected-commit: 14e2c8a3030bd82ac7ba9d843cfa4f09a5645dc2

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: patch
    with:
      patches: values.patch

  - uses: patch
    with:
      patches: readme.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/keycloak

  - uses: charts/package
    with:
      chart-path: iamguarded/keycloak

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: keycloak/
    use-tag: true
    tag-filter-prefix: keycloak/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /keycloak
