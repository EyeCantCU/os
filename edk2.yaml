#nolint:valid-pipeline-git-checkout-tag
package:
  name: edk2
  version: "202508"
  epoch: 0
  description: "A modern, feature-rich, cross-platform firmware development environment for UEFI"
  copyright:
    - license: BSD-2-Clause-Patent
  resources:
    cpu: 16
    memory: 16Gi

environment:
  contents:
    packages:
      - acpica
      - bash
      - build-base
      - busybox
      - coreutils
      - nasm
      - perl
      - perl-json
      - python3
      - util-linux-dev

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/tianocore/edk2
      expected-commit: d46aa46c8361194521391aa581593e556c707c6e
      tag: edk2-stable${{package.version}}

  - uses: git-checkout
    with:
      repository: https://gitlab.com/kraxel/edk2-build-config
      expected-commit: 3d1f20bdce9dd9413b0b73f45286ed155a738a50
      branch: master
      destination: edk2-build-config

  - runs: |
      git submodule update --init

      # Build both arches when we have cross-compilers
      case "${{build.arch}}" in
        "aarch64")
          match=armvirt.aa64.qemu.vars
          ;;
        "x86_64")
          match=ovmf.qemu.vars
          ;;
      esac

      # Change to RELEASE build type
      sed -i 's|DEBUG|RELEASE|' edk2-build-config/bin/edk2-build.py

      # Build firmware
      edk2-build-config/bin/edk2-build.py --jobs 16 --config edk2-build-config/distro/fedora --silent --version-override ${{package.full-version}} --match $match
      # Matching filepaths with Fedora
      mkdir -p ${{targets.destdir}}/usr/share/edk2
      cp -r Fedora/* ${{targets.destdir}}/usr/share/edk2/

update:
  enabled: true
  github:
    identifier: tianocore/edk2
    strip-prefix: edk2-stable
    tag-filter: edk2-stable
