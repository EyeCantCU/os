package:
  name: py3.11-pytorch-cuda-11.8
  version: 2.3.1
  epoch: 11
  description: Tensors and Dynamic neural networks in Python with strong GPU acceleration
  copyright:
    - license: BSD-3-Clause
  resources:
    cpu: 32
  options:
    no-depends: true
  dependencies:
    provides:
      - pytorch-cuda-11.8=${{package.full-version}}
    runtime:
      - cuda-toolkit-11.8
      # For some reason this isn't detected
      - libgomp
      - nvidia-cuda-cupti-11.8
      - nvidia-nccl-cuda-11.8
      - openmp-17-dev
      - py3.11-numpy-2.2
      - py3.11-pyyaml
      - py3.11-typing-extensions

var-transforms:
  - from: ${{package.name}}
    match: '.*cuda-(\d+\.\d+).*'
    replace: '$1'
    to: cuda-version
  - from: ${{package.name}}
    match: '.*cuda-(\d+)\.\d+$'
    replace: '$1'
    to: cuda-major-version
  - from: ${{package.name}}
    match: '.*cuda-(\d+)\.(\d+)$'
    replace: 'cu$1$2'
    to: cuda-pyvariant

environment:
  environment:
    GCC_SPEC_FILE: "/dev/null"
  contents:
    packages:
      - bash
      - build-base
      - busybox
      - ca-certificates-bundle
      - cmake-3
      - coreutils
      - cuda-toolkit-11.8-dev
      - gcc-11-default
      - git
      - isl-dev
      - libffi-dev
      - libgomp
      - libjpeg-dev
      - libjpeg-turbo-dev
      - libpng-dev
      - mpc-dev
      - nvidia-cuda-cccl-11.8
      - nvidia-cuda-cupti-11.8-dev
      - nvidia-cuda-profiler-api-11.8
      - nvidia-cudnn-8-cuda-11-dev
      # Cuda
      - nvidia-driver-11.8
      - nvidia-nccl-cuda-11.8-dev
      - openmp-17-dev
      - openssl-dev
      - patchelf
      - posix-libc-utils
      - py3.11-build
      - py3.11-installer
      - py3.11-numpy-2.2
      - py3.11-pip
      - py3.11-pyyaml
      - py3.11-setuptools
      - py3.11-typing-extensions
      - py3.11-wheel
      - python-3.11
      - python-3.11-dev
      - samurai

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/pytorch/pytorch.git
      tag: v${{package.version}}
      expected-commit: 63d5e9221bedd1546b7d364b5ce4171547db12a9
      recurse-submodules: true

  - uses: patch
    with:
      patches: cstdint.patch serial-func-GHSA-53q9-r3pm-6pq6-fix.patch

  - name: Python Build
    runs: |
      export PYTORCH_BUILD_VERSION="${{package.version}}+${{vars.cuda-pyvariant}}"
      export PYTORCH_BUILD_NUMBER=0
      export PATH="$PATH:/usr/local/cuda-11.8/bin"
      export CMAKE_CUDA_ARCHITECTURES=all
      export USE_CUDA=1
      export USE_CUDNN=1
      export USE_SYSTEM_NCCL=1
      export NCCL_LIB_DIR=/usr/lib
      export NCCL_INCLUDE_DIR=/usr/include
      export CUDNN_HOME="/usr/local/cudnn-8"
      export CUDNN_LIBRARY="/usr/local/cudnn-8/lib64"
      export CUDNN_INCLUDE_DIR="/usr/local/cudnn-8/include"
      export TORCH_CUDA_ARCH_LIST="5.2 6.0 6.1 7.0+PTX 8.0 8.6 8.7 8.9 9.0"
      export TORCH_NVCC_FLAGS="-Xfatbin -compress-all"
      export CUDA_VERSION="11.8"
      export MAX_JOBS="$(nproc --ignore=2)"

      # see pytorch doc on GLIBCXX_USE_CXX11_ABI at
      # https://github.com/pytorch/pytorch/blob/b4b1480b0634a5fb7e116053464301d192e2583a/README.md?plain=1#L236
      # and https://gcc.gnu.org/onlinedocs/libstdc++/manual/using_dual_abi.html
      export _GLIBCXX_USE_CXX11_ABI=1

      # Disable some warnings
      export CFLAGS="$CFLAGS -Wno-error=nonnull -Wno-nonnull"
      export CXXFLAGS="$CXXFLAGS -Wno-error=nonnull -Wno-nonnull"

      python3.11 -m pip install -r requirements.txt
      python3.11 -m build --no-isolation --skip-dependency-check

  - runs: |
      # Setup the virtualenv
      python3.11 -m venv .venv --system-site-packages
      .venv/bin/pip install --no-compile -r requirements.txt

      # Remediate  CVE-2024-6345 and CVE-2022-40897
      .venv/bin/pip install --upgrade setuptools==70.0.0

      .venv/bin/pip install --no-compile dist/*.whl
      .venv/bin/pip install --no-compile typing-extensions

  - name: check libcupti support (customer-issues/issues/1938)
    runs: |
      sitepkgd=$(.venv/bin/python3.11 -c 'import site; print(site.getsitepackages()[0])')
      ldd $sitepkgd/torch/lib/libtorch_cuda.so | grep libcupti

  - runs: |
      mkdir -p ${{targets.destdir}}/usr/share/pytorch
      mv .venv ${{targets.destdir}}/usr/share/pytorch/

      # edit the venv paths
      sed -i "s|/home/build|/usr/share/pytorch|g" ${{targets.destdir}}/usr/share/pytorch/.venv/bin/*

      # allow site-packages
      sed -i "s|include-system-site-packages = false|include-system-site-packages = true|g" ${{targets.destdir}}/usr/share/pytorch/.venv/pyvenv.cfg

subpackages:
  - name: py3.11-wheels-torch-cuda-11.8
    pipeline:
      - runs: |
          mkdir -p ${{targets.contextdir}}/usr/share/wheels
          cp dist/*.whl ${{targets.contextdir}}/usr/share/wheels

update:
  enabled: true
  github:
    identifier: pytorch/pytorch
    strip-prefix: v
    tag-filter: v2.3.

test:
  environment:
    contents:
      packages:
        - nvidia-cudnn-8-cuda-11
  pipeline:
    - runs: |
        export PATH=/usr/share/pytorch/.venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

        python3 -c "import torch"
