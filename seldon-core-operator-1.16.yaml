package:
  name: seldon-core-operator-1.16
  version: 1.16.0
  epoch: 10
  description: Controls Seldon Deployments in the Kubernetes cluster
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - go

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/SeldonIO/seldon-core/
      tag: v${{package.version}}
      expected-commit: 116fd011badbf0d29811eb819ccea012c98df920

  - uses: go/bump
    with:
      deps: github.com/emicklei/go-restful@v2.16.0 google.golang.org/protobuf@v1.33.0 golang.org/x/net@v0.23.0
      replaces: github.com/onsi/ginkgo=github.com/onsi/ginkgo@v1.16.5
      tidy-compat: 1.17
      modroot: ./operator

  - working-directory: /home/build/operator
    runs: |
      mkdir -p ${{targets.destdir}}/usr/bin/
      go build -o bin/manager main.go
      install -Dm755 ./bin/manager ${{targets.destdir}}/usr/bin/manager

  - uses: strip

subpackages:
  - name: "seldon-core-operator-compat-helm"
    description: "Compatibility package to place binaries in the location expected by upstream helm charts"
    pipeline:
      - runs: |
          # The helm chart expects the binary to be in / instead of /usr/bin
          mkdir -p "${{targets.subpkgdir}}"
          ln -sf /usr/bin/manager "${{targets.subpkgdir}}"/manager
      - uses: strip

update:
  enabled: false
