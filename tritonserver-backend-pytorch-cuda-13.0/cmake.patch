diff --git a/CMakeLists.txt b/CMakeLists.txt
index 5b0e399..1d96a27 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -160,7 +160,6 @@ set(PT_LIBS
     "libtorch_cuda.so"
     "libtorch_cuda_linalg.so"
     "libtorch_global_deps.so"
-    "libjpeg.so.62"
 )
 
 if (${TRITON_PYTORCH_NVSHMEM})
@@ -173,7 +172,7 @@ endif() # TRITON_PYTORCH_NVSHMEM
 if (${TRITON_PYTORCH_ENABLE_TORCHVISION})
   set(PT_LIBS
       ${PT_LIBS}
-      $<IF:$<BOOL:${RHEL_BUILD}>,libtorchvision.so,libtorchvision.so.1>
+      "libtorchvision.so"
   )
 endif() # TRITON_PYTORCH_ENABLE_TORCHVISION
 
@@ -374,8 +373,8 @@ if (${TRITON_PYTORCH_DOCKER_BUILD})
 
   if (${TRITON_PYTORCH_ENABLE_TORCHVISION})
     set(TRITON_PYTORCH_LIBS
-    ${TRITON_PYTORCH_LIBS}
-    "${CMAKE_CURRENT_BINARY_DIR}/$<IF:$<BOOL:${RHEL_BUILD}>,libtorchvision.so,libtorchvision.so.1>")
+        ${TRITON_PYTORCH_LIBS}
+        "${CMAKE_CURRENT_BINARY_DIR}/libtorchvision.so")
   endif() # TRITON_PYTORCH_ENABLE_TORCHVISION
 
   if (${TRITON_PYTORCH_ENABLE_TORCHTRT})
@@ -494,18 +493,27 @@ else()
       ${PT_LIB_PATHS}
     DESTINATION ${CMAKE_INSTALL_PREFIX}/backends/pytorch
   )
-
-  FOREACH(plib ${PT_LIBS})
+  # Manually install OpenCV shared libraries when not using Docker-based build
+  FOREACH(opencvlib ${OPENCV_LIBS})
     install(
-      CODE
-        "EXECUTE_PROCESS(
-          COMMAND patchelf --set-rpath \$ORIGIN ${plib}
-          RESULT_VARIABLE PATCHELF_STATUS
-          WORKING_DIRECTORY ${CMAKE_INSTALL_PREFIX}/backends/pytorch)
-        if(PATCHELF_STATUS AND NOT PATCHELF_STATUS EQUAL 0)
-          message(FATAL_ERROR \"FAILED: to run patchelf\")
-        endif()"
+      FILES
+        "${TRITON_PYTORCH_LIB_PATHS}/${opencvlib}"
+      DESTINATION ${CMAKE_INSTALL_PREFIX}/backends/pytorch
     )
+  ENDFOREACH()
+
+  FOREACH(plib ${PT_LIBS})
+    # install(
+    #   CODE
+    #     "EXECUTE_PROCESS(
+    #       COMMAND patchelf --set-rpath \$ORIGIN ${plib}
+    #       RESULT_VARIABLE PATCHELF_STATUS
+    #       WORKING_DIRECTORY ${CMAKE_INSTALL_PREFIX}/backends/pytorch)
+    #     if(PATCHELF_STATUS AND NOT PATCHELF_STATUS EQUAL 0)
+    #       message(FATAL_ERROR \"FAILED: to run patchelf\")
+    #     endif()"
+    # )
+    message(WARNING "Skipping patchelf for ${plib} since TRITON_PYTORCH_DOCKER_BUILD is OFF")
   ENDFOREACH(plib)
 endif() # TRITON_PYTORCH_DOCKER_BUILD
 
@@ -520,6 +528,17 @@ install(
     ${INSTALL_CONFIGDIR}
 )
 
+if (${TRITON_PYTORCH_ENABLE_TORCHTRT})
+  if (EXISTS "/usr/bin/torchtrtc")
+    install(
+      FILES /usr/bin/torchtrtc
+      DESTINATION ${CMAKE_INSTALL_PREFIX}/backends/pytorch
+    )
+  else()
+    message(WARNING "TorchTRT is enabled but /usr/bin/torchtrtc was not found")
+  endif()
+endif()
+
 install(
   FILES
     src/model.py
