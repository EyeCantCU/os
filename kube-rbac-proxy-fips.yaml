package:
  name: kube-rbac-proxy-fips
  version: 0.17.1
  epoch: 2
  description: Kubernetes RBAC authorizing HTTP proxy for a single upstream.
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - build-base
      - busybox
      - ca-certificates-bundle
      - go-fips

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/brancz/kube-rbac-proxy
      tag: v${{package.version}}
      expected-commit: 59cc60538b043297cf6e2a988badfb516d353c3a

  - uses: go/bump
    with:
      deps: google.golang.org/protobuf@v1.33.0 go.opentelemetry.io/contrib/instrumentation/google.golang.org/grpc/otelgrpc@v0.46.1 go.opentelemetry.io/contrib/instrumentation/net/http/otelhttp@v0.46.1 go.opentelemetry.io/otel/exporters/otlp/otlptrace/otlptracegrpc@v1.21.0

  - uses: go/build
    with:
      go-package: go-fips
      packages: ./cmd/kube-rbac-proxy
      output: kube-rbac-proxy
      ldflags: -X k8s.io/component-base/version.gitVersion=v${{package.version}} -X k8s.io/component-base/version.gitCommit=$(git rev-parse HEAD) -X k8s.io/component-base/version/verflag.programName=kube-rbac-proxy

update:
  enabled: true
  github:
    identifier: brancz/kube-rbac-proxy
    strip-prefix: v
    tag-filter: v

test:
  pipeline:
    - uses: test/go-fips-check
