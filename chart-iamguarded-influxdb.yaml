package:
  name: chart-iamguarded-influxdb
  version: "7.1.6"
  epoch: 0
  description: influxdb iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: influxdb/6.6.11
      expected-commit: 40152ae66be8dddb6b7123ceb5d1294c0fba8d7a

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/influxdb

  - uses: charts/package
    with:
      chart-path: iamguarded/influxdb

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: influxdb/
    use-tag: true
    tag-filter-prefix: influxdb/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /influxdb
