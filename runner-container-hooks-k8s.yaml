package:
  name: runner-container-hooks-k8s
  version: "0.8.0"
  epoch: 0
  description: Runner Container Hooks for GitHub Actions
  copyright:
    - license: MIT

environment:
  contents:
    packages:
      - busybox
      - ca-certificates-bundle
      - nodejs
      - npm

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/actions/runner-container-hooks
      tag: v${{package.version}}
      expected-commit: c03a5fb3c1896123e335cef31dc533ef376191d1

  - name: Install dependencies and build
    runs: |
      # Install dependencies for all packages (bootstrap)
      npm run bootstrap

      # Build all TypeScript packages
      npm run build-all

      mkdir -p "${{targets.destdir}}/usr/share/actions-runner-container-hooks/k8s"
      cp -r packages/k8s/dist/* "${{targets.destdir}}/usr/share/actions-runner-container-hooks/k8s/"

  - uses: strip

update:
  enabled: true
  github:
    identifier: actions/runner-container-hooks
    strip-prefix: v
    use-tag: true

test:
  pipeline:
    - runs: |
        test -d /usr/share/actions-runner-container-hooks/k8s
