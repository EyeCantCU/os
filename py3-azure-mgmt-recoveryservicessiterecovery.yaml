package:
  name: py3-azure-mgmt-recoveryservicessiterecovery
  version: 1.2.0
  epoch: 0
  description: This is the Microsoft Azure Recovery Services Site Recovery Management Client Library.
  copyright:
    - license: MIT
  dependencies:
    runtime:
      - py3-azure-common
      - py3-azure-mgmt-core
      - py3-msrest
      - python-3

environment:
  contents:
    packages:
      - ca-certificates-bundle
      - wolfi-base
      - busybox
      - build-base

pipeline:
  - uses: git-checkout
    with:
      expected-commit: fd11a988c5d4986d9d7b544aa11bc20d7d669c26
      repository: https://github.com/Azure/azure-sdk-for-python
      tag: azure-mgmt-recoveryservicessiterecovery_${{package.version}}
      destination: ./azure-sdk-for-python

  - runs: |
      mv ./azure-sdk-for-python/sdk/recoveryservices/azure-mgmt-recoveryservicessiterecovery/* .

  - name: Python Build
    uses: python/build-wheel

  - uses: strip

update:
  # The azure repo has some tags that have a bN suffix, which
  # is not a valid version, so ignore those.
  ignore-regex-patterns:
    - b
  enabled: true
  github:
    identifier: Azure/azure-sdk-for-python
    tag-filter-contains: azure-mgmt-recoveryservicessiterecovery_
    strip-prefix: azure-mgmt-recoveryservicessiterecovery_
    use-tag: true

test:
  environment:
    contents:
      packages:
        - wolfi-base
  pipeline:
    - runs: python -c 'import azure.mgmt.recoveryservicessiterecovery'
