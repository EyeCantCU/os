awspub:
  images:
    "chainguard-{{APPNAME}}-arm64-$BUILD_TIMESTAMP":
      share:
        # End-users to recieve tested image
        # - "arn:aws:organization::something"
      regions:
        # Extra regions for tested images
        # - "us-east-1"
