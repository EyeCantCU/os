version: 1
cloud: azure
data:
  vmuser: azureuser
  region: eastus
vmconfigs:
  x86_64:
    - vm_size: Standard_F2s_v2
      disk_type: StandardSSD_LRS
      tests:
        - group: core
        - group: azure
          # TODO: make this work
          #- vm_size: Standard_F2as_v6
          #  disk_type: StandardSSD_LRS
          #  tests:
          #    - group: core
    - vm_size: Standard_D2_v4
      # The variant of this that includes local storage (Dpdsv6)
      # doesn't supported nested virt which we want to test with
      # hypervisor test, so don't run azure tests.
      disk_type: StandardSSD_LRS
      tests:
        - group: core
        - group: hypervisor
  aarch64:
    - vm_size: Standard_D2pds_v6
      disk_type: StandardSSD_LRS
      tests:
        - group: core
    - vm_size: Standard_E2pds_v6
      disk_type: StandardSSD_LRS
      tests:
        - group: core
