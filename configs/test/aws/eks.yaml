version: 1
cloud: aws
data:
  vmuser: ec2-user
vmconfigs:
  x86_64:
    - instance_type: t3.medium
      tests:
        # TODO: run the whole core test group again
        # when we can run it in EKS or supply some userdata
        # that makes nodeadm happy in EC2
        - name: core/ssh
        - name: core/kernel
  aarch64:
    - instance_type: t4g.medium
      tests:
        - name: core/ssh
        - name: core/kernel
