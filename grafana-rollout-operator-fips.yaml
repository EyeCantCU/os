package:
  name: grafana-rollout-operator-fips
  version: "0.31.0"
  epoch: 0 # CVE-2025-47907
  description: Kubernetes Rollout Operator
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - go-fips-1.25

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/grafana/rollout-operator
      tag: v${{package.version}}
      expected-commit: 4dbcfff9dac6a290eaf9c4e1396470de7a858919

  - uses: go/build
    with:
      packages: ./cmd/rollout-operator
      output: rollout-operator
      go-package: go-fips

test:
  pipeline:
    - uses: test/go-fips-check
    - runs: |
        rollout-operator --help

update:
  enabled: true
  github:
    identifier: grafana/rollout-operator
    strip-prefix: v
