package:
  name: chart-iamguarded-mlflow
  version: "5.0.4"
  epoch: 2
  description: Mlflow iamguarded chart
  copyright:
    - license: Apache-2.0

environment:
  contents:
    packages:
      - chart-iamguarded-common
      - chart-iamguarded-minio
      - chart-iamguarded-postgresql

pipeline:
  - uses: auth/github
    with:
      repo: chainguard-dev/iamguarded-charts

  - uses: git-checkout
    with:
      repository: https://github.com/chainguard-dev/iamguarded-charts
      tag: mlflow/${{package.version}}
      expected-commit: 35ee5a3568e2de13d3dc16aa191408624b3e12aa

  - uses: patch
    with:
      patches: iamguarded.patch

  - uses: iamguarded/prepare
    with:
      chart-path: iamguarded/mlflow

  - uses: charts/package
    with:
      chart-path: iamguarded/mlflow

update:
  enabled: true
  github:
    identifier: chainguard-dev/iamguarded-charts
    strip-prefix: mlflow/
    use-tag: true
    tag-filter-prefix: mlflow/

test:
  pipeline:
    - uses: auth/github
      with:
        repo: chainguard-dev/iamguarded-charts
    - uses: iamguarded/validate
      with:
        chart-name: /mlflow
